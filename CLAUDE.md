# Declyne

Personal financial reset system for Josh. Single user. Not a product.
Read `skills/declyne-session.md` first, then the skill for whatever slice you're building.

## Mission

Declyne exists for ordinary people who never had a financially literate family to learn from. Not investors. Not people with accountants. People whose money is getting away from them and who don't fully understand why.

The goal is to give those people back control of their money and, over time, teach them the skills to keep it. Real bookkeeping concepts. Real financial vocabulary. Introduced gradually, earned through use, never condescending.

**The three-way standard every design decision must clear:**
- A person who has never balanced a chequebook can use it without feeling lost or dumb
- A full-stack engineer looking at the code sees clean, deliberate architecture
- A UI/UX designer and a marketer both look at it and think: this is sharp, this is different, this doesn't look like every other fintech app

Friction is the enemy. Jargon without context is the enemy. Generic is the enemy.

The receipt motif, the ledger desk, the chits and stamps and perforations — these are not decoration. They are the teaching tool. The app looks like bookkeeping so the user starts to think like a bookkeeper, without ever being told that's what's happening.

## Stack

React 19 + Vite 7 + Tailwind v4 · Hono 4 on Cloudflare Workers · D1 + Drizzle · Capacitor 8 iOS sideload

## Live infrastructure

- Worker: `https://declyne-api.bocas-joshua.workers.dev`
- D1 database: `declyne` (`0893ada8-cbdb-4b3c-896c-c93c792023f1`)
- Cloudflare account: `bocas.joshua@gmail.com` (`59b6cf53e5d4cef04586e1deb177093c`)
- Worker secrets set: `API_TOKEN`, `OPENAI_API_KEY`, `TWELVE_DATA_KEY`, `FMP_KEY`
- Latest worker version: `a9919443-16af-49d5-b824-8f859b6158a6` (session 91 — goal_type + smart suggestions)

## Repo state (2026-04-29 handoff, end of session 91)

Sessions through 57 committed to local main. **Commit `71be8cb` bundled sessions 56 + 57**: action vocabulary v2 (tear-tab motion glyph + flanking cut-lines for primary actions, postage stamp tilted with dashed perforation for ceremonies, ink margin glyphs in mascot-purple for inline actions, sticker/appliqué for ad-hoc, swept all 23 remaining `btn-primary` form-submits to `.stamp .stamp-square` → zero generic purple pills remain) + e-transfer payment links (unauthed public `/pay/:token` HTML landing page at workers.dev, Settings Interac email + security answer, Counterparty "Send link" stamp with share/copy, auto-disable on settlement). Mockup at [`/mockup/buttons`](apps/client/src/pages/ButtonsMockup.tsx) shows vocabulary in context. Build: 423.24 kB JS / 41.45 kB CSS. Tests: 193 pass. Worker auto-deploys via CI on push; client live at workers.dev. **Ready for `git push origin main`.** Sessions 1-55 history below. Sessions 1-8 squashed in `67b52f2`; sessions 9-42 each their own commit; sessions 43-48 shipped as the **security/compliance uplift series**; session 49 cleared three security/compliance follow-ups; session 50 shipped automated D1 -> R2 backup; session 51 kicked off a **6-phase showcase/multi-user groundwork program** with Phase 1 (depersonalize the schema — split direction enum renamed `josh_owes`/`owes_josh` → `i_owe`/`they_owe`, drop "Josh" from OpenAI prompt, change capacitor `appId` to `com.bowgull.declyne`); session 52 redesigned the Today header as a 3-column edition plate; session 53 restructured the Today body to "one hero + one queue", dropped the PNG-mask `D` in the wordmark, bumped the mascot to 64px, and extended `HORIZON_DAYS` to 30; session 54 fixed Today hero color thresholds (streak gold ≥2, payday ink>14d/gold≤14d/purple≤7d), wired payday color to the header caption span, fixed a critical pay-period bug (all "current period" queries used `ORDER BY start_date DESC LIMIT 1` which returned future periods when a future paycheque transaction existed — now guarded by `WHERE start_date <= date('now')` in 8 query sites across `today.ts`/`budget.ts`/`periods.ts`/`allocations.ts`), and renamed `Bowgull (Mexico)` debt + `Bowgull` counterparty to `Luther` (live D1 UPDATE + seed file); session 55 deduped the Today header (`{daysLeft}D TO PAYDAY` → `WK NN`), bumped seed paycheques 240000 → 425000 cents so the Tank reads ~$506 instead of clamping to $0, overhauled chit-form UX (chit now renders inline under the long-pressed counterparty, two-tone paper-shade bg makes the saw-tooth tear edge visible, `.stamp-filled` class gives the OWES YOU/YOU OWE toggle a real selected state), and **locked the no-custom-domain constraint** (Josh stated zero spend on infrastructure — workers.dev URL is the surface forever, all CLAUDE.md references to `declyne.app`/`pay.declyne.app` are now obsolete and need rewriting when session 52 ships). Worker auto-deploys via CI on every push to `main` ([`.github/workflows/deploy-worker.yml`](.github/workflows/deploy-worker.yml)). Per-session details in memory file `project_declyne.md`. Test data seeded into remote D1 via `apps/worker/drizzle/seed_test.sql` (4 accounts, ~90d transactions, 3 debts including Luther (Mexico), 3 credit snapshots, 2 holdings + prices, market snapshot, goal, review item, **4 counterparties + 4 splits** -- Luther Mexico $1,100, Marcus Chen $47.50 owes-you Lady Marmalade brunch, Priya Shah $82 you-owe Bar Raval tapas, Diego Alvarez $36 owes-you Golden Turtle dinner).

## Key commands

```
pnpm dev              # client on localhost (preview panel)
pnpm dev:worker       # worker on localhost:8787
pnpm --filter @declyne/worker run deploy   # push worker to Cloudflare (pnpm worker:deploy collides with pnpm deploy keyword)
pnpm db:push          # push schema to remote D1
pnpm test             # 466 tests, all passing (worker 458 + client 8; session 91 added 13 goal-type/suggestion tests)
pnpm cap:run          # build + sync + open Xcode (iOS sideload)
```

## What's built (through session 91, 2026-04-29)

- **Goal types + smart suggestions + 14 sub-cat colors + 4-role paycheque colors + stack-mode batch approve, session 91** (2026-04-29): User wanted Goals to teach (typical goals, smart amounts, why), the sub-category queue to support batch approve via long-press (no checkboxes — brand-appropriate), and every sub-category + paycheque-role to read at a glance via its own color. Migration [`0023_goal_types.sql`](apps/worker/drizzle/0023_goal_types.sql) applied to remote D1 (34 tables, additive). Adds nullable-default `goals.goal_type TEXT NOT NULL DEFAULT 'other' CHECK IN (emergency,vacation,rrsp,tfsa,fhsa,car,other)` + `goals_type_idx`. **Worker** [`goals.ts`](apps/worker/src/routes/goals.ts) extended: `parseGoalInput` + `parseGoalPatch` accept `goal_type` (validated via `isGoalType`); GET / + POST + PATCH read/write the column. New `GET /api/goals/suggest?type=...` route runs pure helper `computeGoalSuggestion(type, inputs)` against live `paycheque_cents` (latest pay_period guarded by `<= today`), `essentials_monthly_cents` (manual setting → `_derived` fallback), TFSA/FHSA contribution room (queries `gl_accounts` for `Assets:Investments:TFSA:%` / `FHSA:%` paths joined to JE lines posted ≥ year-start, subtracts from $7,000/$8,000 limits). Returns `{suggestion: {goal_type, name, target_cents, per_paycheque_cents, why_target, why_paycheque}, inputs}`. Math: emergency = 3 months essentials × 5% paycheque, RRSP = 4% income (rationale changes when room provided), TFSA = min(4% income, room), FHSA = full room ($8K), vacation/car/other = sensible defaults. Helper exported for tests. **Client** [`Goals.tsx`](apps/client/src/pages/Goals.tsx) sheet rebuilt: TYPE dropdown at top (7 entries), auto-fetches suggestion on type change in add-mode and auto-fills target + name; tracks `userEditedTarget` + `userEditedName` flags so user typing stops auto-fill from clobbering. Italic mono teaching captions under target ("based on three months of your essentials spend (~$2594/mo). The cushion you keep so a surprise bill doesn't become a credit card.") and per-paycheque ("~$135.00/paycheque · 5% of your $2700 paycheque. Reaches your target in roughly 26 paycheques."). ✦ SUGGESTED gold tag right-aligned on Target field. **14 sub-category colors** added to [`index.css`](apps/client/src/index.css) as CSS tokens: `--sub-food` clay, `--sub-transit` slate-warm, `--sub-shopping` rose, `--sub-home` olive, `--sub-personal-care` peach, `--sub-entertainment` lavender, `--sub-health` sage, `--sub-bars` deep sienna, `--sub-takeout` sienna, `--sub-fast-food` amber, `--sub-weed` moss, `--sub-streaming` petrol, `--sub-gaming` purple, `--sub-treats` pink-rose. **4 paycheque-role colors**: `--pay-bills` slate, `--pay-debt-min` muted gold (the floor), `--pay-debt-extra` brighter amber (the push), `--pay-goal` sage. **NetworkMap** [`NetworkMap.tsx`](apps/client/src/components/NetworkMap.tsx) extended: new `subCategory?` and `payRole?` optional NetworkNode fields; `merchantFill(n)` precedence is subCategory > payRole > cat; `hubStripe(n)` reads subCategory when present so each sub-hub gets its own readable color. **networkData** [`networkData.ts`](apps/client/src/lib/networkData.ts): `buildMoneyNetwork` tags each commit with `payRole` (bill/debt_min/goal); plan extras stamped `debt_extra`. `buildHabitsNetwork` passes the merchant's validated `sub_category` to its node and stamps `subCategory` on `hub_sub_*` hubs so 14-color palette applies. **Stack-mode batch approve** rewrite of [`SubCategoryQueue.tsx`](apps/client/src/components/SubCategoryQueue.tsx): long-press (450ms timer on pointerdown) enters stack mode — pressed row gets a tilted ✓ PICKED red-ink stamp (#c45b5b border + label, varies tilt per row from a 7-value table), row tilts ~0.2deg. Subsequent taps add/remove from `picked` Set. Tap-empty-desk releases via document pointerdown listener attached only when `picked.size > 0` (cheap). Slide-up sticky strip at bottom: "{N} PICKED · TAP EMPTY SPACE TO RELEASE" + tilted "APPROVE STACK" purple stamp. Approve mutation runs sequential PATCH per row (each keeps detector guess or override) then invalidates queue + merchants/habits queries. Inline tray editor only opens when `picked.size === 0`. Section meta now shows "long-press to stack" hint. Each row's guess-hint shows the inline color dot using the new sub-cat tokens (Starbucks→pink-rose treats dot etc). **Mockups upgraded into product:** the three /mockup/{goals, habits-stack, paycheque-colors} pages remain as design references; canonical surfaces (/goals cream-receipt, /books + /books?view=patterns dark-ledger) carry the same vocabulary in their respective brand contexts. **Tests:** existing `goals.test.ts` updated for new shape (toEqual now includes `goal_type:'other'`); 11 new `computeGoalSuggestion` tests + 2 new parse tests = 13 added (worker 449→458). Total 466 (worker 458 + client 8). **Worker deployed:** `c3c30afe` then redeployed `a9919443` after fixing a bug in `/api/goals/suggest` where SQL referenced `jl.gl_account_id` (actual column is `account_id`) and used `/` as path separator (actual is `:`). **Live verification:** smart suggestion populated $7,781.55 target + $135 per-paycheque on Emergency Fund with full teaching caption; Habits map renders WEED moss-hub + BARS deep-sienna-hub + Tokyo Smoke moss + LCBO/Beer Store sienna + McDonald's amber + Pai/Banh Mi/Uber Eats clay; sub-categories queue stack-mode tested via JS-simulated long-press, ✓ PICKED stamp lands on Starbucks at -1.4deg, slide-up strip shows "1 PICKED · APPROVE STACK". Console clean. **Build:** 598.67 kB JS (+31.78 kB vs session 90) / 46.77 kB CSS (+0.43 kB). **Files added:** `apps/worker/drizzle/0023_goal_types.sql`. **Files changed:** `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/goals.ts`, `apps/worker/src/__tests__/goals.test.ts`, `apps/client/src/index.css`, `apps/client/src/components/NetworkMap.tsx`, `apps/client/src/lib/networkData.ts`, `apps/client/src/components/SubCategoryQueue.tsx`, `apps/client/src/pages/Goals.tsx`, `apps/client/src/pages/GoalsMockup.tsx` (typecheck fix), `apps/client/src/App.tsx`. **Decisions locked:** (1) Goal types are 7 enum values; the dropdown is the only way to set type. (2) Smart suggestion auto-fills target + name once on type change in add-mode; user typing flips edit-flag and stops auto-fill from clobbering. The teaching caption stays regardless. (3) Goals page uses cream receipt vocabulary (matches /goals canonical). SubCategoryQueue lives on Books which is dark-ledger, so the stack-mode UI uses ink-on-rule + mono uppercase, not cream perforations. (4) PICKED stamp red ink, APPROVE STACK mascot-purple — mirrors session 67's vocabulary. (5) Approve sequentially (await per row) so a single failure surfaces clearly. (6) Sub-category color resolution precedence: subCategory > payRole > cat. Always falls through cleanly. (7) Subscription safety net deferred until we observe an actual miscategorized merchant case in the wild.

- **Sub-category system on Habits map; AUTOPILOT hub killed, session 90** (2026-04-29): Implements the locked plan from end-of-session-89. Migration [`0022_merchant_sub_category.sql`](apps/worker/drizzle/0022_merchant_sub_category.sql) adds nullable `merchants.sub_category` + `sub_category_confirmed` (default 0) + indexes. Applied to remote D1 (34 tables). **Detector lib** [`apps/worker/src/lib/subCategoryDetect.ts`](apps/worker/src/lib/subCategoryDetect.ts): pure name-pattern matcher with locked vocab — LIFESTYLE_SUBS (food/transit/shopping/home/personal_care/entertainment/health), INDULGENCE_SUBS (bars/takeout/fast_food/weed/streaming/gaming/treats). Group-aware: passing 'lifestyle' never returns indulgence. Returns null when nothing matches. Helpers `isSubCategory`, `isValidForGroup`, `SUB_LABELS` exported. **Worker routes** in [`merchants.ts`](apps/worker/src/routes/merchants.ts): `GET /api/merchants/sub-categories/queue` returns unconfirmed lifestyle+indulgence merchants with spend ≥1 cent, sorted by 90d spend desc; `POST /api/merchants/sub-categories/backfill` runs detector against every unconfirmed discretionary merchant (one-shot, idempotent). PATCH /:id accepts `sub_category` + `sub_category_confirmed` and writes `merchant_sub_category` edit_log rows (added to allowlist). **Import** [`import.ts`](apps/worker/src/routes/import.ts) calls `detectSubCategory(description_raw)` for every new merchant insert — guess lands on the row immediately without confirming. **Queue UI** [`apps/client/src/components/SubCategoryQueue.tsx`](apps/client/src/components/SubCategoryQueue.tsx): kicker `Sub-categories · N to confirm`, ledger rows show name + guess + group + 90d spend. Tap expands an in-row tray with all 14 sub-category buttons (regardless of group, so user can correct cross-group misfits like Uber Eats coming in as lifestyle/transit when it should be indulgence/takeout). Approve stamp PATCHes `{sub_category, sub_category_confirmed: 1}` and invalidates `sub-category-queue` + `merchants` queries. Mounted at top of PATTERNS view in [`Budget.tsx`](apps/client/src/pages/Budget.tsx); Habits renumbered to 02; Standing orders 03; Spending history 04; Period surplus 05. **Habits map rewrite** [`networkData.ts`](apps/client/src/lib/networkData.ts) `buildHabitsNetwork`: AUTOPILOT hub deleted entirely (subscriptions live exclusively on Books → Standing orders now). Lifestyle/Indulgence moral hubs replaced by sub-category hubs (FOOD, TRANSIT, BARS, WEED, FAST FOOD, etc), only emitted when ≥1 merchant routes there. Lifestyle subs use `hubKind: 'lifestyle'` (clay), indulgence subs use `hubKind: 'indulgence'` (sienna). Unconfirmed merchants cluster under a single slate UNCONFIRMED hub with audit-tape obs `N merchants need a sub-category`. Group/sub mismatches (sub_category set but doesn't match the group's allowed list) also route to UNCONFIRMED — visible signal that something needs a verdict. SubscriptionShape param retained for callsite stability but ignored. **Backfill ran on remote** post-deploy: scanned=20 updated=9. Confirmed live in preview at /books?view=patterns: queue rendered 16 unconfirmed (Tokyo Smoke→weed gold-stamped, LCBO→bars, McDonald's→fast_food, etc; Banh Mi/Netflix/Spotify/Pai Northern Thai/Bar Raval/Lady Marmalade/Apple iCloud/Value Village/Golden Turtle as `unrecognized`); Netflix approve flow tested end-to-end dropped queue 16→14; Habits map renders WEED + BARS + FAST FOOD + SHOPPING + UNCONFIRMED hubs in two-tone (clay vs sienna). **Tests:** 21 new in [`subCategoryDetect.test.ts`](apps/worker/src/__tests__/subCategoryDetect.test.ts) covering isSubCategory, isValidForGroup, group-constrained detection (lifestyle vs indulgence), unknown-group fall-through, and explicit reject of cross-group matches. Worker total 428→449. Typecheck clean. Build clean (566.89 kB JS, +3.5 kB / 46.34 kB CSS). **Worker deployed:** `f5b381f2-412b-4db2-b956-b117b9fac686`. **Files added:** `apps/worker/drizzle/0022_merchant_sub_category.sql`, `apps/worker/src/lib/subCategoryDetect.ts`, `apps/worker/src/__tests__/subCategoryDetect.test.ts`, `apps/client/src/components/SubCategoryQueue.tsx`. **Files changed:** `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/editLog.ts`, `apps/worker/src/routes/merchants.ts`, `apps/worker/src/routes/import.ts`, `apps/client/src/lib/networkData.ts`, `apps/client/src/pages/Budget.tsx`. **Decisions locked:** (1) Sub-categories scope to lifestyle + indulgence only; essentials/debt/savings/income stay coarse — habit observation is the use case. (2) NULL = unconfirmed; `sub_category_confirmed=0` is what the queue filters on (so a detector guess that the user hasn't accepted still surfaces). (3) Approval makes the row sticky; future imports inherit through existing `normalized_key` collapse. No ML. (4) Queue dropdown shows all 14 subs regardless of merchant group — fixes the seed-data category_group bugs without forcing a separate fix. (5) AUTOPILOT hub on Habits is gone forever; subscriptions = Books only. (6) Unconfirmed merchants get their own slate hub on the map — visual nudge to clear the queue. **Known seed-data tuning for next session:** Uber Eats/Netflix/Spotify/Apple iCloud are seeded as `lifestyle` group when they're really indulgence; detector returns null because indulgence patterns won't fire under lifestyle-constrained search. Either fix seed or relax detector when brand is unambiguous.



- **Remove BubbleMapsMockup playground page, session 89** (2026-04-29): Pure cleanup. No schema, no worker, no new code. Deleted `apps/client/src/pages/BubbleMapsMockup.tsx` and removed its import + `/mockup/bubblemaps` route from `apps/client/src/App.tsx`. Other mockup pages (DraftChitMockup, PaychequeTankMockup, TodayConstellationMockup, BooksMockup) left in place. **Tests:** 428 unchanged. **Build:** 563.36 kB JS (-50 kB vs session 88's 613.60 — the mockup page carried all 8 A-H design variants) / CSS unchanged. Worker unchanged, no deploy. **Files deleted:** `apps/client/src/pages/BubbleMapsMockup.tsx`. **Files changed:** `apps/client/src/App.tsx` (import + route removed).

- **Money + Habits maps replaced with obsidian-style network graphs on the canonical Books page, session 88** (2026-04-29): Pure client. No schema, no migrations, no worker code. Triggered by user reaction to session 86's constellation: "this is looking good!!! my only questions now is do we need the pay slip $2700 in there? what is it SHOW what is it SAYING to the intended user and can we get some more colours?" — and prior turn pointing at `https://obsidian.md/` as the visual reference. Iterative design pass in `/mockup/bubblemaps` across variants A-H. User picked H (paper index-card hubs, paper perforation edges, tap-to-pin, audit tape, no drift, iPhone-native), then approved shipping to canonical Books. **Three new CSS vars** in [`apps/client/src/index.css`](apps/client/src/index.css): `--hub-personal: #bf8a8a` (dusty rose, money owed to people not banks), `--hub-autopilot: #5a9b8e` (deep teal, recurring/scheduled), `--hub-time: #2d3a52` (midnight navy, temporal — defined but not yet used in canonical surface). **New component** [`apps/client/src/components/NetworkMap.tsx`](apps/client/src/components/NetworkMap.tsx) (~600 lines): generic obsidian-style network for Money + Habits. Props `{ nodes, edges, mode: 'money'|'habits', destOf?, height, showAmount, empty }`. Node kinds: `merchant` (filled coin in category color), `hub` (paper index card with kind-specific top stripe), `core` (PAY SLIP stamp — mascot purple stripe, DEPOSITED wax mark, content drives live signal `${days}d / TO PAYDAY`). HubKind enum: `institution` (gold), `goals` (sage), `personal` (rose), `autopilot` (teal), `time` (navy), `bills` (slate), `lifestyle` (clay), `indulgence` (sienna). Edges = paper perforations `stroke-dasharray="3 4"`, default opacity 0.13/0.06, lit at 0.85 mascot purple. Tap-to-pin: tap any node pins it, lit-subgraph highlights, others fade to 18%, audit tape strip shows the bookkeeper's read (hub: `LABEL · N flows · $X`; merchant: `LABEL · obs`). Tap empty desk releases. PINNED ink-stamp drops top-right when active. iPhone-native: 22pt minimum tap radius for merchants, 20pt-larger invisible hit pad behind hubs, `touch-action: manipulation`, no hover, no drift, no zoom/pan. Force-directed `relax(placed, padding=6, iters=120)` repels overlapping nodes using `effectiveR = max(card or r) + label_clearance`. Two-pass SVG render: shapes first, merchant labels in a second pass with `paint-order: stroke; stroke: #0a070c; strokeWidth: 3; strokeLinejoin: round` (ink halo around glyphs) so labels stay legible without backing rectangles. Hub ring x=152 / y=142 (tightened from 168/148 after first preview clipped CAPITAL ONE label at left edge); `hubAngleOffset = π/4` only when 4 hubs. Money commits placed at 0.6 of way from core to destination + perpendicular spread (sameDest=2 → ±28; sameDest=3+ → (idx-(n-1)/2)*40). Label truncation at 18 chars. **Core PAY SLIP carries live signal**: `${days}d / TO PAYDAY` not duplicated amount — the headline above already says the amount, so the center stops being decoration and starts being the heartbeat (option C from the brainstorm). **New lib** [`apps/client/src/lib/networkData.ts`](apps/client/src/lib/networkData.ts): `buildMoneyNetwork(snapshot, plan, paychequeCents)` and `buildHabitsNetwork(merchants, subscriptions)`. Helpers: `INSTITUTION_PATTERNS` array (visa/mastercard/capital one/td/rbc/etc.) + `isInstitution(name)` routes destination hubs to gold (institution) or rose (personal). `extractDestRoot(label)` strips `→` prefix and ` min`/` extra`/` priority`/` avalanche` suffixes for stable hub IDs. Money network: core node always present; GOALS hub if any savings line; BILLS hub if any bill line; per-debt destination hub created lazily via `ensureHub(id, label, kind)`. Plan extras (push allocations) become `→ DebtName` merchant nodes routed to the same destination hub as the corresponding min. Habits network: top 9 lifestyle+indulgence merchants by 90d spend; LIFESTYLE/INDULGENCE hubs only emitted when ≥1 merchant in that group; AUTOPILOT hub emitted if any merchant.id appears in subscriptions list. Edges: every commit ↔ core (primary) + commit ↔ destination hub; every habit merchant ↔ category hub + (if recurring) ↔ AUTOPILOT (primary). **Wiring.** [`apps/client/src/pages/Budget.tsx`](apps/client/src/pages/Budget.tsx): both `<Constellation mode="clock">` (Money) and `<Constellation mode="frequency">` (Habits) replaced with `<NetworkMap>`. Deleted helper functions `categoryFromGroup`, `hashStr`, `moneyMapBubbles`, `velocityFor` (referenced ConstellationBubble types now gone from imports). PaychequeView calls `buildMoneyNetwork(snapshot, planData, paychequeCents)`; PatternsView calls `buildHabitsNetwork(habitsMerchants, subs)`. Habits sort changed from txn_count to spend_90d_cents desc — bigger spend reads as bigger node, more meaningful than visit count. **Mockup playground** [`apps/client/src/pages/BubbleMapsMockup.tsx`](apps/client/src/pages/BubbleMapsMockup.tsx) at `/mockup/bubblemaps` retains all 8 variants A-H as design reference. Constellation.tsx left in place but no longer imported by canonical surfaces (only mockup pages reference it) — flagged for future deletion. **Verified live** at localhost:5174/books: Money network renders with `8d / TO PAYDAY` core, GOALS sage hub, CAPITAL ONE + TD VISA gold institution hubs, 5 savings goals + 4 debt commitments, audit tape "tap to pin", DRAFT PAYCHEQUE button intact. /books?view=patterns: Habits network with LIFESTYLE clay hub, INDULGENCE sienna hub, AUTOPILOT teal hub (first canonical use of new --hub-autopilot color), 9 merchants in clean cluster. Tap-to-pin tested live: clicking Amazon.ca lit Amazon + LIFESTYLE + AUTOPILOT, dimmed all others, audit tape read "AMAZON.CA · 5 charges in 90 days · $120 total". Console clean. **Tests:** 428 unchanged (NetworkMap is pure render covered by typecheck; derive helpers in networkData.ts have no unit tests yet — flagged for next session). Typecheck clean across all 3 packages. **Build:** 613.60 kB JS / 47.74 kB CSS (+86 kB JS / +1.42 kB CSS vs session 87's 526.85/46.32 — growth is the NetworkMap component + lib helpers + the BubbleMapsMockup page with all 8 variants for design reference). **Worker:** unchanged, no deploy. **Files added:** `apps/client/src/components/NetworkMap.tsx`, `apps/client/src/lib/networkData.ts`. **Files changed:** `apps/client/src/index.css` (3 hub color vars), `apps/client/src/pages/Budget.tsx` (Constellation → NetworkMap, helper deletions), `apps/client/src/pages/BubbleMapsMockup.tsx` (variants F + G + H added). **Decisions locked:** (1) Network graph is the canonical visualization for Money + Habits on Books. Constellation.tsx is dead-on-canonical, kept only as mockup reference. (2) PAY SLIP center carries live signal (days to payday), not duplicated amount. (3) Hub palette is hierarchical and earns its place: GOLD = institutional debt, ROSE = personal debt, SAGE = goals, TEAL = autopilot, NAVY = time (reserved for future), SLATE/CLAY/SIENNA = category hubs in Habits. Each color represents a *kind of relationship*, not a category of money. (4) Tap-to-pin replaces hover. iPhone-first interaction model. Tap empty desk releases. (5) No drift animation — user explicitly killed it. The graph is static until tapped. (6) Audit tape lives BELOW the network, paper-cream with perforated edges, mono uppercase observation. Rest copy: `· tap to pin ·`. (7) Two-pass label render with paint-order stroke replaces black backing rectangles — labels stay legible without painted-on chrome. (8) Force-relaxation accounts for label clearance, not just bubble radius — solves the long-name overlap problem the original constellation had. (9) Hub stripe lives on the TOP edge of the index card (matches PAY SLIP top-stripe vocabulary), not the bottom. **Pushed back to next session:** unit tests for `buildMoneyNetwork` + `buildHabitsNetwork`; the duplicate "Vacation fund" issue (snapshot has both savings_goal + savings_recurring with same label, both render as separate nodes — decide whether to dedupe or label them differently in the source); `--hub-time` (navy) defined but unused — would land when time-of-day patterns become real data; cleanup of Constellation.tsx + the now-unused TodayConstellationMockup / PaychequeTankMockup files; press-to-pulse animation (concentric ring expanding from pinned node along edges) — design idea floated, not built.


- **Subscriptions rebuilt as the standing-orders verdict ledger, session 87** (2026-04-29): Triggered by user reaction to session 85's bubble-cluster Subscriptions surface ("this just feels flat right? see how fucking bland everything is?"). Iterative design pass inside `/mockup/books` with four variants explored (A standing-orders tape with punch cards, B calendar timeline, C annual stack, D receipt-context tape, E dark-ledger version that shipped). **The reframe locked.** User pushed back on punch-card framing: "i can be cahrged over and over for osmething im not using?" The bank cannot honestly tell you whether a subscription is being used; charges ≠ value. Model: the system shows cost, the user supplies the verdict. Three stamps: KEEP (sage), KILL (sienna), NOT_A_SUB (hidden behind ⋯ overflow — edge case = edge ergonomic). Killed rows fade and surface a per-merchant cancel-instruction torn-tape sticker; stay visible until the next charge fails to land. NOT_A_SUB drops the merchant off standing-orders forever while keeping its category everywhere else. **Schema.** Migration [`apps/worker/drizzle/0021_subscription_verdicts.sql`](apps/worker/drizzle/0021_subscription_verdicts.sql) applied to remote D1 (34 tables now). New table `subscription_verdicts` (merchant_id PK FK to merchants, verdict CHECK in keep|kill|not_a_sub, set_at). **Worker.** [`apps/worker/src/routes/budget.ts`](apps/worker/src/routes/budget.ts) GET /api/budget/subscriptions joins to subscription_verdicts, filters out `not_a_sub` server-side, surfaces `verdict` + `verdict_set_at` per row. New POST /api/budget/subscriptions/verdict (body `{merchant_id, verdict}`; verdict null = clear). Pure helpers `isVerdict`, `parseVerdictBody` exported. Idempotent on no-change. Merchant-existence guard returns 404 instead of FK error. Edit-log allowlist [`editLog.ts`](apps/worker/src/routes/editLog.ts) gained `subscription_verdict` with reason codes `subscription_verdict_set` / `subscription_verdict_clear`. **Client.** New [`apps/client/src/components/SubscriptionVerdictLedger.tsx`](apps/client/src/components/SubscriptionVerdictLedger.tsx) (~670 lines). Hero block: $X/yr in 48px Fraunces with monthly anchor right, then horizontal stack viz colored by category (each segment flexed by monthly cents). Hero row treatment for biggest bleeder: 24px Fraunces name, 36px price, paper-shade tinted background with dashed-edge perforations, italic flavor copy. Compressed rows: 17px Fraunces, 22px price, 3px category rule on left edge, share-of-bleed micro-bar (filled to `monthly/totalMonthly%`). KEEP/KILL as real rubber stamps using the existing triple-inset shadow vocabulary, `-1.2deg`/`+1.4deg` rotation when active. ⋯ overflow opens dashed-bordered menu with NOT A SUB stamp + explanation. Killed rows surface torn-tape sticker (paper-shade cream, jagged clip-path polygon top + bottom, slight rotation, drop shadow) carrying cancel instruction. Editorial lookup tables hardcoded for top streamers/services/passes: CANCEL_INSTRUCTIONS (Netflix→"netflix.com/youraccount → Cancel Membership", Apple iCloud→"iPhone Settings → Apple ID → Subscriptions → iCloud+", 20 entries), FLAVOR (Netflix→"queue you stopped finishing", Apple iCloud→"photos you don't restore", 17 entries). Earned badges (only when warranted): BIGGEST (sienna, n≥2), OLDEST · N MO (gold, n≥3 + months_running ≥ 12), FRESH (sage, n≥3 + months_running ≤ 3). **Wiring.** [`Budget.tsx`](apps/client/src/pages/Budget.tsx) PATTERNS view section 02: kicker `Subscriptions` → `Standing orders`, meta line "N hands in your wallet". Old `<HeroNumber>` + `<SubscriptionRibbon>` removed (the new component carries its own hero). Subscriptions count excludes killed rows in the meta. Subscription type extended with optional `verdict`. [`/paycheque/subscriptions`](apps/client/src/pages/Subscriptions.tsx) drill-in page rewritten to render the same component, kicker "Standing orders" + Back stamp routes to `/books?view=patterns`. SubscriptionRibbon.tsx left in place but no longer imported anywhere — flagged for cleanup. **Tests.** 13 new in [`subscriptionVerdicts.test.ts`](apps/worker/src/__tests__/subscriptionVerdicts.test.ts) — isVerdict 2 cases, parseVerdictBody 11 covering trim, missing/empty/non-string merchant_id, null = clear, missing verdict, invalid verdict string, non-object bodies. Worker total 415 → 428. **Build.** 570.59 kB JS (+43 kB vs session 86's 526.85 — VerdictLedger + lookup tables + stamp factory) / 46.24 kB CSS (-0.08 kB). Typecheck clean. **Worker deployed:** `426d723b-887a-49a5-aa95-c91861956af4`. **Verified live** at localhost:5174/books?view=patterns: standing-orders hero renders ($X/yr + horizontal stack + monthly anchor), Apple iCloud renders with italic "photos you don't restore" + sienna category rule + share-of-bleed bar + KEEP/KILL stamps; Netflix shows "queue you stopped finishing"; Spotify "one playlist on repeat"; Banh Mi Boys + Tim Hortons + Tokyo Smoke all rendering with proper flavor copy. Console clean. **Files added**: `apps/worker/drizzle/0021_subscription_verdicts.sql`, `apps/worker/src/__tests__/subscriptionVerdicts.test.ts`, `apps/client/src/components/SubscriptionVerdictLedger.tsx`. **Files changed**: `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/budget.ts`, `apps/worker/src/routes/editLog.ts`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/pages/Subscriptions.tsx`, `apps/client/src/pages/BooksMockup.tsx` (mockup playground). **Decisions locked**: (1) Bank transaction data CANNOT honestly compute subscription value/usage. The system shows cost; the user supplies the verdict. (2) Three verdicts only (KEEP / KILL / NOT_A_SUB). NOT_A_SUB lives behind ⋯ overflow — primary surface is binary KEEP/KILL. (3) Killed rows stay visible until the next charge fails to land — that's the confirmation loop, since the app can't auto-cancel. The cancel-here sticker shrinks the "google how to cancel" friction to one mono line. (4) Hero biggest-row treatment + share-of-bleed micro-bar + italic flavor copy + earned badges are the brand-specific polish — bookkeeping iconography (rubber stamps, perforations, paper-shade two-tone, torn-tape stickers) doing the work the data alone couldn't. (5) "Standing orders" is the canonical term on Books — bookkeeping's own word for "recurring debit you authorized once." (6) Verdict NULL = undecided. Verdict 'kill' surfaces cancel sticker. Verdict 'not_a_sub' filters out at the worker level — never reaches client.


- **Books tab rename + sticky legend + sub-nav + Constellation rebuild — phase 1 of the Books redesign program, session 86** (2026-04-29): Pure client. No schema, no migrations, no new worker code (uncommitted session-85 velocity columns on `/api/merchants` ship to CI on push). User reaction to session 85: "this is close but honestly looks terrible. we arent really doing anything good with this full stack this feels really under powered and honetly just a UI nightmare its disconnected entierly." Long brainstorm. The unlock: tab name `Paycheque` was creating the conflict — forward-looking pay-period content and backward-looking habits/subscriptions can't share one surface. User reframed Draft as "shows you what next paycheque should look like based on how you've been," meaning Draft is the synthesis bridge from Patterns to next period. **Decisions locked across the redesign program:** (1) Tab renamed `Paycheque` → `Books`. (2) Inside Books, sub-nav `THIS PAYCHEQUE | PATTERNS` (URL state via `?view=patterns`). (3) Sticky single legend at top of Books page, above sub-nav: `size = amount · color = category · solid = paid · ring = autopilot`. Read once, applies to every constellation across the page. (4) Hero numbers move OUT of the constellation cards and into section kickers ABOVE the picture. The picture is the story; the number is the headline. (5) Visual grammar locked across all three constellations — bubble size = amount; color = category (`--cat-*` vars); solid = done/observed; dashed ring = pending/autopilot; lines = relationship to a center (Money map only); biggest closest to center. (6) Animation deferred to a future session. (7) Habits/Subscriptions visualizations themselves NOT redesigned — same bubble grammar applied consistently is the redesign. **New component** [`apps/client/src/components/BooksLegend.tsx`](apps/client/src/components/BooksLegend.tsx) — sticky strip with 4 legend items in mono uppercase: tiered-size dot triplet, 5-dot category swatch (essentials slate / debt gold / savings sage / lifestyle clay / indulgence sienna), one solid dot, one dashed-ring dot. Backdrop-filter blur. **Constellation.tsx full rewrite** [`apps/client/src/components/Constellation.tsx`](apps/client/src/components/Constellation.tsx). Removed: internal HeroCard render (caller now renders the hero kicker above the SVG section), TopCallout (the floating dashed-box overlay user disliked), orbital dashed rings (dead chrome), `TODAY ↻ +14d` tick chrome on clock mode, `hero` / `topCallout` / `footerLeft` / `footerRight` / `center` props. Added: force-directed `relax(placed, padding=10, iterations=60)` that pushes overlapping bubbles apart along their connecting vector. Added: `decideLabelPositions()` chooses above-vs-below per bubble based on canvas clearance and collision check with other bubbles. Added: text-anchor switching — `start` when bubble.x < 80, `end` when bubble.x > W-80, else `middle` — so labels at edges anchor inward and never spill past the SVG edge. Clock mode now uses `SWEEP = π * 1.65` (not full 2π) so days=0 (today) and days=horizon (payday) don't collide at 12 o'clock. Wider angular fan within a day-bucket: `min(π/4, π/18 * list.length)`. Radial stagger across 3 ring-positions so equal-day bubbles don't pile. `PADDING_X = 36` (was 18). Truncation at 16 chars (was 14) since edge-anchored labels have more room. **SubscriptionRibbon.tsx** [`apps/client/src/components/SubscriptionRibbon.tsx`](apps/client/src/components/SubscriptionRibbon.tsx) — removed internal HeroCard render. Added exported helper `subscriptionTotals(subs)` that returns `{monthly_cents, annual_cents}` so callers compute totals for their own section kicker. **Budget.tsx full restructure** [`apps/client/src/pages/Budget.tsx`](apps/client/src/pages/Budget.tsx). New `Subview` type. `useSearchParams` for sub-tab state. Added `BooksLegend` import + render right under header. New `SubNav` component (THIS PAYCHEQUE / PATTERNS toggle with purple bottom-border on active). Page renders `<PaychequeView>` or `<PatternsView>` based on `view`. New local `<HeroNumber>` component — mono kicker + Fraunces 44px primary + mono caption, tones ink/gold/purple/sienna/sage. PaychequeView contents: 01 Money map (constellation + hero "FREE TO SPEND $X" + Draft tear-tab below), 02 Books (P&L / Net worth / Forecast links), 03 Open tabs (counterparties owed/owing). PatternsView contents: 01 Habits (constellation + hero "Spent · last 90d $X / $Y in last 30d"), 02 Subscriptions (ribbon + hero "Subscriptions / month $X / $Y/yr · N running"), 03 Spending history table, 04 Period surplus rows. Patterns queries (`merchants`, `subscriptions`, `spendHistory`, `periodHistory`) gated by `enabled: view === 'patterns'`; PaychequeView-only queries (`counterparties`) gated by `enabled: view === 'paycheque'` — saves network on the inactive view. **App.tsx** [`apps/client/src/App.tsx`](apps/client/src/App.tsx) — tab label `Paycheque` → `Books`, route `/books` is canonical with `<Budget />`, `/paycheque` redirects to `/books`, `/budget` redirects to `/books`. New `BooksIcon` (open ledger book SVG: spine + two facing pages + page hairlines) replaces the envelope icon. Sub-routes `/paycheque/plan`, `/paycheque/pl`, `/paycheque/net-worth`, `/paycheque/forecast`, `/paycheque/subscriptions`, `/paycheque/tabs/:id` left in place — internal `<Link to="/paycheque/...">` references across the codebase still work via the redirect. **Worker leftover** [`apps/worker/src/routes/merchants.ts`](apps/worker/src/routes/merchants.ts) — session 85's velocity columns (`spend_30d_cents`, `spend_prior_30d_cents`, `txn_count_90d`) were live in production worker `05893c27` but the source diff was uncommitted. Bundled into this session's commit so repo matches deployed state; CI redeploys on push. **Verified live** at localhost:5174/books: BOOKS kicker on header, sticky legend renders, sub-nav switches THIS PAYCHEQUE / PATTERNS via `?view=patterns`. Hero numbers above each constellation. Money map labels at edges anchor `start`/`end` and render fully visible (no more `acation fund` / `ash buffer (` clipping). Tim Hortons clearly the focal bubble in Habits cluster. Console clean. **Tests:** 415 unchanged (no new pure helpers — Constellation placement is render-side only, exercised through typecheck). **Build:** 526.85 kB JS (+11.75 kB vs session 85's 515.10) / 46.32 kB CSS (+0.03 kB) — JS growth is BooksLegend + SubNav + HeroNumber + sub-tab plumbing. **Worker:** no manual deploy this session; CI redeploys session-85 leftover columns on push. **Files added:** `apps/client/src/components/BooksLegend.tsx`. **Files changed:** `apps/client/src/components/Constellation.tsx`, `apps/client/src/components/SubscriptionRibbon.tsx`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/App.tsx`, `apps/worker/src/routes/merchants.ts`. **Decisions locked:** (1) `Books` is the tab name forever — `Paycheque` is dead. (2) Sticky single legend at top of Books is the visual-grammar teaching tool — read once, applies everywhere on the page. (3) Hero numbers always live ABOVE the picture as section kickers, never inside the constellation. (4) The 4-item legend (size / color / solid / ring) is the locked vocabulary. `solid = paid` and `ring = autopilot` apply uniformly across every viz. (5) Sub-nav `THIS PAYCHEQUE | PATTERNS` inside Books is the long-term IA — single page with narrative arc was rejected because Draft (the synthesis) needs to live next to the Money map, which lives under THIS PAYCHEQUE. (6) Constellation.tsx no longer takes hero/topCallout/footer/center props — those are caller responsibilities now. The component renders ONLY the picture. **Pushed back to next session (87):** Draft becomes a projection (read), separate from commit (write). New scratch table for stamped draft projections. Split `draftForPeriod` into pure compute + commit. Velocity helper (current-period-to-date if days_elapsed ≥ 5 else rolling 30d). Project next period (virtual — no `pay_periods` row). Money map gets `CURRENT | NEXT (DRAFT)` toggle. Manual stamp + "drafted N days ago · projection has shifted" caption. **Pushed to session 88+:** Animation layer — Money map flow lines, Habits velocity arrows, Subscriptions yearly pile-up reveals. **NOT redesigned this session (deliberate):** Habits cluster algorithm + Subscriptions ribbon visualization themselves — user explicitly said "i dont want to redesign habits or subscriptions right? its just too much like wayu too much the user should be able to understand what they're looking at at a glance no? they dont need 16 visuals they need a legend for right?" — the legend IS the redesign for those two.

- **Paycheque page rebuilt around three constellation maps — Money / Habits / Subscriptions, session 85** (2026-04-29): Pure brand rebuild over existing data — no schema, no migration, only one tiny worker route extension. The Paycheque page was a long vertical scroll of 8 ledger sections (tank visual + Committed list + Variance + Recommended + Trends with mini-tanks + Books links + Open tabs + Spending history + Period surplus). For a beginner that's too many words and too many gauges; for the brand it was reading SaaS. Rebuilt around three picture-summaries that drill into existing detail screens. **New shared component** [`apps/client/src/components/Constellation.tsx`](apps/client/src/components/Constellation.tsx) — SVG-based bubble map with two layout modes (`centered`: mascot D core + inner/outer orbits + radial dotted lines from center, used for the Money map; `cluster`: largest at top center + 6 around inner ring at radius 78 + remaining on outer ring at 130, used for Habits + Subscriptions) plus an `outlined` boolean prop that switches all bubbles from solid fill to dashed-ring (purely the metaphor — solid is "you noticed it happen," dashed is "this runs on autopilot"). Bubbles auto-size by `sqrt(amount/max)` between minR=12 and maxR=22 in cluster, 13–22 in centered. Color is per-category via the existing `--cat-*` CSS vars. Each bubble accepts `to` for navigate or `onClick`. Renders empty-state copy when `bubbles.length === 0`. Optional footer row with mono uppercase left/right labels. **Worker change.** [`apps/worker/src/routes/merchants.ts`](apps/worker/src/routes/merchants.ts) GET / now returns `category_group` (added `c.\`group\` AS category_group` to the SELECT) so the Habits map can color-code merchants. Worker deployed `05893c27-4c25-49d4-8425-446134e7f2e3`. **Money map** (centered mode) replaces the entire tank visual + 02 Committed list + 02b Variance + 03 Recommended sections. Center is the paycheque amount in Fraunces 20px with PAYCHEQUE mono kicker below — the paycheque IS the heart. Satellites: every line from `/api/paycheque` snapshot.committed.lines (bills→essentials slate, debt mins→debt gold, savings_goal/savings_recurring→sage) plus extras-toward-debt aggregated per debt from `/api/plan` next_paycheque_allocations where `role !== 'min'` (debt gold with `→ Name` label, separate bubble from the min). Tap routes: bill → /paycheque/forecast, debt-min/extra → /paycheque/plan, savings → /goals. Footer "COMMITTED $X" / "FREE $Y". The DRAFT PAYCHEQUE rubber stamp button stays directly below as the primary action — the constellation is the picture, Draft is the action that follows. **Habits map** (cluster mode, solid bubbles) replaces 05 Trends entirely (mini-tanks + indulgence ratio velocity line both deleted). Pulls top 8 lifestyle+indulgence merchants by spend_90d_cents from `/api/merchants?limit=200`, color by category_group via `categoryFromGroup()` helper. Tap → /settings/merchants for now (no per-merchant detail page exists yet). Footer "N merchants · $X spent". **Subscriptions map** (cluster mode, OUTLINED dashed-ring variant) replaces the Subscriptions row link in 02 Books. Pulls `/api/budget/subscriptions`, ring color by category_group. Tap → /paycheque/subscriptions. Footer "N running · ~$X/mo" (monthly-normalized via `Math.round(amount_cents * 30 / cadence_days)`). The dashed-ring variant teaches the metaphor: solid bubbles in Habits = "this happened, you noticed"; dashed rings in Subscriptions = "this runs on autopilot, you forgot." Same color system across both — sienna for indulgence, clay for lifestyle. **Header** simplified — LedgerHeader subtitle now reads `${daysLeft}d left · ${free} free` instead of the old date+free combo. **Sections kept:** 02 Books (Subscriptions row link removed since it's now a map), 05 Open tabs, 06 Spending history, 07 Period surplus. **Sections deleted:** the entire `<section style={{height:'40vh', minHeight:280}}>` tank block, the MiniTank component + 4-column history grid, the indulgence ratio velocity line, the AllocationSheet usage (no more tank-band taps), the variance subsection. **Cluster placement algorithm** revised mid-session after first preview showed bubbles overlapping labels: original golden-angle spiral packed too tight, replaced with explicit "largest at top center / 6 around inner ring r=78 / rest on outer ring r=130" tiered layout. **Files added:** `apps/client/src/components/Constellation.tsx` (~250 lines). **Files changed:** `apps/worker/src/routes/merchants.ts`, `apps/client/src/pages/Budget.tsx` (full rewrite — 690 lines → 480 lines, net JS shrank 6kB). **Tests:** 415 pass unchanged (no new pure helpers; constellation is pure render covered by typecheck). Typecheck clean. **Build:** 515.10 kB JS / 46.29 kB CSS (down from 521.22 / 46.57 in session 84). **Verified live** at localhost:5174/paycheque: Money map shows $2,700 paycheque core with TD Visa/Capital One mins + extras as gold satellites + Cash buffer/Vacation/Emergency/Summer camping as sage satellites; Habits cluster shows 8 merchants (Tokyo Smoke/McDonald's/Uber Eats sienna, Banh Mi Boys/Amazon/Netflix clay) with $1,380 spent footer; Subscriptions cluster shows 7 dashed rings (Amazon.ca $30 / Apple iCloud $4 / Netflix $20 / Uber Eats $32 / Spotify $13 / Starbucks $7 / Banh Mi Boys $17) with ~$173/mo footer. No console errors. **Decisions locked:** (1) Tank as the visual hero is over forever — `Constellation mode="centered"` is the new Paycheque hero. The CSS `.tank*` classes still exist for legacy/mockup pages but no canonical surface uses them. (2) Three maps total on Paycheque, no more — user explicitly rejected a Net Worth map ("i dont need networth") and Tabs map ("tabs dont need a map theyre very easy to understand"). (3) Subscriptions earned a map even though some users may have only 4–6 — the visual difference between "you're light" (small cluster) and "you're swamped" (full ring of 12) IS the teaching, and works either way. (4) Subscriptions are visually OUTLINED (dashed rings), not filled — teaches "autopilot/ghostly/forgot" without saying the words. Other maps stay solid filled. (5) Money map's Draft button lives immediately below the constellation (within the same `<section className="ledger-section">`) — the map is the picture, Draft is the action that follows. The user explicitly confirmed Paycheque IS where Draft happens. (6) AllocationSheet wiring removed; if drill-into-allocations is needed again it'll come back as bubble-tap → sheet, but for now bubble-tap navigates to existing dedicated screens (/plan, /goals, /forecast). (7) Plan extras (priority/avalanche allocations from `/api/plan`) become individual `→ DebtName` bubbles in the Money map sized by their amount, separate from the debt-min bubbles, both debt-gold — teaches the difference visually (extra is a *recommended push*, min is a *committed floor*). **User framing this session:** "Sure its great right now if you understand fininance but if you dont this is meant to help right? lets see what we can come up qith here and then how visually we can make this a good addition right? lets push this fullstack!! we can right???" — three picture-summaries shipped. **Next session priority:** iteration on the maps based on user reaction (constellation polish, label truncation when too long, possibly a per-merchant detail page so Habits bubbles tap somewhere meaningful, possibly category drill-in instead of merchant drill-in for Habits). User said "ill change as things crop up."

- **Restore reverted UI work + chit/tab modal alignment + chit form clarity, session 84** (2026-04-29): Pure client. No worker, no schema, no tests. Triggered by user reviewing localhost: "this isnt what i wanted... I added borders to the today screen i changed the paycheque screen a bit i think i made it less dark there is a HUGE problem here HUGE." Session 83's autonomous revert had rolled back the user's own UI work, not stray experiments. Audited git history: the three commits `86a37a1` (chit-pad + draft stamp), `f935fbf` (lifted shell + ledger-page frame + inset ink-rule + tab grid 4→3), and Today.tsx parts of `924b462` (counterparty modal overlay) were the user's intent. **Restore mechanic:** `git revert --no-commit f286753` auto-merged cleanly with session 82's "Up next" tier-priority queue still on top — no conflicts because session 82 only touched the queue rendering area while the reverted commits touched header/wrapper/CSS/modal. The `CounterpartyReceipt({id, onClose})` named export had survived in [`Counterparty.tsx`](apps/client/src/pages/Counterparty.tsx) (only the Today.tsx invocation was reverted), so wiring the modal back was a one-state change. **Bonus fix from `f935fbf`:** tab-bar grid `repeat(4, 1fr)` → `repeat(3, 1fr)` — leftover from when there were 4 tabs before session 41 dropped Debts; three tabs were left-crowded against an empty fourth column. **Bug 1 — chit form stacked behind counterparty modal.** Tapping a counterparty name fired BOTH the modal-overlay AND the long-press chit form. Root cause: inner `<button onClick>` opened the modal, but the modal's `position:fixed; inset:0; z-index:150` scrim immediately covered the row and intercepted the `pointerup` event so `cancelPress` never fired and the 450ms long-press timer kept running, opening the chit form on top. Fix in counterparty name button: `onPointerDown={(e) => e.stopPropagation()}` keeps the inner button out of the row's press tracking, and `onClick` explicitly calls `cancelPress() + setChitOpen(null)` before `setLiftedCpId(cp.id)` so any pending state is cleared atomically. **Bug 2 — chit form opened inline, modal opened as overlay (inconsistent UX).** Counterparty receipt lifted as modal scrim but the chit form rendered inline on the page (per-counterparty under each row + standalone below the OPEN TABS list). User: "shouldnt + new tab act the same way?" Refactored: chit form now renders ONCE at the top of the tree alongside the counterparty modal, gated on `chitOpen`, wrapped in the same z-150 scrim pattern (click-outside dismisses via `discardChit`). `prefilledCp` resolved from `chitOpen.prefilledFor` against `allCps`. Removed the two inline render sites. Long-press still pre-fills counterparty, chit-pad still opens blank — both now lift through the same overlay. **Bug 3 — chit form direction labels ambiguous + invisible unselected state + amateur submit copy.** User: "ist really onlky the buttons and how one is literally blank till you select it... yes the tear it is awful its soo ameature we have way more powerful shit in this app why tf are we using that lame ass shit." Three fixes: (a) labels `OWES YOU` / `YOU OWE` → `THEY OWE ME` / `I OWE THEM` — first-person, unambiguous to a first-time user, matches `they_owe`/`i_owe` schema. Header kicker also updated. (b) Unselected toggle was visually blank (white text on cream paper, transparent stamp frame using `--stamp-frame` rgba). Added `.chit .stamp` + `.chit .btn-outline` overrides in [`apps/client/src/index.css`](apps/client/src/index.css) mirroring the existing `.receipt .stamp` pattern (ink-color frame, paper gap, ink text); also `.chit .stamp.stamp-filled` for selected purple state and `.chit .stamp.stamp-purple` for the submit. (c) Submit was the `.tear-tab .tear-tab-motion` "TEAR IT" — wrong vocabulary (the chit-pad already did the tear ceremony when opening, and using `.tear-tab` for a record-action read amateur on a surface that has rubber stamps and postage available). Replaced with `.stamp .stamp-purple` "OPEN TAB" rubber stamp. **Verified live** at localhost:5174: Today renders with inset ink-rule frame + lifted shell + 3-column nav; tap counterparty → only the modal opens (no chit form behind); tap "+ NEW TAB" → chit form lifts as modal in same z-150 overlay; chit form shows "THEY OWE ME" purple-filled / "I OWE THEM" ink-frame, "OPEN TAB" purple stamp at bottom. **Tests:** 415 unchanged. Typecheck + build clean. **Build:** 521.22 kB JS / 46.57 kB CSS (+0.16 kB JS / +0.65 kB CSS for chit overlay scrim + `.chit .stamp` overrides). **Worker:** unchanged, no deploy. **Commits:** `dc2f276` (restore: bring back lifted shell, ledger-page frame, chit-pad, draft stamp, modal-overlay tabs), `40f23e7` (fix: chit form lifts as modal overlay, mirror counterparty receipt behavior), `009a894` (ui: chit form direction labels + submit + legible stamps in chits). **Files changed:** `apps/client/src/index.css`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/pages/Today.tsx`. **Decisions locked:** (1) Both counterparty drill-in AND new-chit composition lift as modal overlays through the same z-150 scrim — never inline. Consistency over context-preservation. (2) Chit direction labels are first-person ("THEY OWE ME" / "I OWE THEM") — never second-person ambiguity. (3) `.tear-tab` is the OPENING ceremony only (chit-pad to compose, the dropped Paycheque draft tear-tab is now a rubber stamp). Once a form is open, SUBMIT actions use `.stamp` vocabulary, never another `.tear-tab`. (4) `.stamp` inside `.chit` requires explicit overrides like `.receipt .stamp` does — they're cousin contexts, not the same. (5) Session 83's autonomous revert was wrong; future "this isn't what I wanted" reactions on Vercel must be investigated against git history before reverting, especially when the reverted commits are unattributed-but-recent UI work that may BE the user's intent.

- **Revert post-session-81 UI experiments to clean baseline, session 83** (2026-04-29): Pure client revert. No worker, no schema, no tests. Triggered by user looking at the Vercel deployment: "this isn't what i wanted." A burst of exploratory UI commits had landed in `main` between sessions 81 and 82 (plus one post-82 modal overlay) without the session ritual; Vercel auto-deploys main, so the user-facing surface had drifted. **Reverted (effectively):** (1) `86a37a1` tear-tab → rubber draft stamp on Paycheque + tear-tab → chit-pad on Today; (2) `f935fbf` Today wrapped in `.ledger-page`, dark shell colors lifted `#0d0a10/#15111b` → `#1c1622/#241d2c`, receipt padding 22/20px → 32/40px with heavy inset `box-shadow` ink-rule frame; (3) Today.tsx parts of `924b462` counterparty modal overlay (`liftedCpId` state + full-viewport CounterpartyReceipt overlay with z-150 scrim) — counterparty rows are `<Link>` again, navigate to `/paycheque/tabs/:id` instead of opening inline. **Mechanic:** `git checkout 21d4399 -- apps/client/src/index.css apps/client/src/pages/Budget.tsx apps/client/src/pages/Today.tsx` (session 81 baseline), then surgically reapplied session 82's "Up next" rename + tier-priority queue + 3-of-5 truncation onto the rolled-back Today.tsx. **Kept:** `924b462` subscription detector CV ≤ 15% fix (worker code, not part of the regression). `a85bb31` mockup pages (`PaychequeTankMockup.tsx` satellite-orbit view for Paycheque draft, `TodayConstellationMockup.tsx` merchant constellation for Today hero, `DraftChitMockup.tsx` six-variant draft + chit interaction explorations) and their `/mockup/*` routes in `App.tsx` — these stay as design references for future direction, not regressions. **Verified live** at localhost:5174: Today renders "UP NEXT" with 3 visible + "+ 2 more" expand; counterparty rows navigate to drill-in screen; "TEAR NEW CHIT" tear-tab back at bottom of Open Tabs with "LONG-PRESS A NAME ABOVE TO PREFILL" hint. Paycheque shows "↓Draft this paycheque" tear-tab. No console errors. **Tests:** 415 pass unchanged. Typecheck clean. **Build:** 521.06 kB JS / 45.92 kB CSS. **Files changed:** `apps/client/src/index.css`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/pages/Today.tsx`. **Commit:** `f286753`. **Decisions locked:** (1) Vercel auto-deploys `main` directly — exploratory UI must live on branches or behind `/mockup/*` routes, never on canonical pages. (2) The mockup pages represent desired future direction (constellation hero on Today, satellite-orbit committed view on Paycheque draft); canonical pages stay at session 81/82 baseline until those are properly built against live data. (3) Counterparty drill-in stays a real screen navigation, not a modal.

- **Today "Up next" rename + 3-of-5 priority queue, session 82** (2026-04-28): Pure client. No worker, no schema, no tests. User wanted the Today queue reworded and limited so anything beyond a few rows lives in Paycheque. **Naming:** `Queue` → `Up next` (rejected `On the tape` and `Printing next` — receipt motif kept for footer + empty states; queue itself stays plain). **Priority order locked** in [`apps/client/src/pages/Today.tsx`](apps/client/src/pages/Today.tsx): `tier asc, days_until asc` where tier 0 = ritual (reconcile/review), tier 1 = bills, tier 2 = plan installments, tier 3 = payday. Bills outrank installments because bills are external (someone charges you), installments are self-set. Payday last because it's news, not action. **Mechanic:** default 3 visible. Render `+ N more` row (up to 5 max) when truncated; tap expands. When expanded and queue.length > 5, append `see all in paycheque ›` linking to `/paycheque`. When expanded, `show less ‹` row at bottom collapses back to 3. Hard cap of 5 on Today. **Empty-state copy:** `Nothing pending.` → `Nothing up next.` **Verified live** at localhost:5174/today: section reads "Up next", default 3 rows + `+ 2 more`, tap expands to 5 + `show less`, tap show less round-trips to 3. **Tests:** 418 unchanged. Typecheck + build clean. **Files changed:** `apps/client/src/pages/Today.tsx` only.

- **REBUILD_PLAN I + K — period history, subscriptions, tax year, merchant spend, period surplus, session 81** (2026-04-28): No schema, no migrations. Completes the full 11-session REBUILD_PLAN behavioral rebuild. **Session I — Period history + subscription audit.** Worker: `detectSubscriptions()` pure helper added to [`apps/worker/src/lib/recurring.ts`](apps/worker/src/lib/recurring.ts) — mirrors `detectRecurring()` but filters to `lifestyle` + `indulgence` groups (the discretionary categories), no forward horizon filter, adds `first_seen`/`last_seen`/`months_running`, sorted by `amount_cents` descending. New route `GET /api/budget/subscriptions` in [`apps/worker/src/routes/budget.ts`](apps/worker/src/routes/budget.ts) — queries 180d of merchant-tagged txns, runs `detectSubscriptions()`, returns `{subscriptions}`. New route `GET /api/budget/history?periods=N` — last N closed periods, per-category spend by group (essentials/lifestyle/indulgence/debt/savings), oldest-first, excludes current period. New route `GET /api/periods/history?limit=N` in [`apps/worker/src/routes/periods.ts`](apps/worker/src/routes/periods.ts) — last N closed periods with GL-derived income/expense/surplus per period, oldest-first. Client: new [`apps/client/src/pages/Subscriptions.tsx`](apps/client/src/pages/Subscriptions.tsx) at `/paycheque/subscriptions` — dark ledger, "01 Charges" section with cat-rule color per row (lifestyle/indulgence), cadence/months hint, "02 Summary" per-month + per-year totals, empty-state "Nothing found · import 6 months of activity to populate." Wired into Budget "04 Books" as fourth row. [`apps/client/src/pages/Budget.tsx`](apps/client/src/pages/Budget.tsx) gained new "07 Spending history" section: `SpendHistoryTable` component renders a monospace table (thead = period date ranges, tbody = per-group rows with `—` for zero). New "08 Period surplus" section: per-period GL-derived income/expense/surplus rows (sage for surplus ≥0, sienna for deficit). **Session K — Tax year + merchant spend + period surplus.** New worker route file [`apps/worker/src/routes/taxYear.ts`](apps/worker/src/routes/taxYear.ts) at `GET /api/settings/tax-year?year=YYYY`. Income YTD: `SUM(credit_cents - debit_cents)` on `Income:Salary` GL account for [YYYY-01-01, today]. Contributions: debit JEs on `Assets:Investments:TFSA/%`, `FHSA/%`, `RRSP/%`, `NonReg/%` paths grouped by wrapper. Hard-coded 2026 limits: TFSA $7,000, FHSA $8,000, RRSP null. Returns `{year, income_ytd_cents, contributions, annual_limits, note}`. [`apps/worker/src/routes/merchants.ts`](apps/worker/src/routes/merchants.ts) extended: `spend_90d_cents` added to list query (COALESCE SUM of negative txns in past 90 days), ORDER BY changed to `spend_90d_cents DESC, txn_count DESC, display_name ASC`. Client: new [`apps/client/src/pages/TaxYear.tsx`](apps/client/src/pages/TaxYear.tsx) at `/settings/tax` — dark ledger, year selector (current + 2 prior with `stamp-gold` for selected), "02 Income" (Salary YTD from GL), "03 Registered accounts" (TFSA/FHSA/RRSP each showing invested + room remaining, RRSP labeled "room requires prior-year T4"), "04 Non-registered" (only shown when `contributions.nonreg > 0`). [`apps/client/src/pages/Merchants.tsx`](apps/client/src/pages/Merchants.tsx) gained `spend_90d_cents` display in row hint. [`apps/client/src/pages/Settings.tsx`](apps/client/src/pages/Settings.tsx) gained "Tax year" nav item linking to `/settings/tax`. Routes wired in [`apps/client/src/App.tsx`](apps/client/src/App.tsx). **Tests:** 410 unchanged (pure SQL routes + UI extensions; no new pure helpers). Typecheck clean. **Build:** 485.35 kB JS (+8.90 kB) / 44.14 kB CSS (+0.25 kB). Commit `21d4399`. CI redeploy triggered. **Files added:** `apps/worker/src/routes/taxYear.ts`, `apps/client/src/pages/Subscriptions.tsx`, `apps/client/src/pages/TaxYear.tsx`. **Files changed:** `apps/worker/src/lib/recurring.ts`, `apps/worker/src/routes/budget.ts`, `apps/worker/src/routes/periods.ts`, `apps/worker/src/routes/merchants.ts`, `apps/worker/src/index.ts`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/pages/Merchants.tsx`, `apps/client/src/pages/Settings.tsx`, `apps/client/src/App.tsx`. **THE 11-SESSION REBUILD_PLAN IS COMPLETE.** All sessions A/B/C/D/E/F/G/H/I/J/K shipped.

- **REBUILD_PLAN G — forecast + payoff timeline + goals projection, session 80** (2026-04-28): No schema, no migrations. **Forecast.** New pure lib [`apps/worker/src/lib/forecast.ts`](apps/worker/src/lib/forecast.ts): `buildForecast({today, days, starting_balance_cents, events})` walks events in date order (payday before bill/savings/plan on shared dates so running balance reflects inflow first); `projectPaydays({next_due, amount_cents, cadence_days, today, days})` projects bi-weekly forward with safety cap of 64 iterations; `projectGoalCompletion({remaining_cents, per_paycheque_cents, next_payday, cadence_days})` returns absolute completion date or null. New worker route [`apps/worker/src/routes/forecast.ts`](apps/worker/src/routes/forecast.ts) at `GET /api/forecast?days=30` (clamped 7-90, default 30). Reads starting balance from `gl_accounts WHERE parent_id='gla_assets_cash'` (debit−credit through today). Composes events from: detector-recurring filtered to `bill` (essentials/lifestyle) + `savings_recurring` (transfer); committed-not-stamped plan installments aggregated per `(period_end, debt name)` via `stripRoleSuffix`; bi-weekly paydays projected from latest pay_period. Returns `{today, days, starting_balance_cents, min_balance_cents, events}`. New client page [`apps/client/src/pages/Forecast.tsx`](apps/client/src/pages/Forecast.tsx) at `/paycheque/forecast` (dark ledger): LedgerHeader with starting balance + "opening cash · next 30 days" subtitle, sienna warning row when min_balance < 0, single 01 Events section with cat-dot per row + label + date+type tag + signed amount + running balance below in muted (sienna when negative). Empty-state "Nothing scheduled · import recent activity to populate." Wired into Budget "04 Books" as third row "Next 30 days · Cash forecast · running balance"; legacy `/budget/forecast` redirects. **Goals projection.** [`apps/worker/src/routes/goals.ts`](apps/worker/src/routes/goals.ts) GET / now derives `next_payday` from latest pay_period and per-goal `per_paycheque_cents` (mirrors `loadActiveGoals` heuristic in periodIntelligence: `ceil(remaining / max(1, round(daysRem/14)))` with daysRem floored at 14), passes both into `projectGoalCompletion`. Each goal row returns `per_paycheque_cents` + `projected_complete_date`. [`apps/client/src/pages/Goals.tsx`](apps/client/src/pages/Goals.tsx) shows projection caption per goal "projected complete YYYY-MM-DD · $X/paycheque" when non-null. **Payoff timeline** verified already shipped session 76 (PayoffTimeline component on Plan.tsx). No additional work. **Tests:** 12 new in [`forecast.test.ts`](apps/worker/src/__tests__/forecast.test.ts) — projectPaydays (3), buildForecast (4), projectGoalCompletion (5). Worker 410, total 432 (+12). Typecheck clean. **Build:** 476.45 kB JS (+3.49 kB) / 43.89 kB CSS (unchanged). **Files added:** `apps/worker/src/lib/forecast.ts`, `apps/worker/src/routes/forecast.ts`, `apps/worker/src/__tests__/forecast.test.ts`, `apps/client/src/pages/Forecast.tsx`. **Files changed:** `apps/worker/src/routes/goals.ts`, `apps/worker/src/index.ts`, `apps/client/src/App.tsx`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/pages/Goals.tsx`. **REBUILD_PLAN remaining:** I (period history + subscription audit), K (tax year + merchant spend + period surplus).

- **REBUILD_PLAN F — P&L + net worth trend, session 79** (2026-04-28): No schema, no migrations. Two new GL routes + two new sub-pages, both linked from Paycheque section "04 Books." **Worker** [`apps/worker/src/routes/gl.ts`](apps/worker/src/routes/gl.ts): (1) `GET /api/gl/pl?period_id=` (or `?from=&to=`, defaults to current pay_period guarded by `start_date <= date('now')`) — sums income (credit−debit) and expense (debit−credit) GL accounts whose JEs fall in the window, returns `{period_id, start_date, end_date, income_cents, expense_cents, surplus_cents, income_by_path[], expense_by_path[]}` with per-path arrays sorted descending. 404 `period_not_found` / `no_period`. (2) `GET /api/gl/net-worth/history` — reads `period_close` oldest→newest, runs the same TYPE-grouped trial-balance aggregation per close.period_end (guarded by `e.posted_at <= ?`), returns `{points[]}`. **Client.** New [`apps/client/src/pages/PL.tsx`](apps/client/src/pages/PL.tsx) (cream receipt) at `/paycheque/pl` — INCOME STATEMENT wordmark, optional last-4 period chip selector defaulting to current, three perforated blocks (Income +cents mascot-purple, Expenses −cents sienna, Surplus|Deficit colored sage|sienna), per-path expense + income breakdown, `** end of statement **` footer. New [`apps/client/src/pages/NetWorthTrend.tsx`](apps/client/src/pages/NetWorthTrend.tsx) (dark ledger) at `/paycheque/net-worth` — LedgerHeader with live `GET /api/gl/net-worth` value as title, "01 Trend" section listing each close newest-first with delta vs prior close (sage on positive, sienna on negative). Empty-state copy "No closed periods yet · seal a week to start the trend." Routes wired in [`App.tsx`](apps/client/src/App.tsx) with legacy `/budget/pl` + `/budget/net-worth` redirects. **Budget** [`apps/client/src/pages/Budget.tsx`](apps/client/src/pages/Budget.tsx) gained section "04 Books" between Recommended and Trends with two `.ledger-row tap` Links to the new pages. **Verified live** at localhost:5174: section "04 Books" renders both rows with chevrons; /paycheque/pl renders INCOME STATEMENT receipt; /paycheque/net-worth renders live $8,262.49 + empty-state for the new history route (worker not yet redeployed). No new console errors. **Tests:** 420 unchanged (pure SQL aggregation routes; no new helpers). Typecheck clean. **Build:** 472.96 kB JS (+7.16 kB) / 43.89 kB CSS (+0.03 kB). **Files added:** `apps/client/src/pages/PL.tsx`, `apps/client/src/pages/NetWorthTrend.tsx`. **Files changed:** `apps/worker/src/routes/gl.ts`, `apps/client/src/App.tsx`, `apps/client/src/pages/Budget.tsx`. **REBUILD_PLAN remaining:** G (forecast + payoff timeline + goals projection), I (period history + subscription audit), K (tax year + merchant spend + period surplus).

- **REBUILD_PLAN sessions E + H + J — budget variance, transactions search, counterparty aging, DTI, CC mismatch, session 78** (2026-04-27): No schema, no migrations. Three of eleven REBUILD_PLAN sessions in one focused run. **E:** Worker rewrote `GET /api/budget/variance` against `period_allocations` (the actual source of truth since session 41) — was using the dead `budgets` table. New shape per category group: `{group, planned_cents, spent_cents, delta_cents}`. Five groups always returned. Client added `02b Budget vs. actual` ledger section between Committed and Recommended on Paycheque, defensive Map aggregation handles old/new worker shape. Zero-based prompt in section 01: when `unassigned > 0`, sienna text "$X unassigned · give every dollar a job." **H:** New worker route file [`apps/worker/src/routes/transactions.ts`](apps/worker/src/routes/transactions.ts) — `GET /api/transactions/search?q=&from=&to=&account_id=&category_group=&limit=`, searches `description_raw` + `merchants.display_name`, joined account/category/merchant names returned. New client component [`apps/client/src/components/SearchSheet.tsx`](apps/client/src/components/SearchSheet.tsx) — bottom-sheet cream-receipt overlay, query gated on at least one filter set, results as row-tap lines. Mounted in Settings, opened via "Search transactions" row at top of Data section. **J:** Counterparty aging — new helpers `daysSince` + `ageColor` add `· Nd open` tag to each open chit's label-tag, color buckets ink-muted < 14d / gold 14-30d / sienna > 30d. DTI — new `minPaymentCents` helper in Debts.tsx, monthly income proxy `paycheque × 26 / 12`, caption above debt list `Monthly minimums $X · Y% of income · stress threshold 36%`, Y goes sienna over 36. CC mismatch — new query against existing `GET /api/gl/statement-reconcile?window_days=60`, latest mismatch per debt deduped, surfaces `GL $X · stmt $Y · gap $Z` in sienna under severity row when non-zero. **Verified live** at localhost:5174: zero-based prompt visible, 02b section shows 5 unique groups with deltas, Marcus Chen chit shows `0D OPEN` aging tag, Debts shows DTI line + TD Visa GAP $19.99 line. **Tests:** 420 unchanged. Typecheck clean. **Build:** 465.80 kB JS (+6.68 kB) / 43.86 kB CSS (+0.07 kB). **Files added:** `apps/worker/src/routes/transactions.ts`, `apps/client/src/components/SearchSheet.tsx`. **Files changed:** `apps/worker/src/routes/budget.ts`, `apps/worker/src/index.ts`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/pages/Settings.tsx`, `apps/client/src/pages/Counterparty.tsx`, `apps/client/src/pages/Debts.tsx`. **REBUILD_PLAN remaining:** F (P&L + net worth trend), G (forecast + payoff timeline + goals projection), I (period history + subscription audit), K (tax year + merchant spend + period surplus).

- **Today win-footer + over-commitment warning, session 77** (2026-04-27): No schema, no migrations. First behavioral slice from `REBUILD_PLAN.md` (the 11-session rebuild, picking up WIP from previous session). **today.ts** [`apps/worker/src/routes/today.ts`](apps/worker/src/routes/today.ts): GET / now returns `last_paid_installment: {label, amount_cents, stamped_at} | null` — most recent stamped debt allocation since `mostRecentSunday(today)` (re-imported from reconciliation.ts). Label runs through `stripRoleSuffix`. **Today.tsx** [`apps/client/src/pages/Today.tsx`](apps/client/src/pages/Today.tsx): footer copy ladders three ways — `* <label> — done *` when an installment was paid this reconciliation week; else `* still printing *` at vocab level ≥2; else `* still counting *`. New "Committed $X exceeds $Y left" warning row above the queue (sienna `--cat-indulgence` dashed top + bottom) when `tank.committed_cents > tank.remaining_cents`. **Tests:** 420 unchanged. Typecheck clean. **Build:** 459.12 kB JS (+1.98 kB) / 43.79 kB CSS. **Files changed:** `apps/worker/src/routes/today.ts`, `apps/client/src/pages/Today.tsx`. **REBUILD_PLAN.md** committed — 435-line behavioral rebuild plan; payment plan is the spine; installment is the primary action object across Today/Budget/Reconciliation/Notifications.

- **Plan installment detail + Today history accordion + tank committed-cents, session 76** (2026-04-27): No schema, no migrations. Worker code changed, deployed. Pure capability extensions across three surfaces. **plan.ts** [`apps/worker/src/routes/plan.ts`](apps/worker/src/routes/plan.ts): `CommittedSummary` extended with `installments: InstallmentRow[]` (per-row: `allocation_id`, `debt_name` via `stripRoleSuffix`, `amount_cents`, `due_date`, `status: paid|pending`, `stamped_at`) and `period_end_date`. `loadCommittedForPeriod` now requires `periodEndDate` arg. `stripRoleSuffix` exported (was already in today.ts, now shared from plan.ts). **today.ts** [`apps/worker/src/routes/today.ts`](apps/worker/src/routes/today.ts): GET / now returns `active_plan?: {total_pending_cents, installment_count, period_end}` — non-null when committed-but-not-stamped installments exist (hero slot data). New `GET /today/history?kind=bill|payday|plan` drill-in route: `kind=bill` takes `merchant_id`, returns last 3 negative txns (posted_at, abs amount, description_raw); `kind=payday` returns last 3 pay_periods (start_date, paycheque_cents); `kind=plan` takes `label`, returns last 3 completed installments. **budget.ts** [`apps/worker/src/routes/budget.ts`](apps/worker/src/routes/budget.ts): tank now returns `committed_cents` (pending committed debt allocations for current period) and `truly_free_cents = max(0, remaining − committed)`. **reconciliation.ts** [`apps/worker/src/routes/reconciliation.ts`](apps/worker/src/routes/reconciliation.ts): new `GET /reconciliation/plan-summary` — lightweight snapshot for the Sunday receipt's plan section. Returns `{installments[], total_cents, paid_count, pending_count, is_active_plan}` from direct `period_allocations` read (no kernel). **Client.** `Budget.tsx` [`apps/client/src/pages/Budget.tsx`](apps/client/src/pages/Budget.tsx): tank overlay gains a conditional "Free $X · Committed $Y" line (ink-dark / debt-gold) when `committed_cents > 0`. `Plan.tsx` [`apps/client/src/pages/Plan.tsx`](apps/client/src/pages/Plan.tsx): (1) `stamp` mutation lets user tap a PAID stamp directly from the Plan installment list (invalidates plan/today/allocations). (2) `paidIds` Set tracks optimistic paid state between server refreshes. (3) New `PayoffTimeline` component renders a horizontal "Payoff order →" sequence with each debt name + payoff horizon sorted earliest-to-last, ending at "Debt free" in gold — surfaced in the plan receipt below the per-debt rows. `Today.tsx` [`apps/client/src/pages/Today.tsx`](apps/client/src/pages/Today.tsx): `QueueItem`, `Glyph` types moved to module level. New `HistoryRef` discriminated union (`{kind:'bill', merchant_id}` | `{kind:'payday'}` | `{kind:'plan', label}`). New `QueueRow` component renders each queue row as a tappable accordion — closed state identical to previous layout; open state fires `GET /today/history` and renders last 3 occurrences (date + amount) in a perforated sub-list. Bills + payday rows carry a `history` ref; plan rows carry plan history. **Tests:** 420 unchanged (pure SQL aggregation routes + UI; no new pure helpers to test). Typecheck clean. **Build:** 457.14 kB JS (+4.30 kB) / 43.76 kB CSS (+0.64 kB). Worker `f685df68-192d-4a36-acff-138c71ad18b3`. **Files changed:** `apps/worker/src/routes/plan.ts`, `apps/worker/src/routes/today.ts`, `apps/worker/src/routes/budget.ts`, `apps/worker/src/routes/reconciliation.ts`, `apps/client/src/pages/Plan.tsx`, `apps/client/src/pages/Today.tsx`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/index.css`. **Decisions:** (1) `stripRoleSuffix` is now exported from plan.ts; reconciliation.ts has a local copy (inline function) — acceptable for the lightweight snapshot route. (2) History accordion uses `enabled: open && expandable` so the query only fires when the user taps; network-free until then. (3) `active_plan` in Today is nil when no committed installments exist — UI has no hero slot change in that case. (4) `committed_cents` on tank only deducts pending (unstamped) commitments; stamped rows are already in `total_spent_cents` so they don't double-count.

- **Progressive vocabulary system — session 75** (2026-04-27): Worker code changed, deployed. No schema, no migrations. Core product philosophy: teach through structure, not lectures. Plain English first; bookkeeping vocabulary earned through use, introduced exactly once via a cream-paper receipt-strip toast. Never condescending. **New lib** [`apps/worker/src/lib/vocabularyMilestone.ts`](apps/worker/src/lib/vocabularyMilestone.ts): pure `shouldUnlock(current, milestone)` (returns true when current < milestone), pure `vocabMessage(level)` (4-entry map for levels 1-4), impure `maybeUnlockVocabulary(env, milestone)` (reads `vocabulary_level` settings key, unlocks if needed, returns `{level, message}` or null). **4 milestone triggers wired:** (1) [`import.ts`](apps/worker/src/routes/import.ts) milestone 1 when `inserted > 0` — "journal entries"; (2) [`reconciliation.ts`](apps/worker/src/routes/reconciliation.ts) POST /complete milestone 2 — "closing the books"; (3) [`allocations.ts`](apps/worker/src/routes/allocations.ts) POST /:id/stamp milestone 3 only when `category_group === 'debt'` — "liability"; (4) [`plan.ts`](apps/worker/src/routes/plan.ts) POST /accept milestone 4 — "debt schedule." All four responses conditionally include `vocabulary_unlock?: {level, message}`. Errors swallowed at all sites — vocabulary failures never block the main action. **Client.** New module store [`apps/client/src/lib/vocabularyToast.ts`](apps/client/src/lib/vocabularyToast.ts): `showVocabularyToast`, `clearVocabularyToast`, `getVocabularyToast`, `subscribeVocabularyToast` — no React dependency. New component [`apps/client/src/components/VocabularyToast.tsx`](apps/client/src/components/VocabularyToast.tsx): subscribes to store, renders cream paper strip fixed above tab bar, dismisses on tap. CSS `.vocab-toast` (bottom calc offset, dashed perf top, box-shadow, z-index 50). Mounted globally in [`App.tsx`](apps/client/src/App.tsx). 4 client mutation sites wired: [`ImportCsvButton.tsx`](apps/client/src/components/ImportCsvButton.tsx), [`Reconciliation.tsx`](apps/client/src/pages/Reconciliation.tsx), [`AllocationSheet.tsx`](apps/client/src/components/AllocationSheet.tsx), [`Plan.tsx`](apps/client/src/pages/Plan.tsx). **Today footer copy ladder.** [`Today.tsx`](apps/client/src/pages/Today.tsx) reads `vocabulary_level` from the shared `['settings']` query (already present). Level 0-1 → `* still counting *`. Level 2+ → `* still printing *`. The threshold is reconciliation — the week-close ritual is the symbolic crossing. **Pre-existing typecheck fix:** [`notifications.ts`](apps/worker/src/routes/notifications.ts) `exactOptionalPropertyTypes` rejected spread pattern for optional `userName`; fixed by imperative variable + conditional assignment. **Tests:** 8 new in [`vocabularyMilestone.test.ts`](apps/worker/src/__tests__/vocabularyMilestone.test.ts) — shouldUnlock (5: below/equal/above/skip/adjacent), vocabMessage (3: valid range loop, level 0, unknown). Worker 390→398. Total 412→420 (+8 worker). **Build:** 452.84 kB JS (+7.25 kB) / 43.12 kB CSS (+1.30 kB). **Also in this session:** `user_display_name` settings key. Collected on Onboarding step 0 (name input on `StepWelcome`, written to settings via `POST /api/settings/user_display_name` before advancing). Editable in Settings new "01 Profile" section; existing sections renumbered 02-10. Used in two places: (a) [`apps/worker/src/lib/notificationSchedule.ts`](apps/worker/src/lib/notificationSchedule.ts) `buildNotificationSchedule` accepts optional `userName` on `ScheduleInput` — bill/debt/tank-low notifications prefixed `${name}, `, paycheque fires as `"Paycheque is in, ${name}."` when set; [`apps/worker/src/routes/notifications.ts`](apps/worker/src/routes/notifications.ts) fetches `user_display_name` before building schedule. (b) [`apps/worker/src/routes/paymentLinks.ts`](apps/worker/src/routes/paymentLinks.ts) `renderPublicLinkHtml(row, status, userName?)` — `fromName` is now `userName || 'Declyne'` so payment requests show the user's real name to the recipient; page `<title>` fixed to use recipient name (`toName`) instead of sender. Worker `74f6ef08-6a46-4af1-b850-839265b08d64`. **Files added:** `apps/worker/src/lib/vocabularyMilestone.ts`, `apps/worker/src/__tests__/vocabularyMilestone.test.ts`, `apps/client/src/lib/vocabularyToast.ts`, `apps/client/src/components/VocabularyToast.tsx`. **Files changed:** `apps/worker/src/routes/import.ts`, `apps/worker/src/routes/reconciliation.ts`, `apps/worker/src/routes/allocations.ts`, `apps/worker/src/routes/plan.ts`, `apps/worker/src/routes/notifications.ts`, `apps/worker/src/lib/notificationSchedule.ts`, `apps/worker/src/routes/paymentLinks.ts`, `apps/client/src/App.tsx`, `apps/client/src/components/ImportCsvButton.tsx`, `apps/client/src/pages/Reconciliation.tsx`, `apps/client/src/components/AllocationSheet.tsx`, `apps/client/src/pages/Plan.tsx`, `apps/client/src/pages/Today.tsx`, `apps/client/src/pages/Settings.tsx`, `apps/client/src/pages/Onboarding.tsx`, `apps/client/src/index.css`. **Decisions locked:** (1) Milestones are one-way gates — level only increases. (2) Toast dismisses on any tap. (3) Footer reaches `* still printing *` at level 2 (first reconciliation), not at level 1 (first import). (4) Errors swallowed on all vocabulary DB calls. (5) `vocabulary_level` stored as plain settings key — no migration needed. (6) `user_display_name` optional everywhere — all callers fall back gracefully.

- **Nav rename Budget→Paycheque + B leftovers, session 74** (2026-04-27): No schema, no worker changes. Pure client. Sessions 72 + 73 pushed at session start; CI built the periodIntelligence substrate. Verified live: 02 COMMITTED now renders real lines (TD Visa min $89.40, Capital One min $50, Luther min $100, Vacation fund $41.67 + $73.53, Cash buffer $195.46) — session 73's empty-state degradation is no longer needed in production. **Tab rename:** [`apps/client/src/App.tsx`](apps/client/src/App.tsx) tab label "Budget"→"Paycheque" with new envelope-style `PaychequeIcon` (rect + diagonal flap). New canonical routes `/paycheque`, `/paycheque/plan`, `/paycheque/tabs/:id`. Legacy paths redirect: `/budget` → `/paycheque`, `/budget/plan` → `/paycheque/plan`, `/budget/tabs/:id` → `/paycheque/tabs/:id` via a small `RedirectTabs` helper that preserves `:id` through useParams. **Internal Link sweep:** [`Budget.tsx`](apps/client/src/pages/Budget.tsx) (4 hrefs), [`Today.tsx`](apps/client/src/pages/Today.tsx) (plan row + counterparty drill-in), [`Reconciliation.tsx`](apps/client/src/pages/Reconciliation.tsx) (missed-installments link), [`Plan.tsx`](apps/client/src/pages/Plan.tsx) (Back link) all repointed to `/paycheque*`. Worker `/api/budget/*` routes deliberately unchanged — internal API surface, renaming is cosmetic and would risk breaking session 72/73 substrate. **Tests added:** 8 new client tests in [`apps/client/src/lib/__tests__/rowGlyph.test.ts`](apps/client/src/lib/__tests__/rowGlyph.test.ts) — null/undefined/empty group → `?`, income → `+` regardless of sign, transfer → `⇄`, debt → `▸`, negative essentials/lifestyle/savings/indulgence → `·`, positive non-income (refunds) → `+`, zero → `·`. Closes the rowGlyph leftover from session 70. **Dismiss route test** marked closed: pure helpers `dismissAction`/`unresolveAction` are already covered by 4 tests in `review.test.ts` from session 71; house style is helper-only coverage on routes. **Stale-debt cleanup deferred:** `wrangler d1 execute` lacks auth in the current shell; `debt_lindsay` and `debt_1331b5b0b1ca4867` still live in remote D1. The cleanup is also more than a one-line UPDATE — both have GL opening-balance JEs that need reversing alongside the archive (otherwise net worth double-counts). Logged for follow-up. **Verified live** at localhost:5174: tab bar shows TODAY · PAYCHEQUE · YIELD with envelope icon; `/budget` → `/paycheque`; `/budget/plan` → `/paycheque/plan`; `/budget/tabs/cp_marcus` → `/paycheque/tabs/cp_marcus`. **Tests:** 390→398 (+8 client). Typecheck clean. **Build:** 445.59 kB JS (+0.33 kB for redirect helper) / 41.82 kB CSS (unchanged). **Files added:** `apps/client/src/lib/__tests__/rowGlyph.test.ts`. **Files changed:** `apps/client/src/App.tsx`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/pages/Today.tsx`, `apps/client/src/pages/Reconciliation.tsx`, `apps/client/src/pages/Plan.tsx`. **Decisions locked:** (1) Worker `/api/budget/*` routes stay — cosmetic rename not worth the substrate risk. (2) Open Tabs final home: stays inside the Paycheque page (06 OPEN TABS). No `/people` index needed. (3) Stale-debt cleanup needs GL-aware reversing JE work in a separate session, not a quick D1 UPDATE. **NOT in scope:** progressive vocabulary RPG (session 75 — `user_vocabulary_level` setting + milestone toasts).

- **Paycheque page rebuild — Budget.tsx restructured top-to-bottom, session 73** (2026-04-27): No schema, no migrations, no worker route changes. Pure client rewrite around the session 72 substrate. **`apps/client/src/pages/Budget.tsx`** rewritten end-to-end. New section order: header → 01 INCOME (paycheque amount + period range, mascot-purple cat-rule, signed `+` prefix) → 02 COMMITTED (lines from `/api/paycheque` snapshot — bills, debt mins, savings goals, savings sweeps; total in section-meta; `ruleClassFor` maps `bill→essentials`, `debt_min→debt`, `savings_*→savings`; due_date hint per row; empty-state copy "nothing yet · bills, debt mins and savings sweeps land here") → 03 RECOMMENDED (PlanRow renumbered 02→03, label "Toward debt next paycheque") → tank (shrunk 60vh→40vh / minHeight 380→280, overlay label changed "Left to spend" → "Available") → DRAFT THIS PAYCHEQUE tear-tab → 05 TRENDS (velocity caption + 4 history mini-tanks combined) → 06 OPEN TABS (kept). **Removed sections:** NetWorthRow component (still reachable via Settings → Trial balance), Goals strip (folded into Committed via savings_goal lines from snapshot), Debts list (debt mins surface in Committed; managing handled at /debts). **New types** in Budget.tsx: `CommittedSource`, `CommittedLine`, `PaycheckSnapshot` mirror worker `periodIntelligence.ts` shapes. **New query** `paycheque-snapshot` against `GET /api/paycheque`; draft mutation invalidates it alongside allocations. **Verified live** at localhost:5174/budget: section order correct (01 INCOME +$4,250.00, 02 COMMITTED $0.00 empty-state, 03 RECOMMENDED $2,533.52, tank shows AVAILABLE $506.21, DRAFT button, 05 TRENDS with indulgence 30d 64% velocity + 4 mini-tanks, 06 OPEN TABS with 4 cps). COMMITTED renders empty because session 72 worker isn't deployed yet (route returns 404 in prod); page degrades gracefully. Once this commit pushes, CI deploys session 72 + 73 and COMMITTED populates with real bill/debt/savings lines. **Tests:** 390 pass unchanged (no worker code touched). Typecheck clean. **Build:** 445.26 kB JS (-1.06 kB) / 41.82 kB CSS (-0.22 kB) — smaller because Net Worth + Goals + Debts sections deleted. **Files changed:** `apps/client/src/pages/Budget.tsx`. **Decisions locked:** (1) Net Worth removed from Paycheque tab — Settings → Trial balance is the canonical surface. (2) Goals fold into Committed via the snapshot's `savings_goal` source lines. (3) Debt cards removed from Paycheque tab — debt mins surface in Committed, debt extras surface in Recommended, managing happens at `/debts`. (4) Tank stays as the AVAILABLE visual but smaller (40vh) since Committed now carries the forward-looking commitment narrative. (5) Velocity line and mini-tanks combined under TRENDS. **NOT in scope:** nav rename Budget→Paycheque (session 74), small B leftovers (glyph mapper unit tests, dismiss route test) (session 74), stale-debt cleanup in live D1 (session 74), progressive vocabulary RPG (session 75).

- **Paycheque substrate — unified intelligence layer (kicks off the Paycheque Program), session 72** (2026-04-27): No schema, no migration, no client UI changes. The user pointed out the payment plan recommended $1,400 toward debt without knowing Rogers was due in 3 days or the auto-savings sweep fires Friday. Capacity had been computed from a 90d rolling `essentials_monthly_cents` average since session 64 — defensible math, dishonest answer. This session unifies recurring detector + debts + goals + lifestyle history into one "real available this paycheque" computation and rewires the kernel + drafter to read from it. **New lib** [`apps/worker/src/lib/periodIntelligence.ts`](apps/worker/src/lib/periodIntelligence.ts): pure helpers `bucketRecurring(group)` (essentials/lifestyle→bill, transfer→savings, debt→debt-skip, else→skip), `billsInWindow(recurring, today, period_end)` (forward window today→period.end_date, drops debt+transfer + zero amounts + out-of-window), `recurringSavingsInWindow` (transfer-only mirror), `lifestyleBaselinePerPaycheque(90d_cents)` (90d / 6.5 paycheques), `monthlyToPerPaycheque(monthly)` (×12/26), `computePaycheckCommitments({bills, debt_mins, savings_goals, recurring_savings})` returning `{bills_cents, debt_mins_cents, savings_cents, total_cents, lines: CommittedLine[]}` with stable source labels + ref_ids, `essentialsBaselineForKernel({bills, savings, lifestyle_baseline})` (sum — debt mins + indulgence stay separate kernel inputs), `computeAvailableForDebtExtra(...)` (paycheque − committed − lifestyle − indulgence, clamped 0), `computeSpendingMoney(...)` (paycheque − committed − debt extra). Impure orchestrators `loadPaycheckInputs(env)` (returns `PaycheckInputs` for callers needing kernel-grade numbers) and `loadPaycheckSnapshot(env)` (returns user-facing `PaycheckSnapshot`: period, paycheque_cents, committed, available_for_debt_extra_cents, spending_money_cents, baseline {lifestyle, indulgence}, spent_so_far_cents). **New route** [`apps/worker/src/routes/paycheque.ts`](apps/worker/src/routes/paycheque.ts) at `GET /api/paycheque`. **Plan kernel rewire** [`apps/worker/src/routes/plan.ts`](apps/worker/src/routes/plan.ts): `loadPlanInputs` calls `loadPaycheckInputs`, derives essentials_baseline from `essentialsBaselineForKernel({bills, savings, lifestyle_baseline})`. `PlanInputsBundle` extended with `commitment_lines`, `bills_cents`, `savings_cents`, `lifestyle_baseline_cents` so `GET /api/plan` surfaces the breakdown. Pre-onboarding fallback (no period yet) preserved. **Drafter rewire** [`apps/worker/src/routes/allocations.ts`](apps/worker/src/routes/allocations.ts): `draftForPeriod` uses periodIntelligence when `inputs.period.id === periodId` (production-only path); legacy settings path retained for non-current periods. Drafter and plan kernel now agree on essentials. **Tests** [`apps/worker/src/__tests__/periodIntelligence.test.ts`](apps/worker/src/__tests__/periodIntelligence.test.ts): 31 new across 9 describes (bucketRecurring 7, billsInWindow 6, recurringSavingsInWindow 2, lifestyleBaselinePerPaycheque 4, monthlyToPerPaycheque 2, computePaycheckCommitments 3, essentialsBaselineForKernel 2, computeAvailableForDebtExtra 3, computeSpendingMoney 2). Updated `plan.test.ts` baseBundle for the 4 new fields. **Test count: 359→390 (+31).** Build clean (446.32 kB JS / 42.04 kB CSS unchanged — no client code touched). Typecheck clean. Worker code changed; CI auto-deploy on push. **Files added:** `apps/worker/src/lib/periodIntelligence.ts`, `apps/worker/src/routes/paycheque.ts`, `apps/worker/src/__tests__/periodIntelligence.test.ts`. **Files changed:** `apps/worker/src/index.ts`, `apps/worker/src/routes/plan.ts`, `apps/worker/src/routes/allocations.ts`, `apps/worker/src/__tests__/plan.test.ts`. **Decisions locked:** (1) `essentials_baseline_cents` semantics in the kernel changes from "90d average" to "real upcoming bills + savings sweeps + lifestyle baseline." Param name unchanged. (2) Lifestyle baseline = 90d lifestyle / 6.5. Without it the kernel pretends the user can live on bills + indulgence alone. (3) Forward window for bills = [today, period.end_date]; bills after end_date belong to next period. (4) Debt-group recurring (CC payments) excluded from "bills" — debts table handles minimums separately to avoid double-counting. (5) Transfer-group recurring becomes savings sweeps. (6) Indulgence stays a separate kernel input. (7) `spending_money_cents` = paycheque − committed − debt_extra (lifestyle baseline + indulgence are sub-budgets WITHIN this bucket, not reductions of it). (8) Pre-onboarding fallback preserved on both rewired sites — no regression for new users. **NOT in scope:** Today route not rewired (its `printing_ahead` queue already reads `period_allocations` which the drafter now seeds with honest numbers; substrate fix flows through automatically once the user re-drafts). Per-paycheque vs monthly debt min cadence mismatch is pre-existing. **Next sessions (Paycheque Program):** Session 73 = Paycheque page rebuild (Budget.tsx restructured top-to-bottom: Income → Committed → Recommended → Available [tank moves down + smaller] → Trends; goals fold into Committed; Net Worth moves out; Open Tabs gets new home). Session 74 = nav rename Budget→Paycheque + small B leftovers (glyph tests, dismiss test) + stale-debt cleanup. Session 75 = progressive vocabulary RPG (C from session 66's audit).

- **Session B part 2 + D shipped — adherence + notifications, session 71** (2026-04-27): No schema, no migrations. **Review unresolve.** [`apps/worker/src/routes/review.ts`](apps/worker/src/routes/review.ts) gained `POST /:id/unresolve` (clears resolved_at, idempotent on already-open rows, writes edit_log `review_unresolve`). Pure helpers `dismissAction(resolved_at)` / `unresolveAction(resolved_at)` exported for tests. [`apps/client/src/pages/Review.tsx`](apps/client/src/pages/Review.tsx) tracks `lastResolvedId` in useState, sets it on resolve/dismiss success, renders an ↺ ink-glyph row at top when present (calls unresolve route, clears the id on success). **Reconciliation adherence.** [`apps/worker/src/routes/reconciliation.ts`](apps/worker/src/routes/reconciliation.ts) gained `GET /missed-installments` — past-period query of `period_allocations WHERE category_group='debt' AND plan_id NOT NULL AND stamped_at NULL AND pay_period.end_date < today`. Returns `{count, total_cents, installments: [...]}` newest period first, capped at 100. [`apps/client/src/pages/Reconciliation.tsx`](apps/client/src/pages/Reconciliation.tsx) gained Missed installments perforated section between Summary and Accounts — sienna kicker + count, debt-dotted rows linking to /budget/plan, sienna outstanding total. **Budget hierarchy reorder.** [`apps/client/src/pages/Budget.tsx`](apps/client/src/pages/Budget.tsx) PlanRow moved above the tank (renumbered 03b → 02). Per session 66 spec: target user's first question is "how do I get out of debt" not "how much did I spend". **Notification expansion (D).** New worker route file [`apps/worker/src/routes/notifications.ts`](apps/worker/src/routes/notifications.ts) at `/api/notifications/schedule`. Pure lib [`apps/worker/src/lib/notificationSchedule.ts`](apps/worker/src/lib/notificationSchedule.ts): `nextDueDate(today, due_day)` (clamps last day of month, rolls into next month when day already passed), `isTankLow(tank)` (zero/negative remaining → true; otherwise actual daily burn vs paycheque/14 expected × 0.6 threshold), `buildNotificationSchedule(input, today)` returning typed `NotificationRow[]` with stable id ranges (100-149 bills, 200-249 debts, 300 paycheque, 400 tank-low). Bills fire day-before due; debts fire 3 days before next due_day; paycheque fires day-of only; tank-low fires today when pacing is bad. Voice direct ("Rogers hits tomorrow.", "Don't let it slip."). [`apps/client/src/native/notifications.ts`](apps/client/src/native/notifications.ts) gained `syncDynamicNotifications()` — fetches schedule, cancels dynamic id ranges (100-149, 200-249, 300, 400), schedules each at 9am local on its fire_date. [`apps/client/src/native/bootstrap.ts`](apps/client/src/native/bootstrap.ts) calls it best-effort after `scheduleAllNotifications`. **Tests:** 4 new in [`review.test.ts`](apps/worker/src/__tests__/review.test.ts), 16 new in [`notificationSchedule.test.ts`](apps/worker/src/__tests__/notificationSchedule.test.ts) (nextDueDate 4 cases, isTankLow 4 cases, buildNotificationSchedule 8 cases). Total 339→359. **Build:** 446.32 kB JS (+2.64 kB) / 42.04 kB CSS (unchanged). Typecheck clean. Worker code changed (3 new routes); push triggers CI auto-deploy. **Files added:** `apps/worker/src/lib/notificationSchedule.ts`, `apps/worker/src/routes/notifications.ts`, `apps/worker/src/__tests__/notificationSchedule.test.ts`, `apps/worker/src/__tests__/review.test.ts`. **Files changed:** `apps/worker/src/routes/review.ts`, `apps/worker/src/routes/reconciliation.ts`, `apps/worker/src/index.ts`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/pages/Reconciliation.tsx`, `apps/client/src/pages/Review.tsx`, `apps/client/src/native/notifications.ts`, `apps/client/src/native/bootstrap.ts`. **Of session 66's A/B/C/D: A done (sess 67), B done (parts 1+2 in sess 68 + 71), D done (sess 71). Only C remains.**

- **6-glyph row-type vocabulary across Today / Reconciliation / Budget — session 70 part 2** (2026-04-27): No schema, no worker changes, no redeploy. Session 69 added `cat-dot` per-category color to Today queue rows including Payday and the Send-link postage. User pushed back: dots were meant for the bill/payment vocabulary only, not income or chit-actions. Then escalated: "I want a system for everything" — meaning a unified row-type marker across every money row in the app, and the existing ↻ glyph at 12px muted was invisible. **6-glyph vocabulary locked.** Mono, ink-color, fixed 16px slot. (1) `↻` recurring — bills, subscriptions, rent. (2) `▸` payment — toward debt, plan installment, savings sweep. (3) `+` income — paycheque, refund, deposit. (4) `⇄` transfer — between own accounts incl CC payment. (5) `·` one-off — regular spend. (6) `?` unknown — needs a category. **New shared lib:** [`apps/client/src/lib/rowGlyph.ts`](apps/client/src/lib/rowGlyph.ts) exports `RowGlyph` type + `glyphForCategory(group, signedCents)` mapper. Decision tree: no group → `?`; income → `+`; transfer → `⇄`; debt → `▸`; everything else → `·` (outflow) or `+` (inflow). Bill-aware surfaces (Today's `printing_ahead`) override with `↻` since that's where the recurring detector tag is reliable. **CSS** [`apps/client/src/index.css`](apps/client/src/index.css): `.row-glyph` (16px wide mono ink slot) + tone variants `.muted` `.income` (mascot purple) `.debt` (gold). **Today** [`apps/client/src/pages/Today.tsx`](apps/client/src/pages/Today.tsx): replaced `dotClass` model with `glyph` + `glyphTone`. Removed the `dotFor(group)` mapper. Glyph mapping: payday → `+` (income tone), plan → `▸` (debt tone), bill → `↻`, review → `?` (muted). Reconcile action stays glyph-free. Removed amount-color tinting on payday — the `+` glyph carries that signal. **Reconciliation** [`apps/client/src/pages/Reconciliation.tsx`](apps/client/src/pages/Reconciliation.tsx): glyph slot added before existing `cat-dot`. Both markers stay — glyph for row-type semantics, cat-dot for category color. **Budget AllocationSheet** [`apps/client/src/components/AllocationSheet.tsx`](apps/client/src/components/AllocationSheet.tsx): glyph leads each row inside `.row-tap`. Sheets are already grouped by category so cat-dot is implied; only glyph added. **Verified live**: Today rendered `+ Payday` (purple), `↻ Rogers $95.00` and `↻ Enbridge Gas $108.00` in 14px ink (visibility fixed); Reconciliation rendered `· Costco`, `▸ Capital One Payment`, `· Starbucks`; Budget allocations rendered `▸` on every debt row and `·` on essentials. **Decisions / push-backs:** (1) User picked Option 2 — drop cat-dots from queue, queue speaks row-type only. Cat-dots survive in Reconciliation + Budget since those are about "where money went" not "what kind of movement". (2) Refused to collapse `·` and `+` to save a glyph — the "this is a routine charge" cue is load-bearing. (3) No sticker pattern for "unrecognized merchant" beyond `?` — review queue surfaces those already. **Build:** 443.68 kB JS (-1.23 kB) / 42.04 kB CSS (+0.28 kB). **Tests:** 339 pass (unchanged; mapper has no unit test yet — flag for next session). Typecheck clean. **Files added:** `apps/client/src/lib/rowGlyph.ts`. **Files changed:** `apps/client/src/index.css`, `apps/client/src/pages/Today.tsx`, `apps/client/src/pages/Reconciliation.tsx`, `apps/client/src/components/AllocationSheet.tsx`.

- **Today send-link removed from Open Tabs, lives in Counterparty drill-in only — session 70 part 1** (2026-04-27): No schema, no migration, no worker changes, no redeploy. [`apps/client/src/pages/Today.tsx`](apps/client/src/pages/Today.tsx): removed `Share`/`Capacitor`/`MailArt` imports, `latest_owes_you_split_id` from Counterparty type, `linkBusy`/`linkErr` state, `sendPaymentLink` function, the postage stamp button from Open Tabs rows, and the linkErr display block. Tab rows now show name + direction label + amount only — consistent height across all four counterparties. Send link remains in [`apps/client/src/pages/Counterparty.tsx`](apps/client/src/pages/Counterparty.tsx) per-chit, only on open `they_owe` splits (chit must exist first), slim postage `minWidth: 64, rotate(-2deg)`, MailArt + "Send link" label. Branded public landing page at `workers.dev/pay/<token>` is correctly wired and unchanged. Worker version unchanged: `24bf12c8-d940-4739-a3d5-8f759a89d81a`. Build: 443.68 kB JS (-1.23 kB) / 42.04 kB CSS. Tests: 339 pass. **Files changed:** `apps/client/src/pages/Today.tsx` only.

- **UI polish: Today bill cat-dots, send-link postage on Today, mockup canon sweep, Review ⊘ dismiss — session 69** (2026-04-27): No schema, no migrations. **Worker** [`apps/worker/src/lib/recurring.ts`](apps/worker/src/lib/recurring.ts) gained `category_group` on `RecurringPrediction`. [`apps/worker/src/routes/today.ts`](apps/worker/src/routes/today.ts) surfaces it on `printing_ahead` bill rows. [`apps/worker/src/routes/counterparties.ts`](apps/worker/src/routes/counterparties.ts) GET / now returns `latest_owes_you_split_id` so Today's send-link can fire without a separate fetch. [`apps/worker/src/routes/review.ts`](apps/worker/src/routes/review.ts) gained `POST /:id/dismiss` (idempotent, writes edit_log `review_dismiss`). **Today** [`apps/client/src/pages/Today.tsx`](apps/client/src/pages/Today.tsx): MailArt postage on `owes_you` rows fires the existing `POST /api/payment-links` against the largest open `they_owe` split, then Capacitor Share or clipboard fallback. Long-press to tear is preserved via stopPropagation on the button. Queue rows render `cat-dot` per category group via new `dotFor(group)` mapper (transfer→savings); `dotClass: string | null` replaces the old `hue` enum. **Mockup canon sweep.** New shared [`apps/client/src/components/PostageArt.tsx`](apps/client/src/components/PostageArt.tsx) exports MailArt, SeedArt, BagArt, SealArt as the single source of truth (viewBox `0 0 36 36`, currentColor, 1.4 stroke). [`ButtonsMockup.tsx`](apps/client/src/pages/ButtonsMockup.tsx) (the canonical reference) now imports from shared. [`Grow.tsx`](apps/client/src/pages/Grow.tsx) deduped local SeedArt; [`Reconciliation.tsx`](apps/client/src/pages/Reconciliation.tsx) deduped local SealArt. All live `.postage` instances aligned to mockup section 06 layout (denom + 24×24 art + label below): [`Counterparty.tsx`](apps/client/src/pages/Counterparty.tsx) Send link (was missing the envelope before), [`Plan.tsx`](apps/client/src/pages/Plan.tsx) Accept-this-plan (was missing SeedArt), Plan.tsx Refresh-rationale (same). **Slim variant on send-link only.** Per user push-back, Today + Counterparty send-link drop the denom and slim to `minWidth: 64, padding: '8px 10px 6px'` while keeping MailArt 24×24 + label below. Plan.tsx Accept/Refresh keep the full denom-bearing canon (different ceremonial weight; postage family doesn't fragment per-stamp). **Review ink-glyph rollout.** [`Review.tsx`](apps/client/src/pages/Review.tsx) commit button is now state-driven: ▸ default (no category picked, awaiting), ✦ when category selected (gold via `ink-glyph commit`). New ⊘ dismiss button (`ink-glyph danger`) wires to the new dismiss route. **↺ undo deferred** — needs `POST /api/review/:id/unresolve` + client memory of last-resolved id. **Pushed-back design choices:** envelope-as-background-with-text-overlay rejected (illegible without a paper chip carving the envelope); recommended ink-glyph vocabulary for Today's send-link rejected by user in favor of slim postage; mid-session reconciliation/week missed-installments query was started then reverted at user request (still pending session 68 part 2). **Worker deployed:** `24bf12c8-d940-4739-a3d5-8f759a89d81a`. Build: 444.91 kB JS (+1.40 kB) / 41.76 kB CSS (unchanged). Tests: 339 pass (unchanged; dismiss route is untested, flagged). Typecheck clean. **Files added:** `apps/client/src/components/PostageArt.tsx`. **Files changed:** `apps/worker/src/lib/recurring.ts`, `apps/worker/src/routes/today.ts`, `apps/worker/src/routes/counterparties.ts`, `apps/worker/src/routes/review.ts`, `apps/client/src/pages/Today.tsx`, `apps/client/src/pages/Counterparty.tsx`, `apps/client/src/pages/Plan.tsx`, `apps/client/src/pages/Review.tsx`, `apps/client/src/pages/Grow.tsx`, `apps/client/src/pages/Reconciliation.tsx`, `apps/client/src/pages/ButtonsMockup.tsx`.

- **Payment plan commitment layer — session B part 1, session 68** (2026-04-27): Migration [`0020_plan_commitment.sql`](apps/worker/drizzle/0020_plan_commitment.sql) applied to remote D1 (33 tables, additive). Adds nullable `period_allocations.plan_id` (FK plan_cache.id) + `committed_at` + indexes. **Decision: extend `period_allocations` rather than create `plan_installments`** — per-paycheque debt rows already exist; commitment is a stamp on top. **Worker** [`apps/worker/src/routes/plan.ts`](apps/worker/src/routes/plan.ts): `loadCommittedForPeriod` aggregates committed/stamped/unstamped per latest plan_id. `GET /api/plan` extended with `current_period_id` + `committed: {plan_id, committed_at, installment_count, total_cents, stamped_count, unstamped_count} | null`. `POST /api/plan/accept`: ensures plan_cache row exists for current `inputs_hash` (writes `source='manual'` if missing, no AI), batched UPDATE on every debt-group allocation in current period setting plan_id + committed_at, edit_log per row (`reason='plan_accept'`). 400s: `no_current_period`, `no_debt_allocations`. `POST /api/plan/release`: clears plan_id + committed_at on all committed debt allocations in current period (preserves stamped_at — releasing undoes commitment, not payment), edit_log per row (`reason='plan_release'`). **Today queue** [`apps/worker/src/routes/today.ts`](apps/worker/src/routes/today.ts): pure helper `stripRoleSuffix(label)` strips trailing ` priority|avalanche|min` so installments aggregate per-debt; pulls committed-not-stamped debt allocations and surfaces as `kind:'plan'` rows in `printing_ahead` with `due_date=period.end_date`. Removed session-65 client-side "plan-recommends" injection (paycheque-within-24h heuristic) — now server-driven via committed installments. Plan rows render with `.cat-dot.debt` prefix + debt-gold metric color, link to `/budget/plan`. **Plan UI** [`apps/client/src/pages/Plan.tsx`](apps/client/src/pages/Plan.tsx): new "Commitment" perforated section. Uncommitted: postage stamp `$1 ACCEPT THIS PLAN` (rotation -1.6deg) + italic hint "Stamp the plan to lock these installments. Today will surface them. Reconciliation will track them." Committed: `RELEASE` stamp-square + `N installments · $X` + caption `committed YYYY-MM-DD · K/N paid`. Mutations invalidate plan/today/allocations queries. **Tests**: 6 new in [`today.test.ts`](apps/worker/src/__tests__/today.test.ts) — stripRoleSuffix (5 cases including embedded "Avalanche" name not stripped, case-insensitive). Total 333→339. **Live verification** at localhost:5174: `/budget/plan` ACCEPT → 6 installments × $469.40 committed (only seeded mins; full kernel needs Draft first); UI flipped to RELEASE + `0/6 PAID`; Today queue showed 6 plan-kind rows at 10d before Payday at 11d; RELEASE flipped UI back to ACCEPT and showed `RELEASED · 6 CLEARED`. **Worker deployed:** `9176e948-ebd2-4546-af63-4d937a0e8553`. Build: 443.51 kB JS / 41.76 kB CSS (+2.20 kB JS for commitment UI). Typecheck clean. **Files added:** `apps/worker/drizzle/0020_plan_commitment.sql`, `apps/worker/src/__tests__/today.test.ts`. **Files changed:** `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/plan.ts`, `apps/worker/src/routes/today.ts`, `apps/client/src/pages/Plan.tsx`, `apps/client/src/pages/Today.tsx`. **Of session 66's four next-priorities (A/B/C/D), A done in session 67; B partly done — commitment layer + Today queue + accept/release UI shipped. Remaining for B (session B part 2):** (1) Reconciliation adherence — Sunday seal should surface committed-not-stamped debt allocations from past periods as missed installments. (2) Budget hierarchy reorder — payoff plan section should move above the tank per session 66 spec. (3) UX note: acceptance commits whatever is currently in `period_allocations`; user must "Draft this paycheque" on Budget first to seed the kernel's priority/avalanche rows beyond the seeded mins.

- **Action vocabulary v2 rollout — session A of session 66's design audit shipped, session 67** (2026-04-27): Five swaps shipped, no schema, no new CSS (vocabulary classes already existed from session 57's mockup). (1) [`Counterparty.tsx`](apps/client/src/pages/Counterparty.tsx) "Send link" `.stamp stamp-purple` → `.postage` with $0.47 denom, rotation -2deg, only on `they_owe` chits. (2) [`Plan.tsx`](apps/client/src/pages/Plan.tsx) "Refresh rationale" `.stamp stamp-square` → `.postage` with AI denom, rotation 1.4deg. (3) [`Review.tsx`](apps/client/src/pages/Review.tsx) "Resolve" `.stamp stamp-square` → `.ink-glyph commit ▸` (gold). (4) [`Settings.tsx`](apps/client/src/pages/Settings.tsx) Export CSV `.stamp` → `.sticker sticker-cool` with ⤓ glyph (mint, tape strip top). (5) [`Settings.tsx`](apps/client/src/pages/Settings.tsx) Clear token `.stamp stamp-danger` → `.sticker sticker-warn` with ⊘ glyph (pink). **Verified live** at localhost:5174 across `/settings`, `/budget/plan`, `/budget/tabs/cp_marcus`, `/review` — no console errors, all four vocabularies rendering as designed (postage rotation + dashed outline + denom corner, ink-glyph rotated per-row, sticker tape strip + 4deg tilt). **Debug pass**: 333 tests pass (unchanged), typecheck clean, build clean (441.31 kB JS / 41.76 kB CSS, +0.60 kB JS for postage chrome on Counterparty/Plan, CSS unchanged). No worker code changed; no redeploy. **Files changed**: `apps/client/src/pages/Counterparty.tsx`, `apps/client/src/pages/Plan.tsx`, `apps/client/src/pages/Review.tsx`, `apps/client/src/pages/Settings.tsx`. **Of session 66's four next-priorities (A/B/C/D), A is now done.** B (payment plan commitment layer with `plan_installments` table, accept-this-plan flow, Today queue installments, reconciliation adherence checks), C (progressive vocabulary RPG unlock toasts gated by Phase milestones), D (notification expansion: bill due tomorrow, paycheque landed, debt minimum due in 3d, tank-low pacing) remain.

- **Payment plan UI + AI rationale — session 60 (final session of the Accounting Upgrade Program) shipped, session 65** (2026-04-27): Migration [`0019_plan_cache.sql`](apps/worker/drizzle/0019_plan_cache.sql) applied to remote D1 (33 tables now). New `plan_cache` table (id, generated_at, inputs_hash, plan_json, rationale_text, observations_json, source CHECK ai|manual|pending) with generated_at + hash indexes. **Plan route extended** [`apps/worker/src/routes/plan.ts`](apps/worker/src/routes/plan.ts): `loadPlanInputs` + `runPlan` extracted as helpers. New pure `hashPlanInputs(b)` returns stable hash from sorted-debt fields + paycheque/essentials/indulgence/velocity. New pure `validateAiRationale(raw)` enforces shape (rationale string ≤800 chars, observations string[] capped at 3 each ≤240 chars). New `POST /api/plan/refresh`: rate-limited 10/hr (counted from `plan_cache WHERE source='ai'`), pre-writes pending row, calls `gpt-4o-mini` (cheaper than the recommend route's `gpt-4o`) with system prompt forbidding arithmetic invention, AI returns `{rationale, observations}`, validated, UPDATE source→'ai'. When capacity_cents <= 0 the AI call is skipped (rationale hardcoded "No overflow this paycheque. Mins only."). GET / extended to read latest cache row by hash and surface `rationale`/`observations`/`rationale_generated_at`/`rationale_source`/`inputs_hash`. **Plan-aware allocations**: [`allocationSeed.ts`](apps/worker/src/lib/allocationSeed.ts) gained `DraftPlanDebt` type + optional `plan_debts` field on `SeedInputs`. When non-empty, debt rows come from kernel labeled `<Name> priority|avalanche|min`; otherwise legacy `DraftDebt[]` fallback. [`allocations.ts`](apps/worker/src/routes/allocations.ts) `draftForPeriod` now reads paycheque + essentials + indulgence + per-debt 90d charge velocity, calls `computePlan`, aggregates per-debt across roles (priority > avalanche > min), passes as `plan_debts`. **Client `/budget/plan` page** [`apps/client/src/pages/Plan.tsx`](apps/client/src/pages/Plan.tsx) — full-receipt chrome (mascot sigil + PAYOFF PLAN wordmark, debt count + capacity subhead, BACK link). Sections: hero `.hero-num` total interest + "vs minimums-only baseline · saves $X" caption (savings in gold), per-debt rows with role + payoff timeline (`payoffLabel`: `1y 5mo` / `10mo` / `this month` / `beyond horizon`) + amount, expandable to show 12-month balance/payment per debt. Rationale section with Refresh stamp + cache timestamp + source tag. Observations list when present. Footer `** recomputed live · refresh to ask **`. **Debts.tsx severity colors per spec**: `SEVERITY_COLOR` map — sage `current`, gold `past_due`, sienna `in_collections`, danger `charged_off`, mascot-purple `settled_partial`. Debt cards now show severity always with role tag from plan ("priority" / "avalanche" / "min only"). **Budget plan row** [`Budget.tsx`](apps/client/src/pages/Budget.tsx) gained `<PlanRow>` ledger section "03b Payoff plan" linking to `/budget/plan`, capacity + savings caption + total-toward-debt amount on the right. **Today plan-recommends row** [`Today.tsx`](apps/client/src/pages/Today.tsx) gained plan useQuery; queue injects `Plan: $X to <Name>` row at days_until=0 when paycheque has landed within 24h (`tank.period.start_date` within 86400000ms) AND a non-min allocation exists, links to `/budget/plan`. **Tests**: 12 new in [`plan.test.ts`](apps/worker/src/__tests__/plan.test.ts) (hashPlanInputs stability + change-on-paycheque + change-on-severity + change-on-velocity + order-independence; validateAiRationale clean + empty + non-object + 3-cap + 800-char-cap + 240-char-obs-skip + missing-observations). 3 new in `allocationSeed.test.ts` (plan_debts overrides + skips zero + falls back when empty). Total 318→333. **Worker deployed**: `0e551edc-5ff4-4138-808d-52f6203029a9`. Build: 440.71 kB JS / 41.76 kB CSS (+7.27 kB JS / +0.24 kB CSS). Typecheck clean. **Live verification** at localhost:5174: /budget/plan renders 5 debts capacity $2,144.12, total interest $2,411.67 vs baseline $59,082.16, saves $56,670.49; allocations sane (mins on Luther/Bowgull $100, $89.40 TD Visa min, then $1,365 avalanche to Capital One paying off this month, $879.12 spillover to TD Visa ··4429). /debts shows current debts in sage with avalanche/min-only role tags. /budget shows new "03b Payoff plan" row with $2,533.52 next-paycheque sum. **Files added**: `apps/worker/drizzle/0019_plan_cache.sql`, `apps/worker/src/__tests__/plan.test.ts`, `apps/client/src/pages/Plan.tsx`. **Files changed**: `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/plan.ts`, `apps/worker/src/lib/allocationSeed.ts`, `apps/worker/src/routes/allocations.ts`, `apps/worker/src/__tests__/allocationSeed.test.ts`, `apps/client/src/App.tsx`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/pages/Debts.tsx`, `apps/client/src/pages/Today.tsx`. **Cost**: rationale uses `gpt-4o-mini`, skips OpenAI when capacity 0, rate-limited 10/hr; realistic monthly cost $2-5 at 2-3 refreshes/day. **Caveats / deferred**: (1) Kernel recomputes on every GET (no caching of plan output, only the AI rationale is cached). Cheap, no DB writes. (2) `loadPlanInputs` is duplicated between `plan.ts` and `allocations.ts`. Extract to `lib/planInputs.ts` if a third caller appears. (3) Today plan row never shows when `tank.period.start_date` is null. Edge case, acceptable. (4) `validateAiRationale` only checks shape/length — doesn't post-hoc verify every dollar in rationale appears in input. AI receives the kernel's exact numbers and instruction is explicit; future hardening could regex-check. (5) Severity badge now visible for every debt (slightly noisier than the previous "hide when current"). Trade-off accepted: surfacing the role tag is more useful. **The Accounting Upgrade Program is complete** (sessions 52→60 / real sessions 57→65 across 9 sessions). Next sessions free for new product work or the Phases 2-6 multi-user groundwork.

- **Severity column + payment plan kernel — session 59 of the Accounting Upgrade Program shipped, session 64** (2026-04-27): Migration [`0018_debt_severity.sql`](apps/worker/drizzle/0018_debt_severity.sql) applied to remote D1 (32 tables, column add only). Adds `debts.severity` (NOT NULL DEFAULT 'current' CHECK IN current|past_due|in_collections|charged_off|settled_partial), nullable `severity_changed_at`, and `debts_severity_idx`. **New lib** [`apps/worker/src/lib/paymentPlan.ts`](apps/worker/src/lib/paymentPlan.ts) — fully pure, no DB touch. Types: `DebtSeverity`, `PlanDebtInput`, `PlanInput`, `PlanAllocation`, `MonthlySchedule`, `PlanOutput`. Helpers: `isPriority` (collections/charged_off/past_due → true), `minPaymentCents` (caps both fixed and percent at principal so we never ask for more than owed; $10 floor on percent), `monthlyInterestCents` (APR/12 of principal), `planOrder` (priority tier sorted by SEVERITY_RANK then smallest balance first; avalanche tier sorted by highest APR first; `settled_partial` shares rank 3 with `current` so both fall through to avalanche), `allocateCapacity` (Tier 0 mins always, then ordered priority/avalanche extras capped by per-debt headroom), `computeCapacity` (paycheque − essentials − indulgence − sum mins, clamped to 0), `computePlan` (orchestrator: runs `simulate(plan)` and `simulate(baseline)` over a 60-month horizon, accruing monthly interest + charge velocity (priority debts ignore velocity) then applying allocations; returns `next_paycheque_allocations`, `monthly_schedule`, `payoff_months`, `total_interest_cents`, `baseline_total_interest_cents`, `savings_cents`, `capacity_cents`). **Routes**: [`apps/worker/src/routes/debts.ts`](apps/worker/src/routes/debts.ts) extended — `parseDebtPatch` accepts `severity` (validated via `isDebtSeverity`); PATCH stamps `severity_changed_at` when severity actually changes; POST accepts optional `severity` defaulting to 'current'; new dedicated `POST /api/debts/:id/severity` writes edit_log with `reason='debt_severity_change'`. **New route file** [`apps/worker/src/routes/plan.ts`](apps/worker/src/routes/plan.ts): `GET /api/plan` reads non-archived debts, latest pay_period (guarded by `start_date <= date('now')`), `essentials_monthly_cents` setting (falls back to `_derived`), optional `indulgence_allowance_cents`, computes per-debt 90d charge velocity (sum of negative-amount txns on linked account ÷ 3 → monthly), passes everything to `computePlan`. Monthly settings → per-paycheque conversion uses 26 paycheques / 12 months. Mounted at `/api/plan`. **Client**: [`Debts.tsx`](apps/client/src/pages/Debts.tsx) `Debt` type gained `severity`; new `SEVERITY_OPTIONS` + `SEVERITY_LABEL` consts; `DebtSheet` adds severity state + dropdown between Due day and Linked account; POST/PATCH body includes severity; DebtCard surfaces severity in danger-color mono caption when not 'current'. **Tests**: 25 new (paymentPlan.test.ts: 22 — isPriority×1, minPaymentCents×4 inc fixed-cap-at-principal + percent floor + zero-principal, monthlyInterestCents×2, planOrder×3, computeCapacity×2, allocateCapacity×5 inc empty + zero-cap + headroom-cap, computePlan×6 inc priority-beats-higher-APR + velocity-ignored-on-priority + velocity-traps-current-debt + empty-debts + settled_partial-as-avalanche; debts.test.ts: +3 — accepts 5 severity values, rejects unknowns, total path). Total 293→318. **Live D1 verification**: `GET /api/plan` returned `capacity_cents=214412` ($2,144.12 from $4,250 paycheque − $2,106 fixed costs/mins), `savings_cents=5667049` (~$56,670 saved vs minimums-only baseline over 60mo), allocations sane (mins on all 5 debts, then $1,315 avalanche to Capital One @ 24.99%, then $829 spillover to TD Visa ··4429 @ 19.99%). Severity round-trip via UI confirmed (Capital One past_due → revert to current). **Worker deployed**: `2f1a72cd-602b-4b77-be82-cef75127e39f`. Build: 433.44 kB JS / 41.52 kB CSS (+0.87 kB JS, CSS unchanged). Typecheck clean. **Files added**: `apps/worker/drizzle/0018_debt_severity.sql`, `apps/worker/src/lib/paymentPlan.ts`, `apps/worker/src/routes/plan.ts`, `apps/worker/src/__tests__/paymentPlan.test.ts`. **Files changed**: `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/debts.ts`, `apps/worker/src/index.ts`, `apps/worker/src/__tests__/debts.test.ts`, `apps/client/src/pages/Debts.tsx`. **Caveats / deferred**: (1) Plan UI surface deferred to next session (program-session 60) — kernel + route are live but no `/budget/plan` page yet; consumers can poll `/api/plan` directly. (2) Plan output not cached — recomputed on every GET. Fine at current scale; cache row will land with the AI rationale work next session. (3) `draftAllocations` in `allocations.ts` still uses pure avalanche, not the plan output; rewire next session. (4) Two stale debts (`debt_lindsay` "Bowgull (Mexico)" + `debt_1331b5b0b1ca4867` "TD Visa ·· 4429") still in live D1 from session 60's audit — surfaced in plan output but should be archived in a cleanup pass before Net Worth UI ships meaningfully.

- **Period close + Net Worth — session 58 of the Accounting Upgrade Program shipped, session 63** (2026-04-27): Migration [`0017_period_close.sql`](apps/worker/drizzle/0017_period_close.sql) applied to remote D1 (32 tables now). New `period_close` table (id, period_start, period_end, closed_at, closed_by CHECK user|auto|system, trial_balance_debits_cents, trial_balance_credits_cents) with UNIQUE index on period_end and a new index on `journal_entries.locked_at`. **New lib** [`apps/worker/src/lib/periodClose.ts`](apps/worker/src/lib/periodClose.ts): pure `findCoveringClose(posted_at, closes)` returns latest covering period_end or null; pure `netWorthFromTrialBalance(rows)` returns `{assets/liabilities/equity/income/expense_cents}` from natural-sign sums; pure `mostRecentSaturday(today)` and `periodStartFromEnd(period_end)`. Impure `closeWeek(env, period_end, closed_by)` snapshots TB at period_end (refuses if delta != 0 with `unbalanced_books`), inserts row, stamps `locked_at = now()` on every JE with `posted_at <= period_end`, writes one edit_log entry, idempotent on (period_end). Impure `reverseJournalEntry(env, original_je_id, reason)` posts inverse JE in current period via postJournalEntry and writes `journal_entry/reversed_by` log. **`postJournalEntry` hardened** in [`apps/worker/src/lib/gl.ts`](apps/worker/src/lib/gl.ts): every non-`reversal` post now queries `period_close WHERE period_end >= posted_at_day` and throws `JournalEntryError(period_locked, …)` if any row matches. Reversals (source_type='reversal') are exempt — the reversing entry itself is dated in the current period; the original locked one stays. **Routes** in [`apps/worker/src/routes/gl.ts`](apps/worker/src/routes/gl.ts): `GET /api/gl/net-worth?as_of=` returns `{assets_cents, liabilities_cents, equity_cents, income_cents, expense_cents, net_worth_cents}` derived from a single TYPE-grouped sum query; `GET /api/gl/period-close` (60 newest); `POST /api/gl/period-close` body `{period_end?}` defaults to most-recent-Saturday, returns 409 `unbalanced_books` if TB delta != 0, 200 with `already:true` on idempotent re-run; `POST /api/gl/journal/:je_id/reverse` body `{reason?}` posts inverse and returns `{reversal_je_id, original_je_id}`. **Auto-close wired** into [`reconciliation.ts`](apps/worker/src/routes/reconciliation.ts) `POST /complete`: after seal logs land, calls `closeWeek(env, mostRecentSaturday(today), 'auto')` and surfaces `{period_close, period_close_skipped}` in the response. Errors are caught (returned as `period_close_skipped` string) so a TB-out-of-balance situation doesn't break sealing. **Edit-log allowlist** [`editLog.ts`](apps/worker/src/routes/editLog.ts) gained `journal_entry` and `period_close`. **Schema** added `period_close` table export. **Client** added [`apps/client/src/pages/TrialBalance.tsx`](apps/client/src/pages/TrialBalance.tsx) at `/settings/trial-balance` (read-only debug surface): mascot sigil + TRIAL BALANCE wordmark + n-accounts subtitle, perforated Debits/Credits/Delta block (delta turns sienna when non-zero with "books are unbalanced; close refused" hint), Close-this-week stamp button (disabled when unbalanced), one perforated section per type with TYPE_LABEL map (Assets/Liabilities/Equity/Income/Expenses) — subtotal + per-account path/balance rows, Recent closes section (8 newest with period_start → period_end and closed_by tag), `** end of trial **` footer. Linked from Settings 04 System with hint "debits = credits, close the week". [`Budget.tsx`](apps/client/src/pages/Budget.tsx) gained a new `<NetWorthRow>` ledger section between the tank/Draft button and historyRows: section kicker `03 Net worth` + meta link "trial >", single `.ledger-row tap` linking to /settings/trial-balance with "Assets − Liabilities" label, "$Asset − $Liability" hint, net_worth_cents on the right with chevron. **Tests**: 14 new in [`periodClose.test.ts`](apps/worker/src/__tests__/periodClose.test.ts) — mostRecentSaturday (5 cases inc bad input), periodStartFromEnd (Sat→Sun + bad input), findCoveringClose (5 cases inc boundary, ISO timestamp, no-cover, latest-wins-when-multi-cover), netWorthFromTrialBalance (4 cases inc zero, signed cleanup, balance identity assets−liab=equity+(income−expense)). Total 277→293. **Live D1 verification**: net-worth route returned `{assets_cents:2426854, liabilities_cents:1600605, net_worth_cents:826249}` (Net worth $8,262.49). Test close ran cleanly: `{period_close_id:'pc_743d8df51d6d4804', period_end:'2026-04-25', journal_entries_locked:137, already:false}`; idempotent re-run (already:true, locked:0). Reverted via `DELETE FROM period_close WHERE period_end='2026-04-25'; UPDATE journal_entries SET locked_at=NULL` to keep seed clean. **Browser preview verified** at localhost:5174: /budget shows new "03 Net worth" section ($8,262.49 with breakdown caption); /settings/trial-balance renders DEBITS / CREDITS $67,644.51 / DELTA $0.00 — equal, all 5 type sections with subtotals, Recent closes shows the test close. **Worker deployed**: `a9eaa465-e500-4f03-8e7c-ff47822fd095`. Build: 432.57 kB JS / 41.52 kB CSS (+5.92 kB JS / +0.07 kB CSS). Typecheck clean. **Files added**: `apps/worker/drizzle/0017_period_close.sql`, `apps/worker/src/lib/periodClose.ts`, `apps/worker/src/__tests__/periodClose.test.ts`, `apps/client/src/pages/TrialBalance.tsx`. **Files changed**: `apps/worker/src/db/schema.ts`, `apps/worker/src/lib/gl.ts`, `apps/worker/src/routes/gl.ts`, `apps/worker/src/routes/reconciliation.ts`, `apps/worker/src/routes/editLog.ts`, `apps/client/src/pages/Settings.tsx`, `apps/client/src/pages/Budget.tsx`, `apps/client/src/App.tsx`. **Caveats / deferred**: (1) Reverse-entry button in EditLog UI deferred — `POST /api/gl/journal/:je_id/reverse` route + helper are live and tested in the lib, but no client surface yet (edit_log entries don't currently link back to a JE id; UX needs care). Add when first real reversal is needed. (2) `closeWeek` only takes a YYYY-MM-DD period_end; no monthly close (spec is weekly only). (3) Auto-close writes `closed_by='auto'`; manual TrialBalance button writes 'user'. The `system` enum value is reserved for future cron-driven closes. (4) Trial balance page query has no `as_of` filter (defaults to all-time) — TB after a test close included more JEs than the close-time snapshot but both balanced. UI could surface as_of=period_end view if useful. Decision deferred.

- **Bank reconciliation rebuild — session 57 of the Accounting Upgrade Program shipped, session 62** (2026-04-27): Migration [`0016_cleared_flag.sql`](apps/worker/drizzle/0016_cleared_flag.sql) applied to remote D1 (still 31 tables — column add only). Adds nullable `cleared_at` to `journal_lines` plus `journal_lines_cleared_idx`. Each side of a JE clears independently against the bank statement. New pure helpers in [`apps/worker/src/routes/reconciliation.ts`](apps/worker/src/routes/reconciliation.ts): `naturalBalanceCents(type, debit, credit)` (asset/expense=DR-CR; liability/equity/income=CR-DR), `summarizeAccountReconciliation(type, lines)` returning `{gl_balance_cents, cleared_balance_cents, uncleared_count, uncleared_cents}`, `isWeekFullyReconciled(summaries)` (vacuous-true on empty). Impure: `loadAccountSummaries(env, weekStart, today)` joins `journal_lines` ↔ `journal_entries` for asset+liability GL accounts, returns per-account summary plus this-week uncleared lines (filtered by `posted_at >= weekStart`). **New routes**: `GET /api/reconciliation/accounts` (per asset/liability GL account: GL balance, cleared balance, uncleared list); `POST /api/reconciliation/lines/:line_id/clear` (sets `cleared_at=now`, idempotent on already-cleared, writes `journal_line/cleared_at` edit_log with reason `bank_rec_clear`); `POST /api/reconciliation/lines/:line_id/unclear` (NULL+reason `bank_rec_unclear`). `POST /api/reconciliation/complete` extended: returns 409 `{error:'uncleared_lines_remain', accounts:[...]}` when any account has uncleared lines unless body sends `acknowledge_outstanding: true`; on success, writes per-account `reconciliation_seal` edit_log row (or `reconciliation_seal_outstanding` when acknowledged) with serialized JSON of `{path, gl_balance_cents, cleared_balance_cents, uncleared_count, acknowledged_outstanding}`. `journal_line` added to edit-log allowlist in [`editLog.ts`](apps/worker/src/routes/editLog.ts). Schema: `journal_lines.cleared_at` added to [`apps/worker/src/db/schema.ts`](apps/worker/src/db/schema.ts). **Client**: [`Reconciliation.tsx`](apps/client/src/pages/Reconciliation.tsx) gained `AccountsResponse` type + new ACCOUNTS (n) perforated section between Summary and Tabs. Each account renders with `.cat-dot.income` (asset) or `.cat-dot.debt` (liability), GL/cleared balance caption, sage `RECONCILED` badge or sienna `N UNCLEARED` badge, tappable uncleared row list (memo/source_type + posted_at + DR/CR amount). Seal button now passes `acknowledge_outstanding=true` when totalUncleared>0 with caption "N uncleared lines · seal acknowledges them outstanding". `clearLine`/`unclearLine` mutations invalidate `reconciliation-accounts`. New `AccountRec` component encapsulates one account block. **Tests**: 11 new in `reconciliation.test.ts` (35 total in file). naturalBalanceCents (4 cases — asset/liability/equity/expense), summarizeAccountReconciliation (asset mixed cleared+uncleared, liability sign-flip cleared+uncleared, empty zeros, all-cleared cleared==gl), isWeekFullyReconciled (true / false / vacuous-true on empty). Total 266→277. **Live D1 state**: migration applied cleanly (changes:1, num_tables:31, rows_written:372 — all existing 173 JEs × ~2.1 lines now have cleared_at NULL). Browser preview test cleared one Capital One Payment line via tap → uncleared 39→38, cleared balance $0→-$90.00; reverted via `UPDATE journal_lines SET cleared_at = NULL` to restore seed. **Worker deployed**: `914d184b-376b-42d6-8197-51e10f6368ea`. Build: 426.65 kB JS / 41.45 kB CSS (+3.29 kB JS for new section). Typecheck clean. **Files added**: `apps/worker/drizzle/0016_cleared_flag.sql`. **Files changed**: `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/reconciliation.ts`, `apps/worker/src/routes/editLog.ts`, `apps/worker/src/__tests__/reconciliation.test.ts`, `apps/client/src/pages/Reconciliation.tsx`. **Caveats**: (1) Unclear route exists but UI doesn't expose it yet — uncleared list only shows uncleared lines; reverting a cleared line currently requires hitting the route directly. Could add a mini "cleared this week" sublist in a future polish pass. (2) Per-line `loadAccountSummaries` issues one joined query for all asset+liability lines through today; fine for current 173-JE size, would want pagination if it grows large. (3) Sealing is currently a settings-write only — actual JE locking comes in session 63 of the program (period_close + locked_at on journal_entries).

- **Assets migration — session 56 of the Accounting Upgrade Program shipped, session 61** (2026-04-27): Migration [`0015_holding_accounts.sql`](apps/worker/drizzle/0015_holding_accounts.sql) applied to remote D1 (still 31 tables — column add only). Adds nullable `holdings.account_id` FK + index. New lib [`apps/worker/src/lib/glHoldings.ts`](apps/worker/src/lib/glHoldings.ts): pure `holdingCostCents(units_scaled, avg_cost_cents)` (units stored × 10_000), pure `holdingGlPath(wrapper, symbol)` returning `Assets:Investments:<TFSA|FHSA|RRSP|NonReg>:<SYMBOL>`, pure `inferCurrency(symbol)` ('CAD' on `.TO`/`.V`/`.CN`/`.NE`, else 'USD'), pure `computeAcb({existing, buy})` weighted-average ACB across multiple buys, pure `buyJeLines` (DR asset / CR cash), pure `sellJeLines` (3-line split with realized gain/loss against `gla_equity_realized`; collapses to 2 lines at break-even; loss debits realized, gain credits), pure `mtmDeltaLines` (DR/CR vs `gla_equity_unrealized` for mark-to-market). Impure: `runHoldingsBackfill(env)` walks holdings, creates `Assets:Investments:<Wrapper>:<Symbol>` GL accounts at id `gla_hold_<holding.id>` parented to `gla_assets_invest`, writes ACB + units_scaled + currency into `metadata_json`, posts opening JE (DR Asset / CR Equity:Opening Balance) idempotent on `source_type='holding_opening'`. `glAssetBalance(env, glaId, asOf)` returns SUM(debit)−SUM(credit) windowed via parent journal_entries.posted_at. New route `POST /api/admin/holdings-backfill` in [`gl.ts`](apps/worker/src/routes/gl.ts). Schema [`apps/worker/src/db/schema.ts`](apps/worker/src/db/schema.ts) gained `holdings.account_id`. Tests: 20 new in [`glHoldings.test.ts`](apps/worker/src/__tests__/glHoldings.test.ts) covering all helpers + ACB convergence across three sequential buys + validateLines invariant on every JE shape. Total 246→266. Live D1 state: backfill ran `{scanned:2, created_new:2, linked_existing:0, skipped:0, opening_balance_je_posted:2}`. Trial balance still equal: `{debit_cents:6764451, credit_cents:6764451, delta_cents:0}` (+$5,870 vs session 60 — XIU.TO 50u @ $36.80 = $1,840 + SPY 8u @ $50.375 = $4,030). Worker deployed: `109757e9-b6d7-48e4-b6ae-51ea68bc321e`. No client code changed (build unchanged at 423.36 kB JS / 41.45 kB CSS). **Files added**: `apps/worker/drizzle/0015_holding_accounts.sql`, `apps/worker/src/lib/glHoldings.ts`, `apps/worker/src/__tests__/glHoldings.test.ts`. **Files changed**: `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/gl.ts`. **Caveats / deferred**: (1) MTM auto-call into `POST /api/market/fetch` is deferred — helpers wired but the fetch route doesn't yet emit per-lot MTM JEs after each price refresh. Activates next session if needed. (2) Buy/sell helpers wired but not auto-called from any transaction kernel — wait until first investment-tagged transactions show up. (3) `Yield.tsx` (formerly Grow) still reads from the existing prices+holdings join, not GL. Migration to GL deferred (would change observable behavior). (4) Multi-currency lots stay in their native currency on the GL account; no FX conversion happens until sale. No USD lots in current data so the FX path is purely theoretical.

- **Liabilities migration — session 55 of the Accounting Upgrade Program shipped, session 60** (2026-04-27): Migration [`0014_debt_accounts.sql`](apps/worker/drizzle/0014_debt_accounts.sql) applied to remote D1 (still 31 tables — column add only). Adds nullable `account_id` FK on `debts` referencing `gl_accounts(id)` plus index. New lib [`apps/worker/src/lib/debtGl.ts`](apps/worker/src/lib/debtGl.ts): pure `computeInterestAccrued(principal, apr_bps, days)` (simple daily interest, half-up rounding, clamps negatives), pure `splitLoanPaymentLines({totalPaid, principal, apr_bps, daysSinceLast, debtAccountId, cashAccountId})` (3-line JE: DR debt principal portion / DR Expenses:Debt Service interest / CR cash; collapses to 2 lines at 0% APR; caps interest at total when accrual exceeds payment), pure `statementMismatchCents(gl, stmt)` (signed cents diff). Impure: `runDebtBackfill(env)` walks non-archived debts via 3 resolution paths — (a) `account_id_linked` set → look up that bank account's existing GL liability by path (`Liabilities:CreditCards:<name>` / `Liabilities:Loans:<name>` already created by session 58 glBackfill), link; (b) counterparty exists with name=debt.name → link to counterparty's account_id (avoids double-counting personal IOUs that exist in both tables); (c) free-standing debt → create new `Liabilities:Loans:<Name>` GL account at id `gla_debt_<debt.id>` + post opening-balance JE (`DR Equity:Opening Balance / CR Liabilities:Loans:<Name>`) idempotent on `source_type='debt_opening'`. `glLiabilityBalance(env, glaId, asOf)` returns SUM(credit)−SUM(debit) windowed via parent journal_entries.posted_at. `reconcileStatements(env, windowDays=60)` joins `cc_statement_snapshots` to `debts`, computes per-statement GL vs snapshot mismatch. Routes: [`gl.ts`](apps/worker/src/routes/gl.ts) gained `POST /api/admin/debt-backfill` + `GET /api/gl/statement-reconcile?window_days=N`. [`debts.ts`](apps/worker/src/routes/debts.ts) GET / extended with subquery returning `gl_balance_cents` (NULL when not linked). Schema [`apps/worker/src/db/schema.ts`](apps/worker/src/db/schema.ts) gained `debts.account_id`. Client: [`Debts.tsx`](apps/client/src/pages/Debts.tsx) `Debt` type gained optional `gl_balance_cents`; per-debt amount + `totalOwed` reads GL when present, falls back to `principal_cents`. Tests: 15 new in [`debtGl.test.ts`](apps/worker/src/__tests__/debtGl.test.ts) — computeInterestAccrued (zero principal/apr/days, $1000@19.99%/30d=$16.43 base, negative clamp, fractional truncation), splitLoanPaymentLines (zero total, zero APR 2-line, positive APR 3-line, interest≥total fallback, negative total), statementMismatchCents (match=0, signed dirs, fractional truncation). All feed validateLines() invariant. Total 231→246. Live D1 state: backfill ran `{scanned:5, linked_existing_bank:2, linked_counterparty:0, created_new:3, opening_balance_je_posted:3, skipped:0}`. Trial balance still equal: `{debit_cents:6177451, credit_cents:6177451, delta_cents:0}`. GL-balance per debt: TD Visa $4,824.08 + Capital One $449.80 (linked, transaction-driven), TD Visa ··4429 $8,432.17 + Bowgull (Mexico) $1,200 + Luther (Mexico) $1,100 (free-standing openings). Worker deployed: `697d1c1e-681e-442c-8281-dab26d01fd5f`. Build: 423.36 kB JS (+0.12) / 41.45 kB CSS (unchanged). **Files added**: `apps/worker/drizzle/0014_debt_accounts.sql`, `apps/worker/src/lib/debtGl.ts`, `apps/worker/src/__tests__/debtGl.test.ts`. **Files changed**: `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/gl.ts`, `apps/worker/src/routes/debts.ts`, `apps/client/src/pages/Debts.tsx`. **Caveats / data hygiene flags for next session**: (1) Counterparty-match path scored 0 hits — debt name "Luther (Mexico)" vs counterparty "Luther" is exact-match miss. Luther's $1,100 IOU is now in TWO GL accounts (`Assets:Receivable:Luther` -110000 from cp path + `Liabilities:Loans:Luther (Mexico)` +110000 from this session). Trial balance preserved (equity offsets), but Net Worth (session 64) will double-count unless cleaned. Recommend archiving `debt_bowgull` + reversing its opening-balance JE — counterparty Luther is the canonical record. (2) Live D1 has two stale debts surfaced by backfill: `debt_lindsay` (privacy-leak ID + display name "Bowgull (Mexico)", 120000 cents) and `debt_1331b5b0b1ca4867` ("TD Visa ·· 4429", 843217 cents — orphan UI test data). Both got opening-balance JEs of face value. Should be archived + reversed when Net Worth ships. None are tracked-file/seed leaks — pure data hygiene. (3) Loan principal/interest split helper wired but not yet auto-called: no loan-type debts in current data, no APR>0 on free-standing debts. Activates when first non-zero-APR loan transaction lands. (4) CC payment JEs continue through the existing transfer-clearing pattern (paired txns on chequing + credit, both 'transfer', clearing through Income:Transfer). Spec preferred single `DR Liab:CC / CR Cash` JE — cosmetic, math identical, deferred. (5) CC statement reconciliation endpoint live but no UI surface yet — Reconciliation page surfacing deferred (program rule minimizes new screens).

- **AR/AP migration — session 54 of the Accounting Upgrade Program shipped, session 59** (2026-04-27): Migration [`0013_counterparty_accounts.sql`](apps/worker/drizzle/0013_counterparty_accounts.sql) applied to remote D1 (still 31 tables — column add + per-row INSERT + UPDATE only). Adds `account_id` FK to `counterparties` referencing `gl_accounts(id)`, then `INSERT OR IGNORE` per existing CP at path `Assets:Receivable:<Name>` parent `gla_assets_recv`, then `UPDATE counterparties SET account_id`. NET model — one signed asset account per person, negative balance = I currently owe them, no separate Liabilities:Payable subtree. New lib [`apps/worker/src/lib/glCounterparty.ts`](apps/worker/src/lib/glCounterparty.ts): pure `splitCreateLines()` (they_owe→DR cp/CR Income:Reimbursement; i_owe→DR Expenses:Lifestyle/CR cp), pure `splitEventLines()` (handles all 4 quadrants of direction × delta sign — payment normal case is delta<0; positive delta = tab grew = reverses legs); impure `resolveCounterpartyGl()` (creates GL row if missing, writes FK back to `counterparties`), `resolveTxnCashGl()` (looks up bank GL account by path from txn's account, falls back to `gla_equity_opening`), `postSplitCreateJe()` idempotent on `source_type='split'`, `postSplitEventJe()` idempotent on `source_type='split_event'` and **deletes any existing transaction-source JE for the linked txn first** so split_event JE owns the cash leg without double-counting income/expense; `runArApBackfill(env)` walks all splits + split_events posting any missing JEs. Routes: [`splits.ts`](apps/worker/src/routes/splits.ts) POST + POST /:id/event call JE helpers (errors swallowed so JE failure doesn't break split mutation); [`import.ts`](apps/worker/src/routes/import.ts) `autoMatchSplits` posts settlement JE after marking split closed; [`counterparties.ts`](apps/worker/src/routes/counterparties.ts) GET /:id now returns `gl_balance_cents` (SUM(debit)−SUM(credit) on cp's GL account); [`gl.ts`](apps/worker/src/routes/gl.ts) gained `POST /api/admin/arap-backfill`. Schema [`apps/worker/src/db/schema.ts`](apps/worker/src/db/schema.ts) gained `counterparties.account_id`. Tests: 15 new in [`glCounterparty.test.ts`](apps/worker/src/__tests__/glCounterparty.test.ts) — splitCreateLines (both directions, validateLines invariant, zero, fractional truncation, magnitude on negative input), splitEventLines (4 quadrants, zero delta, validateLines invariant), NET semantics (full-pay→0, partial leaves correct residue, mixed they_owe + i_owe to same CP nets correctly). Total 216→231. Live D1 state: AR/AP backfill ran `{splits_scanned:4, splits_posted:4, splits_skipped:0, events_scanned:3, events_posted:3, events_skipped:0}`. Trial balance still equal: `{debit_cents:5104234, credit_cents:5104234, delta_cents:0}`. Per-CP balances correct: Luther -110000 (I owe $1,100), Marcus Chen +4750, Priya Shah -8200, Diego Alvarez +3600. Worker deployed: `857e5c8a-d6bc-4eea-adea-6bd71dad8472`. No client code changed (build unchanged at 423.24 kB JS / 41.45 kB CSS). **Files added**: `apps/worker/drizzle/0013_counterparty_accounts.sql`, `apps/worker/src/lib/glCounterparty.ts`, `apps/worker/src/__tests__/glCounterparty.test.ts`. **Files changed**: `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/splits.ts`, `apps/worker/src/routes/import.ts`, `apps/worker/src/routes/counterparties.ts`, `apps/worker/src/routes/gl.ts`. **Caveat**: counterparties LIST view still aggregates from `splits.remaining_cents` — drill-in surfaces `gl_balance_cents` for verification but list-view migration to GL is deferred (would change observable behavior).

- **GL foundation — session 53 of the Accounting Upgrade Program shipped, session 58** (2026-04-27): Migration [`0012_gl_foundation.sql`](apps/worker/drizzle/0012_gl_foundation.sql) applied to remote D1 (31 tables now). Three new tables: `gl_accounts` (chart of accounts — id, path UNIQUE, name, type CHECK asset/liability/equity/income/expense, parent_id self-FK, archived_at, metadata_json, created_at), `journal_entries` (id, posted_at, source_type, source_id, memo, created_at, locked_at), `journal_lines` (FK to entry + gl_account, debit_cents/credit_cents — CHECK single-sided non-negative). 25 chart-of-account rows seeded with stable IDs (Assets:Cash/Investments/Receivable; Liabilities:CreditCards/Loans/Payable; Equity:Opening Balance/Retained Earnings/Unrealized Gains-Losses/Realized Gains-Losses; Income:Salary/Reimbursement/Investment/Transfer; Expenses:Essentials/Lifestyle/Indulgence/Debt Service/Savings/Uncategorized). Note: GL table is `gl_accounts` not `accounts` — the existing `accounts` table is bank accounts and stays unchanged. **New lib files**: [`apps/worker/src/lib/gl.ts`](apps/worker/src/lib/gl.ts) (`validateLines()` pure — rejects unbalanced/double-sided/zero/negative/<2 lines/empty account_id; error codes in message for greppability; `postJournalEntry(env, lines, meta)` strict D1 batch; `computeTrialBalance()` pure per-account dr/cr/balance + totals delta); [`apps/worker/src/lib/glBackfill.ts`](apps/worker/src/lib/glBackfill.ts) (`bankAccountToGlPath()` pure — chequing/savings→Assets:Cash, credit→Liabilities:CreditCards, loan→Liabilities:Loans; `categoryGroupToAccountId()` pure — maps category group to stable chart ID, null→Equity:Opening Balance; `buildTransactionLines()` pure — sign rule: cashDebits=inflow regardless of cashType because DR means money-in whether asset increases or liability decreases; `runGlBackfill(env)` idempotent — skips existing source_type='transaction'/source_id=txn.id). **Routes** at [`apps/worker/src/routes/gl.ts`](apps/worker/src/routes/gl.ts): `GET /api/gl/accounts` (chart list), `GET /api/gl/trial-balance?as_of=` (per-account dr/cr/balance + totals), `GET /api/gl/journal?account_id=&limit=` (entries + lines with account_path for audit), `POST /api/admin/gl-backfill` (idempotent one-shot, auth-gated). **Live D1 state**: backfill ran, 173 txns → 173 JEs, trial balance `{debit_cents:4897684, credit_cents:4897684, delta_cents:0}`. Books clean. TD Chequing $17,647 DR, Income:Salary $29,750 CR, TD Visa $4,824 CR liability, Income:Transfer nets to $0 (paired transfers worked). Re-run idempotent. **Tests**: 23 new in [`apps/worker/src/__tests__/gl.test.ts`](apps/worker/src/__tests__/gl.test.ts). Total 193 → 216. **Files added**: `apps/worker/drizzle/0012_gl_foundation.sql`, `apps/worker/src/lib/gl.ts`, `apps/worker/src/lib/glBackfill.ts`, `apps/worker/src/routes/gl.ts`, `apps/worker/src/__tests__/gl.test.ts`. **Files changed**: `apps/worker/src/db/schema.ts` (3 new table exports), `apps/worker/src/index.ts` (glRoutes + glAdminRoutes). **Worker deployed**: `b8979ccc-0b2a-4fb9-b377-b1dd69750671`. No UI changes. Substrate only.

- **E-transfer payment links — session 52 of the Accounting Upgrade Program shipped, session 57** (2026-04-27): Migration [`0011_payment_links.sql`](apps/worker/drizzle/0011_payment_links.sql) applied to remote D1 (28 tables, 31 after session 58): `payment_links` (id, split_id FK, token unique, email, security_answer nullable, created_at, viewed_at, expires_at, disabled_at). New worker file [`apps/worker/src/routes/paymentLinks.ts`](apps/worker/src/routes/paymentLinks.ts) exports route + pure helpers `generateToken` (12-char from a 32-char unambiguous alphabet — no 0/o/1/l), `expiryFrom` (90d default), `linkStatus` (active/disabled/expired/settled, settled wins over disabled), `parseCreateLinkInput`, `loadPublicLink`, `markViewed`, `escapeHtml`, `renderPublicLinkHtml`. **Routes**: `POST /api/payment-links` body `{split_id, security_answer?}` validates the split is open and `they_owe`-direction (collector model — payment links only go on chits where someone owes you, not the other way around), pulls `interac_email` from settings (returns 400 with a setup nudge if unset), falls back to `interac_security_answer_default` if no per-call answer, returns `{id, token, url, expires_at}`. `POST /api/payment-links/:id/disable` for manual kill. `GET /api/payment-links?split_id=` for listing. **Public unauthed page**: `app.get('/pay/:token')` mounted in [`apps/worker/src/index.ts`](apps/worker/src/index.ts) BEFORE the `app.use('/api/*', auth)` middleware, returns standalone HTML matching the locked design (cream `#f2ece0` receipt on dark `#1a141d` bg, Fraunces+Geist Mono CDN fonts, 64px mascot with onerror fallback, hero amount in Fraunces 44px tabular, three perforated rows of email/amount/security-answer with `data-copy` stamp buttons that flip to gold on copy, "** sent via Declyne **" footer with mascot-purple D, 90d expiry disclaimer below). Status banner shows `** this tab is settled **` / `** this link has expired **` / `** this link is no longer active **`. Increments `viewed_at` on first hit when active. Clean 404 page for unknown tokens. **Auto-disable wired in 2 spots**: `autoMatchSplits` in [`apps/worker/src/routes/import.ts`](apps/worker/src/routes/import.ts) calls `disableLinksForSplit(env, split.id, 'split_auto_matched')` after settling; [`splits.ts`](apps/worker/src/routes/splits.ts) POST `/:id/event` calls `disableLinksForSplit(env, id, 'split_settled')` when remaining drops to 0. Both swallow errors so a missing-link case doesn't break settlement. **Counterparty UI**: [`Counterparty.tsx`](apps/client/src/pages/Counterparty.tsx) gained `Send link` purple stamp on every open `they_owe` chit (next to the existing gold `Paid` stamp). Tap calls `POST /api/payment-links` then `@capacitor/share` on native or `navigator.clipboard.writeText` on web; routes the `interac_email`-missing case to "Set your Interac email in Settings first." **Settings UI**: new "02 Interac e-transfer" ledger section in [`Settings.tsx`](apps/client/src/pages/Settings.tsx) with email + optional default security answer fields and a Save stamp; existing sections renumbered 03-09. **Spec correction landed**: session 55's flagged inversion is fixed — the stamp lives on `they_owe` chits only and the worker rejects `i_owe` server-side. **No-domain constraint honored**: the public landing page is HTML-from-worker at `https://declyne-api.bocas-joshua.workers.dev/pay/<token>` — no separate Pages deploy, no DNS, no client SPA route. Mockup `apps/client/src/pages/PaymentLinkMockup.tsx` deleted, route removed from `App.tsx`. **Tests**: 21 new tests in `paymentLinks.test.ts` covering token uniqueness over 500 calls, alphabet, expiry math, linkStatus transitions including settled-wins-over-disabled precedence, `parseCreateLinkInput` validation, `escapeHtml`, and `renderPublicLinkHtml` (active renders all 3 copy stamps + amount; settled hides them + shows banner; expired shows expired banner; XSS payload in `split_reason` + `counterparty_name` properly escaped). Test count 172 → 193 (+21). **Debug pass green**: typecheck clean (one fix for `exactOptionalPropertyTypes`), build clean (423.24 kB JS / 41.45 kB CSS, +0.04 kB JS for the Counterparty stamp wiring; no CSS changes). **Worker deployed**: `00bb2e52-7dd8-459c-894e-f42c6f10c0e8`. Smoke test on `/pay/doesnotexist` returned 404 (not 401), confirming unauthed reachability. **Files added**: [`apps/worker/drizzle/0011_payment_links.sql`](apps/worker/drizzle/0011_payment_links.sql), [`apps/worker/src/routes/paymentLinks.ts`](apps/worker/src/routes/paymentLinks.ts), `apps/worker/src/__tests__/paymentLinks.test.ts`. **Files changed**: [`apps/worker/src/db/schema.ts`](apps/worker/src/db/schema.ts), [`apps/worker/src/routes/editLog.ts`](apps/worker/src/routes/editLog.ts), [`apps/worker/src/index.ts`](apps/worker/src/index.ts), [`apps/worker/src/routes/import.ts`](apps/worker/src/routes/import.ts), [`apps/worker/src/routes/splits.ts`](apps/worker/src/routes/splits.ts), [`apps/client/src/pages/Counterparty.tsx`](apps/client/src/pages/Counterparty.tsx), [`apps/client/src/pages/Settings.tsx`](apps/client/src/pages/Settings.tsx), [`apps/client/src/App.tsx`](apps/client/src/App.tsx). **Files removed**: `apps/client/src/pages/PaymentLinkMockup.tsx`. **Caveat**: the public page references `mascot-head.png` from the workers.dev origin which doesn't serve static assets — `onerror` hides it cleanly, recipient just sees the typography. Future cleanup could inline an SVG mascot.

- **Today header dedup + chit-form UX polish + seed paycheque tuning + no-domain constraint locked, session 55** (2026-04-27): Receipt header had `{daysLeft}D TO PAYDAY` while the queue and a hero state ALSO surfaced payday — three spots of duplication. Replaced the header segment with `WK NN` (ISO week, computed in a new `isoWeek(d)` helper in [`apps/client/src/pages/Today.tsx`](apps/client/src/pages/Today.tsx)). Edition plate now reads `MON, APR 27 · RCPT 0081 · WK 18` — no echo with the hero cycle. **Seed paycheque tuning**: user wanted Today's Tank to show "around $600". Current period had paycheque $2,400 vs $4,043 spend — Tank was clamped to $0. Bumped all 7 seed paycheques 240000 → 425000 cents (more realistic Toronto net bi-weekly, ~$110K gross) in [`apps/worker/drizzle/seed_test.sql`](apps/worker/drizzle/seed_test.sql) and ran a live D1 UPDATE on the matching `transactions.amount_cents` + `pay_periods.paycheque_cents` rows (changes:7). After: Tank shows $506.21, queue shows Payday +$4,250.00. (`transfer` group is excluded from Tank spend so the $4,650 first attempt overshot — settled at $4,250.) **Chit-form UX overhaul**: (1) The chit form was rendering at the top of OPEN TABS regardless of which counterparty was long-pressed — moved the conditional render *inside* `openCps.map()` so the chit appears directly under the clicked row; the from-scratch case (`prefilledFor === null`) now renders separately below the list. (2) The `.chit::before` saw-tooth in `--color-paper` was invisible on cream-on-cream. Three iterations rejected (dark `radial-gradient` dots — "i dislike the black"; weak shadow — needed more depth; stippled torn-fibers band — "oh god no theyre terrible"). **Final fix**: switched `.chit` background from `--color-paper` to `--color-paper-shade` (#e6dfd0 vs receipt #f2ece0). The `clip-path` saw-tooth cuts now reveal the receipt's lighter cream through the gaps — clean two-tone torn edge with no painted-on textures. Drop-shadow stack at 0.95/0.7/0.55 alpha gives real depth. (3) New `.stamp-filled` class in [`apps/client/src/index.css`](apps/client/src/index.css) — solid purple bg + paper-color text for active state. Wired into the OWES YOU / YOU OWE toggle in `ChitForm` (replaced `stamp-purple` which was outline-only and got further overridden inside `.receipt`, so both buttons looked identical with no selection state). **No-domain constraint LOCKED**: user stated zero spend on this project, no domain ever. Saved as `feedback_no_custom_domain.md` in memory + indexed. **All references in this CLAUDE.md to `declyne.app` / `pay.declyne.app` are now obsolete** — when session 52 ships, links go out as `https://declyne-api.bocas-joshua.workers.dev/pay/<token>` wrapped in an iOS Share-sheet message. **Session 52 spec correction needed**: the locked design says "Send payment link" stamp goes on `i_owe` chits — that's inverted. A payment link is what *the collector* sends, so it belongs on `they_owe` chits (where someone owes you and you want them to pay you via Interac). Recipient-page Fraunces label "from Bowgull" reframes as "Bowgull is collecting from you." Fix when 52 actually ships. **Debug pass green**: 172 tests pass (no test changes — UI/CSS only), typecheck clean, build clean (409.92 kB JS / 32.32 kB CSS, +0.04 kB JS / +0.28 kB CSS). No worker code changed; no redeploy. **Files changed**: [`apps/client/src/pages/Today.tsx`](apps/client/src/pages/Today.tsx), [`apps/client/src/index.css`](apps/client/src/index.css), [`apps/worker/drizzle/seed_test.sql`](apps/worker/drizzle/seed_test.sql).

- **Today color tightening + critical pay-period query fix + Luther rename, session 54** (2026-04-27): Today screen hero color thresholds tightened — `streakColor()` now goes gold at `n >= 2` (was 4) and `paydayColor()` re-graded to ink >14d / gold ≤14d / mascot-purple ≤7d (was ≤7d gold / ≤2d purple — too conservative; the hero was flat-ink for 75% of every pay period). Header caption "NND TO PAYDAY" now wrapped in a `<span style={{ color: paydayColor(daysLeft) }}>` so it color-shifts the same way against the muted caption text. Hero number stays at `.hero-num` 44px (user explicitly rejected size bump). **Critical pay-period bug fixed**: discovered while user was questioning why a tank showed "24D TO PAYDAY" (impossible for Ontario bi-weekly/15th-30th schedules). Root cause: seed data has paycheque transactions through `2026-05-08` (future relative to today=Apr 27), so `detectPeriods` correctly built a future period May 8 → May 21. **All 8 "current period" queries used `ORDER BY start_date DESC LIMIT 1`** which returned that future period instead of the actual Apr 24 → May 7 period. Fixed by adding `WHERE start_date <= date('now')` to: [`apps/worker/src/routes/today.ts:71`](apps/worker/src/routes/today.ts), [`apps/worker/src/routes/budget.ts:9`](apps/worker/src/routes/budget.ts) + line 75, [`apps/worker/src/routes/periods.ts:34`](apps/worker/src/routes/periods.ts) (`/current` route), [`apps/worker/src/routes/allocations.ts`](apps/worker/src/routes/allocations.ts) lines 85, 117, 268, 282 (POST/draft/auto-match + the no-FROM variant). Real production bug, not just a seed issue — any user importing a pending payroll deposit (some banks show pending) would have seen it. Verified post-deploy: header shows "10D TO PAYDAY", queue shows Payday at 11d, tank/budget all reading the correct current period. **Luther rename**: `Bowgull (Mexico)` debt + `Bowgull` counterparty renamed to `Luther` per user request. Updated `seed_test.sql` (3 references) + ran live D1 UPDATE on `counterparties.name` and `debts.name` (changes:2). Kept the `cp_bowgull` and `debt_bowgull` IDs and split_event note copy "etransfer to Luther {Feb,Mar,Apr}" — only display names changed. **Pattern lesson for future code reviews**: any `ORDER BY date DESC LIMIT 1` for "current X" needs a `WHERE date <= today` guard. Future periods, future statements, future paycheques can leak in. **Debug pass green**: 172 tests pass, typecheck clean, build clean (409.88 kB JS / 32.04 kB CSS, +0.06 kB JS for the color span wiring). Worker deployed manually mid-session: `324b75e3-a61a-4b28-b7f3-d8b5b6e3f322`. CI will redeploy the same code on push (idempotent). **Files changed**: [`apps/client/src/pages/Today.tsx`](apps/client/src/pages/Today.tsx), [`apps/worker/src/routes/today.ts`](apps/worker/src/routes/today.ts), [`apps/worker/src/routes/budget.ts`](apps/worker/src/routes/budget.ts), [`apps/worker/src/routes/periods.ts`](apps/worker/src/routes/periods.ts), [`apps/worker/src/routes/allocations.ts`](apps/worker/src/routes/allocations.ts), [`apps/worker/drizzle/seed_test.sql`](apps/worker/drizzle/seed_test.sql).

- **Today screen — header redesign + body restructure to "one hero, one queue", session 53** (2026-04-27): conceptual reframe locked: Today = the open till, not a dashboard. Receipts record the past; Today is the live ledger before it gets printed. **Header rebuilt as edition plate (variant C)**: thick 3px ink rule on top, three-column row (mascot 64px left · Fraunces 28px "Declyne" wordmark center with purple `D` · settings cog right), 1px hairline below, mono caption `MON, APR 27 · RCPT 0081 · 24D TO PAYDAY` centered. The previous oversized 116px mascot + 44px wordmark was reading "amateur"; the new edition plate is disciplined and rule-anchored. **Wordmark cleanup**: [`apps/client/src/components/DeclyneWordmark.tsx`](apps/client/src/components/DeclyneWordmark.tsx) dropped the `letter-d.png` mask/sprite (felt gimmicky, slightly off-baseline) and now uses a plain Fraunces capital `D` styled with `color: #9e78b9` followed by `eclyne` in current-color. **Body restructure**: dropped the duplicate "24d to payday" sub-label from hero state 0 (header already shows it), collapsed the three separate sections (NEXT block + NEXT RECONCILIATION row + PRINTING AHEAD list) into a single chronological QUEUE that combines reconciliation prompt (only on Sunday when not yet completed), review-queue prompt (only when count > 0), upcoming bills, and payday — all sorted by `days_until` ascending, each row leads with a 44px-wide mono day-count gutter (`now` for actions, `Nd` for upcoming) then label then meta. Removed standalone PHASE / INDULGENCE / REVIEW / STREAK link rows from the bottom of the receipt — they were status-not-action and broke the "open till" framing. Footer copy `** between receipts **` → `* still printing *`. **Worker fix bundled**: [`apps/worker/src/routes/today.ts`](apps/worker/src/routes/today.ts) `HORIZON_DAYS` extended from 14 to 30 because Rogers/Enbridge/Bell/Payday were all 23-31 days out from Apr 27 with the seed data. **Session 51 leftovers committed**: 19 files of session 51 worker + client changes had been sitting uncommitted since the session 50 docs commit; the deployed worker was running old `s.direction = 'owes_josh'` SQL against renamed `i_owe`/`they_owe` data, so every counterparty aggregate was returning 0 (tabs all showed `SETTLED · 1 CHIT $0.00` despite splits having real `remaining_cents`). Bundled everything into commit `196e5b0` "session 51 + session 52: depersonalize schema + Today header redesign", pushed to main, CI auto-deployed. Verified post-deploy: tabs now render Bowgull -$1,100, Priya Shah -$82, Marcus Chen +$47.50, Diego Alvarez +$36; queue renders Rogers $95 24d, Enbridge Gas $108 24d, Payday +$2,400 25d. **Files changed**: [`apps/client/src/pages/Today.tsx`](apps/client/src/pages/Today.tsx) (-103 +72), [`apps/client/src/components/DeclyneWordmark.tsx`](apps/client/src/components/DeclyneWordmark.tsx), [`apps/worker/src/routes/today.ts`](apps/worker/src/routes/today.ts). 172 tests pass unchanged, typecheck clean, build clean (409.82 kB JS / 32.04 kB CSS). **Future Today work should preserve "one hero + one queue"** — adding more sections fights the "open till" frame.

- **Phase 1 of showcase/multi-user groundwork — depersonalize the schema, session 51** (2026-04-26): `splits.direction` enum renamed from `josh_owes`/`owes_josh` to first-person relative `i_owe`/`they_owe` so the schema works for any user. Migration [`0010_rename_split_direction.sql`](apps/worker/drizzle/0010_rename_split_direction.sql) applied to remote D1; rebuild had to do **both** splits AND split_events together because D1 enforces FKs and rejected DROP TABLE splits while child rows existed (and `PRAGMA defer_foreign_keys = ON` failed at the D1 import endpoint). Worker code (schema, splits/counterparties/reconciliation/import routes) and client (Today/Reconciliation/Debts/Counterparty pages) updated; tests + all three seed files updated; one test that used `'they_owe'` as an *invalid* direction switched to literal `'invalid'`. OpenAI system prompt in `apps/worker/src/routes/investment.ts` no longer references "Josh". `apps/client/capacitor.config.ts` `appId` is now `com.bowgull.declyne` (no runtime impact for sideload). 172 tests pass unchanged. **Phases 2–6 designed but not shipped:** P2 add `users` table + nullable `user_id` FK on 22 personal-data tables, P3 `DEMO_TOKEN` env + `demoGuard` middleware (read-only demo mode), P4 legal docs polish for demo mode, P5 Cloudflare Pages deploy, P6 `WHERE user_id = ?` enforcement everywhere.


- **Automated D1 -> R2 backup, session 50** (2026-04-26): closes the session 48 follow-up. New [``.github/workflows/backup-d1.yml``](.github/workflows/backup-d1.yml) — weekly cron Sunday 3am UTC + manual dispatch. Uses two GitHub secrets: `CLOUDFLARE_D1_TOKEN` (new "Declyne D1 Backup" token, D1:Read scope, minted via browser automation) for the `wrangler d1 export declyne --remote` step, and the existing `CLOUDFLARE_API_TOKEN` (Edit Cloudflare Workers, includes R2 write) for the `wrangler r2 object put declyne-backups/...` upload step. Bucket `declyne-backups` is created idempotently on each run. [`docs/runbooks/backup-d1-to-r2.md`](docs/runbooks/backup-d1-to-r2.md) updated to document the automation. No worker code changed.

- **Security/compliance follow-ups, session 49** (2026-04-26): closed three of the five remaining items from the session 43-48 program. **(1) In-app token rotation UI**: new "08 API token" ledger section in [`apps/client/src/pages/Settings.tsx`](apps/client/src/pages/Settings.tsx) with Rotate-token (purple stamp opens a bottom-sheet password input that calls `setToken()` from [`tokenStore.ts`](apps/client/src/lib/tokenStore.ts) → Keychain) and Clear-token (danger stamp calls `clearToken()`). Closes the session 46 follow-up — token rotation no longer needs the `.env.local` cold-restart path documented in [`docs/runbooks/rotate-api-token.md`](docs/runbooks/rotate-api-token.md). **(2) Rate limiting on `/api/invest/recommend`**: `RECOMMEND_LIMIT_PER_HOUR = 10` cap in [`apps/worker/src/routes/investment.ts`](apps/worker/src/routes/investment.ts) — counts rows in `recommendations` where `generated_at >= now-1h` and returns 429 `{error:'rate_limited', limit, window:'hour'}` if over. Uses existing infrastructure (no new table, no Cloudflare dashboard binding config), defending against runaway loops or token-leak abuse on the only money-cost route. **(3) Drop legacy `splits.counterparty` text column**: migration [`apps/worker/drizzle/0009_drop_splits_counterparty.sql`](apps/worker/drizzle/0009_drop_splits_counterparty.sql) (`ALTER TABLE splits DROP COLUMN counterparty`) applied to remote D1 (changes:1, num_tables:27). Schema, all worker routes (splits/export/reconciliation), `Debts.tsx` Split type (renamed `counterparty` → `counterparty_name` to match the GET /api/splits join), POST body in SplitSheet (now sends `counterparty_name` matching what `parseSplitInput` expects — also fixes a pre-existing bug where the legacy field name was being rejected), `packages/shared/src/types.ts`, and all three seed files cleaned. Test count unchanged at 172. Worker auto-deploys via CI.

- **Security/compliance uplift, sessions 43-48** (2026-04-26): Six-session program closing the gaps flagged in the session 43 doc-reconciliation audit. **Session 43 (hotfixed)**: critical SQL-injection fix in `apps/worker/src/routes/debts.ts` PATCH (allowlist-based `parseDebtPatch` + `ALLOWED_PATCH_FIELDS`), defense-in-depth tightening of `apps/worker/src/routes/allocations.ts` PATCH (`ALLOC_PATCH_FIELDS`), Drizzle CVE bump (`drizzle-orm` 0.38 -> 0.45, `drizzle-kit` 0.30 -> 0.31), 11 new SQLi defense tests covering classic SQLi shape, pivot-to-other-tables, and `__proto__` prototype pollution. **Session 44**: GitHub Actions security workflow (`.github/workflows/security.yml`) running `pnpm audit` (high+ blocks), `gitleaks` (secret scan), `semgrep` (`p/owasp-top-ten` + `p/typescript` + `p/javascript`), and `trivy` (CVE + filesystem scan) on every PR + weekly cron. New `.github/workflows/checks.yml` running typecheck + tests + client build on every PR (so regressions are caught before merge instead of failing during deploy). New `.github/dependabot.yml` (weekly npm + github-actions, grouped by minor+patch). **Session 45**: transport hardening. New [`apps/worker/src/middleware/security.ts`](apps/worker/src/middleware/security.ts) sets HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy (denies sensors/camera/mic/payment/USB), CSP `default-src 'none'; frame-ancestors 'none'` (API-only), Cross-Origin-Resource-Policy `same-site`. CORS locked from `*` to a fixed allowlist (`isAllowedOrigin()` checks `capacitor://localhost`, `ionic://localhost`, `http://localhost:5173-5175`). `app.onError` returns `{ error: 'internal' }` instead of `err.message` (no more DB schema fragments in error responses). New unauthenticated `/healthz` route for uptime checks. 7 new tests. Rate limiting deferred to a smaller follow-up (needs Cloudflare dashboard binding config). **Session 46**: iOS Keychain migration. New [`apps/client/src/lib/tokenStore.ts`](apps/client/src/lib/tokenStore.ts) (`getToken`/`setToken`/`clearToken`) backed by `capacitor-secure-storage-plugin` (hardware-backed Keychain on iOS, localStorage fallback on web). `apps/client/src/lib/api.ts` rewritten — `req()` awaits `getToken()` per call instead of reading a top-level `VITE_API_TOKEN` constant. `VITE_API_TOKEN` env is now a one-shot migration source only (read once on first launch, written into Keychain, then ignored). `apps/client/.env.example` updated to document the new flow. **Session 47**: compliance artifacts + data rights. New `SECURITY.md` mapping implemented controls to OWASP ASVS 5.0 Level 2 categories with `file:line` citations. New `PRIVACY.md` (PIPEDA-aligned data handling notice). New `THREATS.md` (STRIDE-style threat model). New `DELETE /api/data/purge` route gated by typed-confirmation body (`{"confirm": "DELETE EVERYTHING"}`); iterates a fixed allowlist of 26 tables, writes `edit_log` row `user_data_purge`. Pure helper `isValidPurgeBody` exported with 5 new tests. New `docs/runbooks/rotate-api-token.md`. **Session 48**: log redaction + LLM error sanitize + portfolio README. New [`apps/worker/src/lib/logRedact.ts`](apps/worker/src/lib/logRedact.ts) (`redactSensitive()`) strips Bearer tokens, emails, $-prefix amounts, `N CAD/USD` amounts, and 6+ digit account numbers from any string before it hits Cloudflare logs. Applied to `app.onError` and the OpenAI upstream-error path in `apps/worker/src/routes/investment.ts` (was leaking OpenAI rate-limit error bodies). New `docs/runbooks/backup-d1-to-r2.md` (manual D1 -> R2 export procedure; automated CI version flagged as follow-up — needs separate Cloudflare token with D1-read scope). README rewrite: CI badges (deploy/security/checks), "Security and privacy" section linking SECURITY.md / PRIVACY.md / THREATS.md / runbooks. **Test count: 140 -> 172.** Worker auto-deploys via CI on every push to main; the production worker is now SQLi-patched + CSP/CORS-locked + sanitized + Keychain-token-backed.

- Reconciliation TABS TO MATCH section (session 43): closes the loop on import-time splits payment matching shipped in session 42. New worker route `GET /api/reconciliation/tabs-to-match` returns open splits (`closed_at IS NULL AND settlement_txn_id IS NULL AND remaining_cents > 0`) that have ≥2 candidate transactions in the amount + ±3d window — exactly the ambiguous cases `autoMatchSplits` skips. New `POST /api/reconciliation/tabs-to-match/:split_id/match` accepts `{ transaction_id }`, validates the txn matches the split's expected signed amount + ±3d window via the same shared rule (`isCandidateValid`), then settles: sets `settlement_txn_id`, `remaining_cents=0`, `closed_at`, inserts a `split_event` with note "matched in reconciliation" and `delta_cents = -remaining_cents`, writes edit_log `entity_type='split'`, `field='settlement_txn_id'`, `actor='user'`, `reason='split_user_matched'`. Pure helpers `expectedSignedAmount`, `withinThreeDays`, `findAmbiguousSplits`, `isCandidateValid` exported from `apps/worker/src/routes/reconciliation.ts` for tests. **Reconciliation.tsx** gained a new `TABS TO MATCH (n)` perforated section between Summary and Line items — only renders when `tabs.length > 0`. Each tab shows counterparty + direction-tinted dot (sage if owes-you, sienna if you-owe) + reason + amount, then a list of candidate transaction `row-tap` buttons (description + posted_at + account, signed amount on right). Tap a candidate → POST match → invalidates `reconciliation-tabs-to-match`, `splits`, `counterparties` queries. Copy: "Multiple transactions match these open tabs. Pick the one that settled it." 15 new tests in `reconciliation.test.ts` (155 total: 141 worker + 14 shared, was 140). Worker code changed; redeploy from local: `pnpm --filter @declyne/worker run deploy`. Build: 404.15 kB JS / 31.81 kB CSS (+2.62 kB JS / +1.21 kB CSS). **Files changed:** `apps/worker/src/routes/reconciliation.ts`, `apps/worker/src/__tests__/reconciliation.test.ts`, `apps/client/src/pages/Reconciliation.tsx`. **Caveat:** the route fetches candidate txns per-split (one query each); fine for the typical case of <10 open splits, would want a single batched query if Josh ever ends up with hundreds of open tabs.

- pnpm monorepo: `apps/client`, `apps/worker`, `packages/shared`
- 27-table D1 schema, live and seeded
- All worker routes: accounts, import, phase, budget, debts, splits, investment, review, settings, export, categories, routing, periods, signals
- Four-tab client: Today / Budget / Debts / Grow + Settings cog
- Brand foundation (session 25): mascot wired in. App icon + favicon (`apps/client/public/brand/{icon-1024,icon-512,apple-touch-icon,favicon-32,favicon-16,mascot-head,mascot-full}.png`) cropped from `assets/Mascot Full image.PNG` via PIL — head+wings square padded with sampled bg color. Linked from `index.html`. Display font Fraunces (serif, ceremonial/literary), body Geist, mono Geist Mono, all via Google Fonts CDN. CSS vars `--font-display`, `--font-mono`, `--color-paper`. New utility classes `.display` (Fraunces 600) + `.mascot-mark` (28px inline icon). Today h1 now uses `.display` + `.mascot-mark` in header. Grain upgraded from radial-dot to inline SVG fractalNoise with warm-tinted color matrix + mix-blend-mode overlay; added edge vignette via `.grain::after`
- Brand system: Tailwind v4 CSS vars, SVG-noise grain + edge vignette, receipt motif with stub perforations (.stub-top/.stub-bottom), .field/.field-label form primitives
- Devil-voice notifications (session 25): `apps/client/src/native/notifications.ts` — Sunday id 1 "I kept the receipts." / "All week. Every one. Sit down with me."; Tuesday id 2 "Still pretending Sunday didn't happen?" / "I'll wait. The numbers won't."
- CSV pipeline: Web Worker, format autodetect (TD Chequing/Visa, Capital One), merchant normalization, real account picker sheet (no more window.prompt)
- Accounts UI at /settings/accounts: list + add/edit bottom sheet + archive/restore, receipt-card rendering
- Debts UI: tap-to-edit receipt cards, add/edit bottom sheet (principal, APR, min payment fixed/percent, statement day, due day, optional linked account), archive
- Splits UI in Debts tab: add sheet (counterparty, direction, amount, reason), tap-to-settle sheet (partial or full payment, optional note, posts split_events)
- Review UI at /review: list items with category dropdown resolve; Today card links here
- Routing UI at /budget/routing: shows latest pay period plan, regenerate button (avalanche order: min payments by APR descending, any remainder to highest-APR debt), mark-executed per row
- 2 local notifications: Sunday 9am reconciliation, Tuesday 9am follow-up (anti-scope: no third notification). Wired via `apps/client/src/native/bootstrap.ts` from `main.tsx` on every native boot
- Sunday reconciliation flow: new worker route `apps/worker/src/routes/reconciliation.ts` — `GET /api/reconciliation/status` returns `{last_reconciliation_at, reconciliation_streak, completed_this_week, week_starts_on}`; `POST /api/reconciliation/complete` is idempotent within the Sun→Sat week (Sunday counts as itself), increments `settings.reconciliation_streak`, sets `settings.last_reconciliation_at` to ISO now, writes 2 edit_log rows under `reconciliation` entity_type. Pure helpers `mostRecentSunday(today)` + `isCompletedThisWeek(lastAt, today)` exported for tests. `reconciliation` added to edit-log allowlist. Today tab gained Reconciliation card (streak count + last date + Mark-done button); on success, client calls `dismissFollowUpThisWeek()` which cancels + reschedules the Tuesday Capacitor LocalNotification
- Phase engine + behaviour signals (deterministic, shared package)
- Pay period detection: paycheque-anchored, substring-match + min-cents threshold, auto-runs after CSV import, routes at `/api/periods` (GET, GET /current, POST /detect). Config via settings keys: `paycheque_source_account_id`, `paycheque_pattern`, `paycheque_min_cents`, `paycheque_fallback_days`
- Behaviour signals compute at `/api/signals/compute`: writes `behaviour_snapshots` row with all 8 signals, deterministic SQL inputs, no GPT math
- Nightly cron trigger at `0 8 * * *` UTC runs `computeAndStoreSignals` via `scheduled()` handler in `apps/worker/src/index.ts`. Registered at deploy: `schedule: 0 8 * * *`
- Paycheque detection settings UI on `/settings`: source account dropdown (filters to chequing + unarchived), pattern, min cents, fallback days, Save + Detect-now buttons
- Cron observability: `cron_runs` table logs every scheduled run (started_at, finished_at, status, detail). Scheduled handler wraps `computeAndStoreSignals` in `logCronRun`. Route `GET /api/cron/runs?limit=30` returns recent rows
- ~~AI coach narration~~ **Removed in session 25.** Worker route, helper, tests, Today UI, queries all deleted. `coach_messages` table dropped from remote D1 in session 26 (migration `apps/worker/drizzle/0003_drop_coach_messages.sql`) and removed from `apps/worker/src/db/schema.ts`. `OPENAI_API_KEY` secret left set but unused by client-facing code. The investment-recommendation GPT call in `apps/worker/src/routes/investment.ts` (Grow tab, locked behind Phase 4) is unrelated and intact
- Migration `apps/worker/drizzle/0001_cron_coach.sql` applied to remote D1
- Wrangler 4.85.0 at root and worker
- Settings Cron runs card: pulls `/api/cron/runs?limit=5`, renders started_at, job, detail, status per row. Confirms nightly firing without tailing Worker logs
- Market data fetch: `POST /api/market/fetch` fetches Twelve Data time_series for all holding symbols (FMP fallback), BoC overnight rate (Valet API), USD/CAD + XIU.TO + SPY spot prices. Stores in `prices` + `market_snapshots`, recomputes signals. `GET /api/market/snapshot` returns latest row
- Investment holdings enriched: `GET /api/investment/holdings` now joins with latest price from `prices` and latest row from `signals` (sma50, sma200, rsi14, momentum_30d)
- Grow tab: market snapshot card (BoC rate, USD/CAD, XIU, SPY), holdings with market value, gain/loss, RSI + momentum per holding, total portfolio value, Refresh prices button, Get recommendation button (calls `/api/investment/recommend` with current phase). Stays locked at Phase < 4
- Secrets set in Cloudflare: `TWELVE_DATA_KEY`, `FMP_KEY` (set 2026-04-24)
- Phase transitions (auto): `apps/worker/src/lib/phase.ts` gathers inputs from settings, behaviour_snapshots, debts, and accounts, runs shared `evaluatePhase`, and writes `phase_log` + `edit_log` + updates `settings.current_phase` on change. On first promotion to phase 2 it snapshots the then-current non-mortgage debt into `phase2_entry_non_mortgage_debt_cents`. Routes: `POST /api/phase/recompute`, `GET /api/phase/inputs`. Nightly cron now runs signals.compute then phase.recompute, both logged in `cron_runs`
- Vice dashboard in Budget tab: expanded Vice card shows 30d ratio, week-over-week delta, 8-week trend bars, peak weekday (over 90d), and top 5 vice categories (30d). New `GET /api/budget/vice/trend` backs it
- Phase streak computation: `apps/worker/src/lib/streaks.ts` walks pay_periods + transactions to count trailing periods where chequing income ≥ essentials spend (`essentials_covered_streak_periods`), reads `credit_snapshots` newest-first for `utilization_under_30_streak_statements` (threshold 3000 bps) and `on_time_streak_days`, and derives a fallback `essentials_monthly_cents_derived` from rolling 90d essentials / 3 when the manual setting is absent. Writes results to settings, logs changes to `edit_log`. `phase.ts` now falls back to the derived value if manual unset. Route: `POST /api/phase/streaks/recompute`. Nightly cron runs `streaks.recompute` between signals and phase, wrapped in `logCronRun`
- CC payoff + missed-payment streaks (auto): `streaks.ts` extended with `ccPayoffStreak` (per pay_period, compares `debt_payments` to CC-linked debts vs CC-account spend; period counts when paid > 0 AND paid >= spent) and `findLastMissedMinPayment` (walks last 6 cycles per non-archived debt, sums payments in a 35-day pre-due window against `requiredMinPaymentCents` which honors fixed vs percent-of-principal with a $10 floor). Persists `cc_payoff_streak` and `last_missed_min_payment_date` to settings, deletes the miss key when no miss found, logs changes to `edit_log`. Wired into same nightly cron (signals → streaks → phase). `POST /api/phase/streaks/recompute` now returns both new fields plus `cc_debts_considered`
- Test data seed: `apps/worker/drizzle/seed_test.sql` populates remote D1 with 4 accounts (TD Chq/Sav/Visa, Capital One), 90 days of paycheque + bill + vice + CC payment transactions, 3 debts (TD Visa percent-min, Capital One fixed-min, Bowgull Mexico split), 3 credit snapshots, 2 holdings with seeded prices, market snapshot, goal, one uncategorized review row. Verified end-to-end: periods/detect → 7 periods, signals/compute → vice_ratio_bps=6584, streaks/recompute → essentials_streak=2 cc_payoff_streak=1 last_miss=2026-03-21, phase/recompute auto-promoted 1→2 (`essentials_2p_and_cc_streak_1p`)
- Edit log viewer: new `GET /api/edit-log?entity_type=&entity_id=&limit=` route (`apps/worker/src/routes/editLog.ts`) with entity type allowlist + `GET /api/edit-log/entity-types`. Client page at `/settings/edit-log` with entity-type filter dropdown and last-100 entries; linked from Settings "Audit" card. `merchant` added to the entity-type allowlist in session 16
- Merchant review UI: new `apps/worker/src/routes/merchants.ts` with `GET /api/merchants?status=unverified|all|verified&q=&limit=` (joins txn count, last_seen, uncategorized count, category name) and `PATCH /api/merchants/:id` (updates display_name, category_default_id, verified; `apply_to_uncategorized: true` backfills category on this merchant's null-category txns + resolves matching review_queue rows; all diffs logged to edit_log). Pure helper `parseMerchantPatch` exported for tests. Client page `apps/client/src/pages/Merchants.tsx` at `/settings/merchants` with status filter buttons (unverified/all/verified), search box, tap-to-edit sheet (display name, grouped category picker by group, verified toggle, backfill checkbox that auto-checks when there are uncategorized txns). Linked from Settings "Data" card
- Credit snapshot entry UI: new `apps/worker/src/routes/credit.ts` with `GET /api/credit/snapshots?limit=` (newest first) and `POST /api/credit/snapshots` (validates `as_of` YYYY-MM-DD, `score` 300..900, `utilization_bps` 0..10000, `on_time_streak_days` 0..100000, `source` manual|equifax; writes edit_log; triggers `computeAndStoreStreaks` so utilization + on-time streaks refresh immediately). `DELETE /api/credit/snapshots/:id` also recomputes streaks. Pure helper `parseCreditInput` exported for tests. `credit_snapshot` added to edit-log allowlist. Client page `apps/client/src/pages/Credit.tsx` at `/settings/credit` — lists snapshots (date, score, util %, on-time days, source) with Delete per row and an Add sheet (date picker, score, utilization %, on-time days, source select). Linked from Settings "Data" card. Verified end-to-end in preview: 3 seeded rows render, Add flow rounds 3→4, Delete rounds back to 3
- Goals UI: new `apps/worker/src/routes/goals.ts` with `GET /api/goals?include_archived=1`, `POST /api/goals`, `PATCH /api/goals/:id` (validates name/target_cents/target_date YYYY-MM-DD/linked_account_id/progress_cents/archived; pure helpers `parseGoalInput` + `parseGoalPatch` exported for tests; all create + per-field updates logged to edit_log). Client page `apps/client/src/pages/Goals.tsx` at `/goals` — list with progress bar (% of target, remaining cents), Add sheet (name, target $, progress $, target date, optional linked-account dropdown), tap-to-edit sheet (same fields plus archived toggle), Show/Hide archived button. Linked from Settings "Data" card. Verified end-to-end in preview: seeded buffer goal renders, POST creates Vacation fund (1→2 rows), PATCH archives/unarchives + edits progress
- Onboarding flow: new `apps/client/src/pages/Onboarding.tsx` mounted at `/onboarding` — 5-step skippable flow (Welcome, Add account, Paycheque detection, Essentials baseline, Done) with per-step progress bar, Back/Next/Skip-all controls. Each step writes directly to existing routes (`POST /api/accounts`, `POST /api/settings/:key`); no new worker route. Gate in `App.tsx`: when `settings.onboarding_completed !== '1'`, `/today` and `/` redirect to `/onboarding`; once Finish or Skip-all is clicked, posts `onboarding_completed=1` and routes to `/today`. Settings "Data" card gained "Re-run onboarding" link for re-entry. Verified live in preview on localhost:5174: gate redirected fresh load, all 5 steps rendered with seeded values prefilled, Finish flipped flag and reload stayed on Today, no console errors
- 67 tests passing (53 worker, 14 shared), all packages typecheck clean, client build clean, worker unchanged (no redeploy)
- Phase journey UI: new `apps/client/src/pages/PhaseJourney.tsx` at `/phase` over existing `GET /api/phase/log` route. Three sections: Current (phase number + name + blurb + entered_at + trigger_rule), Path (5-step list, current ringed in accent gold, future steps dimmed), Transitions (per-row phase/name/date/trigger_rule + grouped metrics from metrics_json with vice_ratio + non_mortgage_ratio rendered as %, buffer_months to 2 decimals). Today phase hero card now wraps in `<Link to="/phase">` with "tap for journey" hint. Settings "System" card gains Phase journey link. No new worker code, no edit_log writes, no tests added (pure UI over existing route)
- Seed adjustments to unlock Grow: `apps/worker/drizzle/seed_test.sql` now sets `current_phase=4` + `phase2_entry_non_mortgage_debt_cents=580500`, wipes + repopulates `phase_log` with a 3-step trail (P1→P2 2026-02-01, P2→P3 2026-03-01, P3→P4 2026-04-24) so `loadCurrentPhase` reads 4. Holdings unit storage corrected: XIU.TO 50→500000, SPY 8→80000 (units stored as actual_units × 10_000 per the 4-decimal scaling convention). Grow tab opens at Phase 4, portfolio renders $6,496 (XIU 50u @ $36.80 = $1,840 +$130, SPY 8u @ $582 = $4,656 +$496)
- Holdings entry UI: new `apps/worker/src/routes/holdings.ts` with `GET /api/holdings`, `POST /api/holdings`, `PATCH /api/holdings/:id`, `DELETE /api/holdings/:id`. Validates `symbol` (1..20, uppercased), `account_wrapper` in {tfsa,fhsa,rrsp,nonreg}, `units` 1..1e10 (4-decimal scaled int), `avg_cost_cents` 0..1e11. Pure helpers `parseHoldingInput` + `parseHoldingPatch` exported for tests. PATCH bumps `updated_at`. All create/update/delete diffs written to edit_log; `holding` added to edit-log allowlist in `editLog.ts`. Client page `apps/client/src/pages/Holdings.tsx` at `/holdings` — list (symbol, wrapper, units, avg cost), Add sheet (symbol, wrapper select, units 4-decimal, avg cost $), tap-to-edit sheet with Delete button. Linked from Settings "Data" card. Verified live in preview: 2 seeded rows render, programmatic POST went 2→3, PATCH `changed=1`, DELETE returned ok, list back to 2, no console errors. 75 tests pass (61 worker + 14 shared, +8 new). Worker deployed `3042176e-3793-4a8c-95be-2bf48bc598ff`
- CC statement snapshots: new table `cc_statement_snapshots` (migration `apps/worker/drizzle/0002_cc_statements.sql` applied to remote D1 → 27 tables) with per-cycle rows (debt_id, statement_date, statement_balance_cents, min_payment_cents, due_date, paid_in_full, created_at). New worker route `apps/worker/src/routes/ccStatements.ts` with `GET /api/cc-statements?debt_id=&limit=`, `POST`, `PATCH /:id`, `DELETE /:id`. Pure helpers `parseCcStatementInput` + `parseCcStatementPatch` exported for tests (validates ISO dates, non-negative cents, coerces `paid_in_full` truthy→1). POST validates `debt_id` exists; all mutations write `edit_log` and recompute streaks. `cc_statement_snapshot` added to edit-log allowlist. Streaks upgrade: new pure helper `ccPayoffStreakFromStatements(snapshots, ccDebtIds)` walks per-debt newest-first leading `paid_in_full=1` and aggregates with MIN (any debt with no statements contributes 0, breaking the aggregate streak). `computeAndStoreStreaks` now prefers statement-snapshot streak when any CC debt has ≥1 snapshot row; otherwise falls back to existing pay-period heuristic. Client page `apps/client/src/pages/CcStatements.tsx` at `/settings/cc-statements` — list (card name, statement date, balance, min, due, paid-in-full badge), Add sheet gated to CC-linked debts with card picker + date inputs + balance/min + paid-in-full checkbox, tap-to-edit with Delete. Linked from Settings "Data" card. 11 new tests (86 total: 72 worker + 14 shared). Worker deployed `22ac8e3d-e285-4a15-ae80-6bd3de802879`
- Tab bar iconography (session 26): replaced text-only Today/Budget/Debts/Grow labels with inline stroke SVG icons above each label (calendar, bar chart, card, sprout). No new dependency -- icons are local React components in `apps/client/src/App.tsx`. Settings cog in Today header upgraded from `⚙` glyph to inline cog SVG with `currentColor` stroke. `.tab-icon` utility class added in `apps/client/src/index.css`; tab-item font reduced to 10px with tighter tracking to balance the icon row
- CC statement auto-ingest: new pure helper `apps/worker/src/lib/ccStatementDerive.ts` `deriveCcStatements(debt, txns, existing, today)` walks each statement cycle based on `debt.statement_day` starting at first-txn month, skips cycles whose due_date is in the future, skips already-existing snapshot dates, and emits rows where `statement_balance_cents` = sum of negative-amount charges in cycle (proxy), `min_payment_cents` = `requiredMinPaymentCents({principal_cents: charges, ...})`, `due_date` computed from `debt.due_day` (rolls to next month when due_day <= statement_day), `paid_in_full` = payments_in_window >= charges. New orchestrator `runCcStatementDerivation(env, today?)` in `ccStatements.ts` joins debts with their linked credit accounts, fetches txns + existing snapshots per debt, inserts derived rows with `actor: 'rules'` edit_log entries (`reason: 'cc_statement_derived'`), recomputes streaks once at end. New route `POST /api/cc-statements/derive` exposes it, and `import.ts` now runs derivation after `autoDetectPeriods`, surfacing `cc_statements_inserted` in the import response. Client `CcStatements.tsx` gained a "Derive from txns" button beside "Add statement" with a transient "{n} new statements derived from {k} cards." hint. Verified live in preview at localhost:5174/settings/cc-statements: button derived 4 statements across 2 cards (Capital One Feb/Mar, TD Visa Feb/Mar) with correct percent + fixed min payments ($12.97 on $432.24, $50 fixed on Capital One), no console errors. 7 new tests (95 total: 81 worker + 14 shared). Worker deployed `df0566cb-5296-4cd3-91e8-7bc69f1323b3`
- Budget redesign — the Tank (session 31): Budget tab rebuilt around a full-screen vertical fuel-tank visual after a long brand/UX conversation that explicitly rejected receipt-motif for Budget (records vs in-flight planning). New worker routes in `apps/worker/src/routes/budget.ts`: `GET /api/budget/tank` returns current pay period + paycheque_cents + by_group spend (essentials/lifestyle/vice/debt/transfer/uncategorized) + total_spent_cents + remaining_cents + days_remaining; `GET /api/budget/tank/history?limit=N` (max 8, default 4) returns N most recent CLOSED periods (drops the current one) with vice_cents + other_cents (transfer excluded) for the silhouette mini-tanks. New `apps/client/src/pages/Budget.tsx` (full rewrite) — header is mono "THE PLAN" kicker + Fraunces date range; tank section is `60vh` with `.tank` container holding three stacked bands inside `.tank-fill` (justify-content:flex-end): paper-colored remaining at bottom, red diagonal-stripe vice band above it, purple gradient "other" band at top. Heights normalized via `denom = max(paycheque, otherSpend+vice+remaining, 1)`; transitions 600ms ease so the tank animates as data refreshes. Tank chrome: `.tank-grid` (25/50/75% horizontal hairlines, opacity 0.5), 6 edge ticks, top/bottom center decorative ticks via `::before/::after`. Pointer-events:none overlay carries top mono row "IN $X / Nd LEFT", bottom dark Fraunces 5xl number for `formatCents(remaining)` (color #1a141d for contrast against paper fill) labeled "LEFT TO SPEND", and bottom mono row "VICE $X / SPENT $Y" (vice red when > 0). Below tank: "Where it goes" section (renamed from Routing) with kicker showing planTotal/paycheque + arrow link to `/budget/routing`, card lists first 5 routing_plan rows. Below that: 4-column grid of `.mini-tank` (96px tall) showing the period silhouettes — vice stripe band visibly grows/shrinks across periods (the "holy shit" visual). Below that: goals strip (max 3 active) with name + progress/target + 1px hairline progress bar in `--color-accent-purple-soft`. **Routing rename:** `apps/client/src/pages/Routing.tsx` UI strings updated to "The Plan" / "Rebuild the plan" / "Mark sent" / "sent" / "Planned"; file name + route path `/budget/routing` + DB table `routing_plan` + edit_log entity_type left unchanged. **CSS additions** in `apps/client/src/index.css` (~110 lines after `.field-label`, before `.stub`): `.tank` + ticks + grid, `.tank-fill`, `.tank-band-fill/-vice/-other`, `.tank-overlay` + `.tank-overlay-top/-bottom`, `.tank-number` + `.tank-number-label`, `.mini-tank` + `.mini-tank-fill`. No new tests (pure SQL aggregation routes; existing budget routes have no test coverage). 99 tests pass (85 worker + 14 shared, unchanged), typecheck clean, build clean (382.66kB JS, 14.91kB CSS, +0.67kB / +0.42kB vs session 30). Worker deployed `f9a2a12c-b45e-43f6-a9e1-de45b73da140`.
- Brand cohesion pass — vice→indulgence rename + 6-category color system + Budget tank rebuild (session 34): the brand decision is locked — vice was shame-coded, weed is legal here, indulgence is observed not judged. **Schema rename end-to-end** via `apps/worker/drizzle/0004_rename_vice_to_indulgence.sql` applied to remote D1: rebuilt categories table with new CHECK constraint adding `'indulgence'` (kept `'savings'` reserved for future), translated rows `vice→indulgence`, then ALTER TABLE behaviour_snapshots RENAME COLUMN twice (`vice_ratio_bps`→`indulgence_ratio_bps`, `vice_peak_day`→`indulgence_peak_day`). Schema, signals, phase engine, behaviour helpers, all worker routes, all tests, all client pages, both seed files, all CSS classes — every reference renamed (no aliases). **Route paths renamed** `/api/budget/vice` → `/api/budget/indulgence` and `/api/budget/vice/trend` → `/api/budget/indulgence/trend`. **6-category color system** in `apps/client/src/index.css`: tokens `--cat-income` (mascot purple — Josh insisted income owns the brand inflow color), `--cat-indulgence` `#c97a4a` (burnt sienna, watched outflow, no shame), `--cat-essentials` `#7a8595` (slate-blue, foundational), `--cat-lifestyle` `#b88e7a` (warm clay, human wants), `--cat-savings` `#94a888` (sage, kept/growing), `--cat-debt` `#c8a96a` (gold, paying down is a win), `--cat-uncategorized` rgba dashed-only. Each gets an auto-generated `*-soft` color-mix variant. Helper classes `.cat-dot.{group}` (6px round dots) and `.cat-rule.{group}` (3px vertical color rule for ledger row prefixes). **Tank bands rewritten**: removed `.tank-band-other` (purple) + `.tank-band-vice` (red diagonal stripes) — both gone. Added `.tank-band-essentials/-lifestyle/-debt/-savings/-indulgence/-uncategorized` (linear gradients in their hue, indulgence saturated highest, uncategorized as 135°-stripe of rgba white). `.tank-band-fill` (paper, remaining) preserved. **`apps/client/src/pages/Budget.tsx` rewritten**: LedgerHeader (kicker "The Plan" + Fraunces date range), velocity line above tank computed client-side from last-4 vs prior-4 weeks of `/api/budget/indulgence/trend` ("INDULGENCE 30D 61% · $392 · ↓ 10.3pt vs prior" with sienna dot prefix, gold delta arrow), 7-band tank stack (DOM order top-down: fill, savings, essentials, debt, lifestyle, uncategorized, indulgence — with `flex-end` packing to bottom of container, indulgence sits at the very bottom of the band stack as "residue/sediment" framing — the orange line at the bottom of each mini-tank tells the story without red), tank overlay copy "INDULGENCE $X" / "SPENT $Y" (no more red). Routing/mini-tank/goals sections rebuilt as `.ledger-section` + `.ledger-row` with chevrons + `.cat-rule` prefixes (debt-gold for routing rows, sage for goals progress bars). MiniTank simplified to 3 bands (fill + slate essentials + sienna indulgence). **Today.tsx**: hero label "VICE 30D" → "INDULGENCE 30D" with leading sienna `.cat-dot`; query key + URL renamed. **Reconciliation.tsx**: SummaryRow now takes `hue` prop (replaces `emphasize`), every summary row leads with a `.cat-dot` of its category color; new `hueFor(group)` helper; every line item also leads with a `.cat-dot` so each transaction in the receipt is color-tagged by category at a glance (McDonald's sienna, Rogers slate, Capital One Payment gold, Acme Payroll mascot-purple, etc). **PhaseJourney.tsx**: metric key `vice_ratio` → `indulgence_ratio` for the % formatter. Verified live in preview at localhost:5174: Budget renders new tank with sienna velocity line + colored ledger rows; Today renders "INDULGENCE 30D 65.8%" with sienna dot; Reconciliation shows all 6 category dots in summary + per-line-item color dot. No console errors. 99 tests pass (85 worker + 14 shared, unchanged), typecheck clean, build clean (385.71 kB JS, 29.74 kB CSS, +2.00 kB JS / +1.59 kB CSS for the color system + Budget rebuild). Worker deployed `3809e84b-473a-4603-ac5a-8dcf6c8989e2`. Files added: `apps/worker/drizzle/0004_rename_vice_to_indulgence.sql`.
- Ledger Desk dark-shell typography pass (session 33): the dark app shell now has its own brand vocabulary, separate from the cream receipts. Conceptually, the dark background is a leather ledger desk and the receipts lie on top of it. **CSS additions** in `apps/client/src/index.css`: tokens `--rule-ink: rgba(255,255,255,0.08)`, `--rule-ink-strong: rgba(255,255,255,0.18)`, `--stamp-frame: rgba(232,228,236,0.55)`. `.btn-primary` rewritten as mono uppercase tracked (font-mono 12px, letterspacing 0.18em, radius 2px, padding 11/18). `.stamp` (and aliased `.btn-outline` so existing call sites upgrade for free) rendered as rubber-stamp: triple `box-shadow` inset (1px frame / 3px bg-color gap / 4px frame again) + `transform: rotate(-0.6deg)` + mono uppercase 11px/0.18em. Variants: `.stamp-square` (no rotation), `.stamp-gold`, `.stamp-purple`, `.stamp-danger`. Receipt-scoped override flips stamp frame to ink-on-cream so stamps inside `.receipt` (Edit/Archive on Account cards, Cancel sheets, etc.) auto-recolor. **`.ledger-page`** = 18/18/24 padding shell. **`.ledger-header`** = flex row with hairline-strong bottom rule, kicker (mono 10/0.28em uppercase muted) + Fraunces 30px title in `.ledger-header-title` column + optional mono subtitle + optional action right. **`.ledger-section`** = top hairline rule with absolutely-positioned `.ledger-section-kicker` punching through (mono 10/0.28em uppercase, optional gold `.num` prefix); optional `.ledger-section-meta` mirror on right. **`.ledger-row`** = flex space-between with bottom hairline rule; `.ledger-row-main` (label + hint stack) + `.ledger-row-value` (mono right) + `.ledger-row-chevron` (`&rsaquo;` glyph that slides 2px right and tints purple on `.tap:active`). **`.hero-num-dark`** = Fraunces 600 56px tabular -0.02em (`.gold` variant uses `--color-accent-gold`); `.unit` span is the trailing-% glyph at 28px/0.55 opacity; `.hero-num-dark-label` is mono 10/0.28em above. **Component:** `apps/client/src/components/LedgerHeader.tsx` (kicker + title + optional subtitle + optional action). **Pages updated:** Settings rebuilt as 7 ledger sections with chevron nav rows (01 Data / 02 Paycheque / 03 System / 04 Notifications / 05 Cron runs / 06 Audit / 07 Export). PhaseJourney Current section now uses `.hero-num-dark.gold` for the phase title in massive Fraunces. Routing (The Plan) gets LedgerHeader + Σ Planned trailing section. Debts page header is now the formatCents(totalOwed) hero in Fraunces with stamp-purple ADD DEBT; Splits section now uses ledger-rows. Grow Σ Total value renders the portfolio total in `.hero-num-dark.gold` with colored "+/- unrealized" mono pill below; Market and Holdings as ledger rows. Accounts and Merchants got LedgerHeader + BACK stamp. EditLog left alone (already a receipt). Today.tsx left alone (mascot+receipt is the moment). 99 tests pass (85 worker + 14 shared, unchanged), typecheck clean, build clean (383.71 kB JS, 28.15 kB CSS — +1.62 kB JS / +6.69 kB CSS for the ledger-desk system). No worker changes, no redeploy.
- UI polish pass — cream-paper receipts (session 32): dark app stays, but every `.receipt` surface now renders as cream paper (`#f2ece0`) with dark ink (`#1a141d`) and ink-muted secondary (`#6b6470`), floating on a soft drop shadow with subtle paper texture (inline SVG fractalNoise alpha 0.05) + two ink-stain radial gradients. Top + bottom edges show 10px-tall thermal-tear strips (radial gradient with 4px dark dots biting in from outside). `.receipt` rewritten in `apps/client/src/index.css` to redeclare `--color-bg-card`, `--color-text-primary`, `--color-text-muted`, `--color-hairline` locally — so existing receipt screens with markup like `text-[color:var(--color-text-muted)]` and inline `borderTop: 1px dashed var(--color-hairline)` auto-recolor without rewriting any of the 7 child files (Reconciliation, Review, Edit log, CC Statements, Goals, Holdings, Credit). New theme tokens: `--color-paper-shade`, `--color-ink`, `--color-ink-muted`, `--color-accent-gold` (`#c8a96a`) + `--color-accent-gold-soft` (`#e3c994`), `--color-hairline-ink`. Purple stays primary accent (matches mascot); gold is reserved for streaks/wins/sealed states (Reconciliation streak pill, Phase journey current-step ring). Mascot transparent: user provided `assets/Mascot Transparent.png` (RGBA, 74% transparent); regenerated `apps/client/public/brand/mascot-head.png` + `mascot-full.png` from it via PIL alpha-bbox crop + square-pad. Favicons left alone (need solid bg). `.mascot-sigil` simplified to plain 56×56 background-contain image with subtle drop-shadow — removed mix-blend-mode + radial mask workarounds for the old dark-bg PNG. New utility classes: `.hero-num` (Fraunces 600 / 44px tabular), `.label-tag` (mono 10px tracked uppercase ink-muted), `.pill-gold` + `.pill-purple`, `@keyframes paper-in` + `.paper-in`. Receipt-scoped helpers: `.ink-muted`, `.perf` (1px dashed ink hairline), `.row-tap` (full-bleed tappable row with active bg + transform). Today rebuilt to use the new vocabulary — header is mascot-sigil + Fraunces 30px "Declyne" + date label; hero is `useCountUp`-animated VICE 30D % in `.hero-num`; Phase / Review / Reconciliation as `.row-tap` lines; gold pill on streak ≥ 1; `** End of day **` footer. Failed approach: ink-stamp via Python+PIL color-distance keying on the original RGB-with-baked-bg PNG produced a solid purple square (mascot face/eyes were near-black like the bg). Reverted, then user supplied transparent source. 99 tests pass (85 worker + 14 shared, unchanged), `pnpm -r typecheck` clean, `pnpm --filter @declyne/client build` clean (382.09kB JS, 21.46kB CSS — +6.55kB CSS for new tokens + receipt rewrite). No worker code touched, no redeploy.
- Paycheque allocations rework + tab restructure (session 41): migration `apps/worker/drizzle/0007_period_allocations.sql` applied to remote D1 — drops `routing_plan`, creates `period_allocations` (id, pay_period_id FK, category_group CHECK in essentials/lifestyle/debt/savings/indulgence, label, planned_cents, matched_txn_id nullable FK, stamped_at nullable, stamped_by CHECK in user|csv_match, created_at). New pure helper `apps/worker/src/lib/allocationSeed.ts` with `draftAllocations` (recurring + debt mins + goals + indulgence carry) + `diffDraft` (additive — never overwrites stamped/edited rows by group+label collision). New worker route `apps/worker/src/routes/allocations.ts` with GET (rows + totals {paycheque, assigned, unassigned, stamped}), POST/PATCH/DELETE, POST /:id/stamp + /unstamp (idempotent), POST /draft (auto-runs detector + debts + goals + last-period indulgence carry), POST /auto-match (unique amount + ±3d window auto-stamps via stamped_by='csv_match', ambiguous skipped). `import.ts` calls `draftForPeriod` then `autoMatchAllocations` after period detect; response surfaces `allocations_drafted` + `allocations_matched`. `period_allocation` added to edit-log allowlist (replaces `routing_plan`). Old `apps/worker/src/routes/routing.ts` and `apps/client/src/pages/Routing.tsx` deleted. **Tab restructure shipped:** Today/Budget/Yield (Debts removed; debt cards + open tabs section now inside Budget below the tank); Grow renamed to Yield, `/grow` redirects to `/yield`; new wheat-stalk YieldIcon SVG. **Budget rebuilt:** "Where it goes" routing section deleted entirely; tank bands are now tappable buttons (`.tank-band-tap` CSS) that open `<AllocationSheet>` modal showing band-scoped line items with PAID stamp / Undo / × delete / + Add line; tank overlay gained "ASSIGNED $X · UNASSIGNED $Y" line under the LEFT TO SPEND number (UNASSIGNED tints sienna when negative, framing over-allocation visually); "Draft this paycheque" stamp-purple button below tank; new ledger sections "06 Debts" (links to /debts, totals owed) and "07 Open tabs" (per-counterparty drill-ins). New component `apps/client/src/components/AllocationSheet.tsx`. **Wording overhaul shipped per spec:** "The Plan"→"This paycheque" kicker, "Where it goes"→deleted, "Rebuild the plan"→"Draft this paycheque", "Mark sent"→"Paid". **Privacy scrub** on live D1: pre-Bowgull-rename "Lindsay (Mexico)" debt rename to "Bowgull (Mexico)" via wrangler d1 SQL; also REPLACE'd label in 1 period_allocation row + 2 edit_log rows. Tracked files clean. 15 new tests (allocationSeed.test.ts + allocations.test.ts), 140 total (126 worker + 14 shared). Worker deployed `7463a0b1-ce51-438e-90fa-22ff50dca04e`. Build: 400.45 kB JS / 31.11 kB CSS.
- Counterparties + chit-tear UX on Today (session 39): new migration `apps/worker/drizzle/0006_counterparties.sql` applied to remote D1 — adds `counterparties` table (id, name, default_settlement_method enum, archived_at, created_at), adds `counterparty_id TEXT REFERENCES counterparties(id)` to splits, backfills from existing `splits.counterparty` strings. New worker route `apps/worker/src/routes/counterparties.ts`: `GET /` aggregated open-tab balance per person (owes_you_cents - you_owe_cents = net_cents, direction owes_you|you_owe|settled, open_tab_count, last_tab_at; sorted by recency), `GET /:id` drill-in (counterparty + all splits + all split_events), `POST /` create, `PATCH /:id` rename/settle/archive. Pure helper `parseCounterpartyInput` exported for tests. `splits.ts` rewritten: GET joins counterparty name; POST accepts `counterparty_id` OR `counterparty_name` (looks up by name, creates new counterparty inline + writes edit_log if absent). Pure helper `parseSplitInput` exported. `counterparty` added to edit-log allowlist. **Today.tsx rebuilt** — header restructured as poster-style: 168px mascot bleeds left edge (negative margin), then 56px Fraunces DECLYNE wordmark masthead below, RCPT/WK/date subline as caption (after 3 sizing rounds: 56→96→160→168, user explicitly called the earlier sizes invisible/cramped). OPEN TABS section now per-counterparty with green/red signed amounts (owes-you sage `.tab-direction-pos`, you-owe sienna `.tab-direction-neg`), chit count, name links to drill-in. **Long-press detection** via pointerdown + 450ms setTimeout → opens chit prefilled with that counterparty + direction default; "+ tear from scratch" button at bottom opens blank chit. Discard via × stamp in chit corner (no swipe — conflicts with scroll). New `apps/client/src/pages/Counterparty.tsx` at `/budget/tabs/:id` — receipt with mascot + counterparty name as DISPLAY wordmark, running balance hero in sage/sienna, chits section with per-split rows + nested split_events sub-list. **CSS** (~95 lines): `@keyframes chit-tear-down` (rotate-down translate-down 320ms cubic-bezier landing), `@keyframes chit-crumple` (220ms ease-in scale+rotate fade), `.chit` (paper bg, drop-shadow stack, transform-origin top center, ::before is 12px jagged tear edge via mask + 135deg saw-tooth, ::after is bottom radial shadow), `.chit-discard`, `.chit-field` (transparent ink-bordered input), `.chit-trigger` (long-press affordance with .pressing state, user-select none), `.tab-direction-pos/-neg` color classes. **Splits never count toward institutional debt math** — phase, routing, non_mortgage_debt, indulgence ratio all blind to splits (already true; counterparties view doesn't change this). **Privacy fix on live D1**: a residual `split_lindsay_mexico` row (real-name leak) was discovered when the migration backfilled it as `cp_lindsay`; tracked files were already clean, leak was data-only from an earlier seed before the Bowgull rename. Purged with DELETE on splits + counterparties + split_events, then re-seeded with Bowgull. **Seed updates** in `apps/worker/drizzle/seed_test.sql`: 4 counterparties + 3 fictional Toronto splits (Marcus Chen $47.50 owes-you Lady Marmalade brunch in Leslieville; Bowgull owes Priya Shah $82 from Bar Raval tapas in Little Italy; Diego Alvarez $36 owes-you Golden Turtle Vietnamese dinner-for-two on Ossington — Vietnamese mid-range pricing $17 mains × 2 + appetizer + drinks ≈ $72 split in half). Mirrored as `seed_counterparties_one_off.sql` and applied to live D1. **Architecture decisions locked** for next session: Budget tab will become the GL with Tank as the live gauge and per-counterparty tabs / debt cards / routing / goals as views into the same `transactions` store; payments are transactions tagged to splits/debts (not a separate concept) — primary path auto-match from CSV import on amount + 3d window, secondary stamp-PAID from drill-in. **Verified live** at localhost:5174: mascot at 168px next to DECLYNE Fraunces masthead reads as poster-style header; OPEN TABS shows 4 rows colored by direction; long-press on Marcus tore prefilled chit; tear-from-scratch tore blank chit; Diego drill-in renders +$36 hero with Golden Turtle chit. 14 new tests in `counterparties.test.ts` (109 worker total, 14 shared unchanged). Worker deployed `05111738-eaf6-4bba-b6ab-10ad033d8ce8`. Caveat: `splits.counterparty` text column kept alongside the new FK for now; future cleanup could drop the text column once all reads are FK-only.
- Recurring-bills detector — finishes Today screen (session 37): new pure helper `apps/worker/src/lib/recurring.ts` exports `detectRecurring(txns, today, horizonDays)` and `predictNextPayday(latestPeriod, today, horizonDays)`. Detector groups merchant_id-tagged negative txns by merchant, requires ≥3 occurrences and category group in `{essentials, debt, transfer}`, computes median interval days as cadence (rejects <5 or >35), median amount as predicted bill, predicts next_due = last_seen + cadence, filters to within `horizonDays` and sorts ascending. Payday = latest pay_period.end_date + 1. `/api/today` extended: pulls last 90d of merchant-tagged charges with category group via single SQL join, runs detector with HORIZON_DAYS=14, stitches bills + payday into `printing_ahead` array `{kind, label, amount_cents, due_date, days_until}`, exposes `next_bill` for the NEXT block. Today.tsx NEXT block now renders `{merchant_name} {amount} · Nd`; PRINTING AHEAD now renders `<ul>` with day-count gutter, payday gets leading mascot-purple `.cat-dot.income` and `+` prefix on amount. 8 new tests in `recurring.test.ts` (95 total, 81 worker + 14 shared). Worker deployed `070d5c0b-0760-40d9-8547-2baee2030b1b`. Caveat: detector flags Loblaws as a "bill" because it shows up weekly in essentials — groceries vary in amount but the median is fine for forward-looking signals; gate on amount-stability in v2 if noisy.
- Today screen redesign (session 36): new `apps/worker/src/routes/today.ts` with `GET /api/today` returning `{ rcpt_days, last_indulgence }`. `rcpt_days` = days since earliest `transactions.posted_at` (proxy for install). `last_indulgence` = newest negative-amount txn joined to category group `indulgence`, returns `{ posted_at, amount_cents (positive), description_raw, days_ago }` or null. Wired in `apps/worker/src/index.ts`. `apps/client/src/pages/Today.tsx` rebuilt to session 35 wireframe: header DECLYNE wordmark + `RCPT NNNN · WK NN · DAY MON DD` subline (ISO week, padded counters); tap-cycle hero (button cycling `$X LEFT IN TANK / Nd to payday`, `Nd / period end_date`, `N weeks kept`) reads `/api/budget/tank` for remaining + days_remaining; NEXT block + PRINTING AHEAD section render honest empty-state copy until recurring detection ships; single-line `Next reconciliation · sunday · Nd` computed client-side from `getDay()`; OPEN TABS section pulls `/api/splits` (counterparty + direction + days-since-created + remaining cents per row, `+ tear from scratch` link to `/debts` until splits-redesign rewires it to a sheet); signals rows demoted to Phase / Indulgence (`last Nd ago · $X` with sienna `.cat-dot` prefix) / Review / Streak (gold pill when streak > 0); footer `* * between receipts * *`. Old `useCountUp` hook removed. Worker deployed `beae5b2f-63c1-4300-a6fe-03e9951383bb`. 99 tests pass, typecheck + build clean (387.81 kB JS / 28.62 kB CSS).
- Receipt-motif rollout pass 3 (session 30): Goals, Holdings, and Credit pages converted to full-receipt chrome matching session 28-29 template (mascot sigil + display wordmark + count subhead, perforated sections, `** End of __ **` footer). Goals: wordmark "GOALS", count "{n} on the books", action row Add/Show-archived in first perforated section, per-goal lines with name + target_date label, progress / target / remaining / pct, slim 1px progress bar; footer `** End of ledger **`. Holdings: wordmark "HOLDINGS", count "{n} lots", Add-holding button section, per-lot lines with symbol + wrapper label, units / avg cost; footer `** End of book **`. Credit: wordmark "CREDIT", count "{n} snapshots", Add-snapshot button section, per-snapshot lines with date + source label, score / util% / on-time days, small Delete underline; footer `** End of bureau **`. Sheets stay as bottom-sheet cards. No worker changes, no new tests. Receipt-motif coverage: Today, Reconciliation, Review, Edit log, CC Statements, Goals, Holdings, Credit (8 surfaces).
- Receipt-motif rollout pass 2 (session 29): Review, Edit log, and CC Statements pages all converted to full-receipt chrome (mascot sigil + display wordmark + count subhead, perforated sections, `** End of __ **` footer). Review queue items are now perforated lines (reason+date label, description, amount, inline category select + Resolve). Edit log wordmark "AUDIT TAPE" with filter inside the receipt, entries show `entity_type.field` + timestamp + `entity_id · actor · reason` + `old → new` in mono. CC Statements wordmark "STATEMENTS" with Add/Derive buttons inline; rows show card name + date + MIN/DUE/PAID + balance. StatementSheet kept as bottom-sheet card (sheets are actions, not records). Receipt-motif coverage: Today, Reconciliation, Review, Edit log, CC Statements (5 surfaces). No worker changes.
- Reconciliation as full-receipt screen (session 28): new `GET /api/reconciliation/week` route in `apps/worker/src/routes/reconciliation.ts` returns Sunday-to-today txns (joined with merchant/account/category) plus aggregated totals by category group (income, essentials, lifestyle, vice, debt, transfer, uncategorized cents). New `apps/client/src/pages/Reconciliation.tsx` mounted at `/reconcile` — full receipt with mascot sigil + RECONCILIATION display wordmark + "WEEK OF Apr 19–25" band, perforated sections (Streak, Summary with +/− and Net, Line items grouped by day SUN/MON/...), Mark-done button copy "I kept the receipts" → sealed timestamp, `** End of week **` footer. Vice rows + vice line items in `--color-accent`. Today's Reconciliation row simplified to a `<Link to="/reconcile">` (button removed; sealing now happens on the dedicated screen). Settings System card gained Reconciliation link next to Phase journey. No new tests (route is pure SQL aggregation; existing reconciliation tests cover the helpers). 99 tests pass, typecheck clean, build clean (376.99kB JS). Worker deployed `4cf10595-01e2-4612-9c52-6343e7ea30a0`.
- Ledger Desk round 2 + splits payment matching (session 42): **Ledger Desk:** Phase Journey 03 Transitions converted from custom `<ul>` to `.ledger-row` system (phase name left + trigger_rule hint + date right-aligned value + metrics sub-grid below each row). Merchants page rebuilt: two `<section className="card">` blocks replaced with `ledger-section` -- 01 FILTER (stamp-style Unverified/All/Verified toggle buttons replacing btn-outline, search field) + 02 MERCHANTS (count in section-meta, rows as `.ledger-row` with display name + normalized key hint + verified status colored ok/muted + txn count + chevron). Merchants EditSheet Cancel changed from `btn-outline` to `stamp stamp-square`. Grow (now Yield) kicker fixed from `§ GROW` to `§ YIELD`. Grow Recommendation section rebuilt: "Get recommendation" changed from `btn-primary` to `stamp stamp-gold`; when result is set, renders as `.ledger-row` with action+symbol+wrapper in label + reason as hint + cited signals as a second row -- no more generic freeform div. **Splits payment matching:** migration `apps/worker/drizzle/0008_splits_txn_fks.sql` applied to remote D1 -- adds `source_txn_id` + `settlement_txn_id` nullable TEXT FKs to `splits` (additive, no data change). Drizzle schema updated in `apps/worker/src/db/schema.ts`. New `autoMatchSplits(env)` function in `apps/worker/src/routes/import.ts`: for each open split (no settlement_txn_id, no closed_at), looks for transactions within ±3 days of split created_at with matching signed amount (owes_josh = positive incoming, josh_owes = negative outgoing); when exactly one match found, auto-settles: sets `settlement_txn_id`, sets `remaining_cents=0`, sets `closed_at`, inserts `split_event` with note "auto-matched on import", writes edit_log `split_auto_matched`. Ambiguous (multiple candidates) or no-match splits are skipped. Import response now includes `splits_settled` count. `Counterparty.tsx` gains `settle` mutation + gold `Paid` stamp button on each open (not closed_at) chit -- calls `POST /api/splits/:id/event` with `delta_cents = -remaining_cents, note = 'marked paid'`, invalidates counterparty + counterparties queries on success. **Verified live:** Phase Journey 03 TRANSITIONS shows clean hairline-ruled rows with dates right; Merchants All shows 20 ledger-rows with verified/unverified coloring + chevrons; Counterparty Marcus Chen shows gold PAID stamp on Lady Marmalade brunch chit. **Debug pass:** 140 tests pass (126 worker + 14 shared, unchanged), typecheck clean, build clean (401.53 kB JS / 30.60 kB CSS). Worker deployed `4f93cdac-6c5e-4bef-b1dc-34f0d37e8869`. **Files added:** `apps/worker/drizzle/0008_splits_txn_fks.sql`. **Files changed:** `apps/client/src/pages/PhaseJourney.tsx`, `apps/client/src/pages/Merchants.tsx`, `apps/client/src/pages/Grow.tsx`, `apps/client/src/pages/Counterparty.tsx`, `apps/worker/src/db/schema.ts`, `apps/worker/src/routes/import.ts`.
- Today screen receipt-motif rebuild (session 27): `apps/client/src/pages/Today.tsx` rebuilt as one continuous thermal receipt. Single `<section className="receipt stub-top stub-bottom">` wraps the full page. Header: mascot sigil + DECLYNE wordmark (`.display`) left, date in tracked uppercase mono below, settings cog SVG right. Four line-item rows separated by `borderTop: 1px dashed var(--color-hairline)` perforations -- Phase (links to /phase), Vice 30d, Review queue (links to /review), Reconciliation (with Mark-done button). Footer: `** End of day **` centered tracked mono. All `.card`/`.card-hero` wrappers removed. No CSS changes needed -- `.receipt`, `.stub-top`, `.stub-bottom`, `.display`, `.mascot-mark`, mono inheritance already present. 99 tests pass, typecheck clean, build clean. No worker changes, no redeploy. Commit `d538d86`.

## What's NOT built yet (next session priorities)

### The Paycheque Program (sessions 72 → 75) — COMPLETE

All four sessions shipped. Session 66 audit's A/B/C/D backlog fully cleared.

### The REBUILD_PLAN behavioral rebuild (sessions 77 → 81) — COMPLETE

All 11 sessions (A/B/C/D/E/F/G/H/I/J/K) shipped across sessions 77–81. The payment plan is the spine; every behavioral surface (Today queue, Paycheque committed/recommended, Reconciliation adherence, Forecast, Goals projection, Period history, Subscriptions, Tax year) now reads from the unified substrate built in session 72.

### Pre-Paycheque-Program backlog (all closed)

**A. Action vocabulary rollout** — SHIPPED session 67.
**B. Payment plan commitment layer** — SHIPPED session 68 + session 71.
**C. Progressive vocabulary RPG** — SHIPPED session 75.
**D. Notification expansion** — SHIPPED session 71.

---

### Standing items (not part of the upgrade program)

1. **iOS `cap add ios`** (blocked on physical device). iOS project folder doesn't exist yet, `cap:run` will fail. When a physical iPhone + Mac with Xcode are available, run `npx cap add ios` from `apps/client` after ensuring `capacitor.config.ts` `webDir` points to `dist`. Splash screen + iOS app icon deferred until then.

2. **OpenAI zero-data-retention application** (session 48 deferred). Manual application form, not code. Until granted, the "GPT never does arithmetic" rule keeps PII out of prompts.

3. **Showcase/multi-user groundwork — Phases 2–6** (session 51 designed, not shipped). Phase 1 (depersonalize the schema) shipped in session 51. Remaining: P2 add `users` table + nullable `user_id` FK on 22 personal-data tables, P3 `DEMO_TOKEN` env + `demoGuard` middleware (read-only demo mode), P4 legal docs polish for demo mode, P5 Cloudflare Pages deploy, P6 `WHERE user_id = ?` enforcement everywhere. These can run in parallel with the accounting upgrade below or after — they don't block each other.

---

### The Accounting Upgrade Program (sessions 52–60, the big one)

**Why this exists.** Declyne today is a stack of clever rules — phase engine, indulgence ratios, splits, debts, allocations, holdings, routing, credit snapshots, behaviour snapshots — each its own island with its own logic, none of them reconciling against each other. There's no underlying model that says "everything must balance." This program turns Declyne from "ad-hoc rules over a transaction store" into **personal-grade double-entry accounting software** — Wealthsimple/QuickBooks/Bench-class rigor sized for one person's life — with the existing behavioral overlay (phases, indulgence, signals) sitting cleanly on top of provably-correct books rather than running parallel to them.

**Why nobody else does this.** Mint/Wealthsimple/Copilot/Monarch are aggregators — read-only views of bank data, no double-entry, no AR/AP, no period close. YNAB does envelopes, not books. QuickBooks/Xero do real double-entry but are built for businesses with vendors/customers/invoices, hostile to personal use, expensive, ugly. Nobody ships *real bookkeeping rigor sized for one person's life* — counterparties as AR/AP subledger entries, debts as proper liability accounts with amortization, holdings at cost basis, net worth as a derived equity number that reconciles because it has to. Declyne's brand was already built for this without naming it: receipts are accounting documents, the Ledger Desk CSS system is literally accounting iconography, "I kept the receipts" is bookkeeping language.

**Scope rule for the entire program.** **No new UI or UX is being added by this program.** Same Today/Budget/Yield tabs, same Settings, same screens. What changes is the substrate underneath. New screens that *do* appear (Net Worth, Trial Balance, /budget/plan) are minimal additions to existing tabs. The brand voice and design system are locked.

**Decisions locked across the program** (do not re-litigate without explicit instruction):
- **Account naming:** hierarchical paths stored (`Assets:Cash:TDChequing`), rendered flat in dropdowns
- **Equity accounts:** `Equity:Opening Balance`, `Equity:Retained Earnings`, `Equity:Unrealized Gains/Losses`
- **Backfill strategy:** post unknowns to `Equity:Opening Balance`, reclassify later (option A — friendlier than refusing)
- **Period granularity:** weekly close (matches Sunday reconciliation ritual), monthly view layered on top
- **Multi-currency:** USD positions held in USD, only converted at sale (option B — investment side is personal-only, won't ship publicly)
- **Counterparties:** NET model — one signed account that flips, not separate AR/AP sub-accounts per person
- **Investment cost basis:** ACB (adjusted/weighted average — Canadian T5008 requirement)
- **Trial balance enforcement:** strict — any unbalanced JE rejected at write time, transaction fails, very loud error messages
- **Cash basis vs accrual:** implicit accrual — CC charge = expense + liability immediately (already true, formalized)
- **Payment plan optimizer:** hybrid (severity-first then avalanche), manual severity (no auto-inference), splits never count, charge velocity assumed to continue, AI writes rationale only — no external text input
- **Payment link page design:** locked — cream receipt, mascot top-right 64px (transparent `mascot-head.png`), Fraunces "from" + Bowgull, hero amount in Fraunces with CAD subscript, three ink-only copy stamps (email/amount/security answer), one line of plain text instructions ("Open your bank app, send via Interac e-Transfer, paste these in"), footer "** sent via Declyne **" with only the D in mascot purple, 90-day expiry disclaimer below. **No bank-specific deep-link buttons** — research (session 51 follow-up) confirmed 0 of the Big 6 Canadian banks (TD, RBC, CIBC, BMO, Scotiabank, Simplii) expose a usable Universal Link or URL scheme for "compose new Interac e-Transfer." Only EQ Bank has Interac-related Universal Link paths (`/pay-and-transfer/interac/*`) and they're recipient-side, undocumented, and EQ covers ~1% of Canadian banking. Interac itself has no consumer URL standard. Bundling speculative schemes in `LSApplicationQueriesSchemes` carries App Store review risk. Optional future addition: a single row of App Store links framed as "don't have your bank app installed?" — those always work, no scheme risk.
- **Interac email:** set once in Settings with the ability to edit. Pulled automatically when a payment link is generated. No per-link override prompt — if Josh wants to send from a different address, he edits the Settings value first.

---

#### Session 52 — E-transfer payment links — SHIPPED in session 57

See the entry at the top of "What's built". Public page is HTML-from-worker at `https://declyne-api.bocas-joshua.workers.dev/pay/<token>`; no domain bought, no Pages deploy. Stamp lives on `they_owe` chits only. Auto-disable on settle. 21 new tests.

#### Session 53 — GL foundation — SHIPPED in session 58

See GL foundation entry in "What's built". GL tables live in D1 (31 tables). Trial balance live at `GET /api/gl/trial-balance`. Backfill ran: 173 JEs, delta=0. Worker `b8979ccc`. 23 new tests (193→216).

#### Session 54 — AR/AP migration (counterparties) — SHIPPED in session 59

See AR/AP migration entry at top of "What's built". Per-CP NET asset account at `Assets:Receivable:<Name>`. Backfill ran clean: 4 splits + 3 events posted, trial balance still 0. Worker `857e5c8a`. 15 new tests (216→231). Counterparties LIST aggregation deferred — drill-in returns `gl_balance_cents` alongside existing splits aggregation.

#### Session 55 — Liabilities migration (debts + CC) — SHIPPED in session 60

See entry at top of "What's built". `debts.account_id` FK live. Backfill resolves linked CC debts to existing bank-account GL accounts; free-standing debts get fresh `Liabilities:Loans:<Name>` + opening-balance JE. `splitLoanPaymentLines` + `computeInterestAccrued` + `statementMismatchCents` pure helpers wired (auto-call deferred until first non-zero-APR loan txn). `GET /api/debts` returns `gl_balance_cents`. Worker `697d1c1e`. 15 new tests (231→246). 5 caveats logged for cleanup in session 64 (Net Worth view).

#### Session 56 — Assets migration (holdings) — SHIPPED in session 61

See entry at top of "What's built". `holdings.account_id` FK live. Backfill creates `Assets:Investments:<Wrapper>:<Symbol>` per lot, posts opening JE from cost basis. Pure helpers `holdingCostCents`, `holdingGlPath`, `inferCurrency`, `computeAcb` (weighted-average), `buyJeLines`, `sellJeLines` (realized gain/loss against `gla_equity_realized`), `mtmDeltaLines` (vs `gla_equity_unrealized`) all wired with 20 tests. MTM auto-call into `/api/market/fetch` deferred. Yield-page migration to GL deferred. Worker `109757e9`. 20 new tests (246→266).

#### Session 57 — Bank reconciliation rebuild — SHIPPED in session 62

See entry at top of "What's built". `journal_lines.cleared_at` live. Per-account GL balance / cleared balance / uncleared list rendered in the receipt with sage RECONCILED / sienna N UNCLEARED badges. Tap to clear posts edit_log; seal acknowledges outstanding when totalUncleared > 0 and writes `reconciliation_seal` (or `reconciliation_seal_outstanding`) row per account. Streak survives unchanged. Worker `914d184b`. 11 new tests (266→277).

#### Session 58 — Period close + Net Worth — SHIPPED in session 63

See entry at top of "What's built". `period_close` table live with UNIQUE index on period_end. `closeWeek` snapshots trial balance (refuses unbalanced), locks JEs in range, idempotent. `postJournalEntry` rejects backdated writes with `period_locked` (reversals exempt). `GET /api/gl/net-worth` derived from GL — surfaced as a Net worth ledger row on Budget below the tank (not Today; per design call). New `/settings/trial-balance` read-only page with manual close-this-week button. `reverseJournalEntry` helper + route live but no EditLog UI yet. Auto-close fires from `reconciliation/complete` post-seal. Worker `a9eaa465`. 14 new tests (277→293).

#### Session 59 — Severity column + payment plan kernel — SHIPPED in session 64

See entry at top of "What's built". Migration `0018_debt_severity.sql` adds severity column (default 'current'). Pure kernel `paymentPlan.ts` ships `computePlan` with all helpers. `POST /api/debts/:id/severity` + `GET /api/plan` live. Severity dropdown on debt edit sheet. Plan output not cached yet (caching lands with AI work in session 60). 25 new tests (293→318). Worker `2f1a72cd`.

#### Session 60 — Payment plan UI + AI rationale — SHIPPED in session 65

See entry at top of "What's built". `/budget/plan` page live with hero + per-debt rows + AI rationale section (gpt-4o-mini, rate-limited 10/hr, skipped when capacity=0). plan_cache table stores hashed-input rationale rows. `draftForPeriod` calls `computePlan` and feeds `plan_debts` into the seeder. Severity colors per spec on debt cards + role tag from plan. Today injects plan-recommends row when paycheque landed within 24h. Worker `0e551edc`. 15 new tests (318→333). **The Accounting Upgrade Program is complete.**

---

### What this program explicitly does NOT add
- **No new tabs.** Today/Budget/Yield + Settings stays.
- **No new top-level navigation.** Net Worth and Trial Balance are sub-pages of Settings/Today.
- **No collections letter parsing or any external text input to AI.**
- **No automatic severity inference** (manual only).
- **No card-freeze toggle.**
- **No splits in payment plan math.**
- **No multi-debt consolidation suggestions, balance-transfer recommendations, or financial advice generated by AI** (those are advice we shouldn't give).
- **No invoice/quote/billing concepts** (this is personal, not business).
- **No multi-user changes** (those are the Phases 2–6 program, separate).

### Total scope
9 sessions (52 → 60). Each ends green per the ritual: tests + typecheck + build + memory + CLAUDE.md update + commit. Worker redeploys via CI on every push to main. At no point does the user sit on a half-migrated GL with broken screens — each session lands in a working state with the previous behavior intact + new substrate underneath.

## iOS redeploy ritual (free provisioning, no Apple Dev account)

Free provisioning expires every 7 days. Set a calendar reminder or notice when the app stops launching.
When it expires: plug in phone, `pnpm cap:run`, hit play in Xcode. Two minutes.

## Session-end ritual (mandatory, every session)

Every session must end with three steps, in order:
1. **Memory update** — append a new session entry to `project_declyne.md` in the auto-memory dir with what shipped, files touched, commit hash, deploy version.
2. **CLAUDE.md update** — move anything new from "NOT built" to "What's built", bump the session number in the heading, update any infra/version strings.
3. **Debug pass** — `pnpm test`, `pnpm -r typecheck`, `pnpm --filter @declyne/client build`. All three must be green. Worker redeploy only if worker code changed. Commit the result. Do not hand off a session with red tests or uncommitted work.

## Rules (locked, do not change without explicit instruction)

- **Privacy: the dev identity for this project is Bowgull. Never write the user's real/legal name (or any personally identifying real-world name) into any tracked file, commit message, comment, copy, or memory. If you find a real-name leak, flag it immediately and replace with "Bowgull". Git author/committer must always be `Bowgull <256685449+Bowgull@users.noreply.github.com>` — never override.**
- All money: integer cents. Never floats.
- Every financial mutation writes `edit_log`.
- CSV parsing is client-side only. Worker receives parsed rows, never raw files.
- Dedup key: `SHA256(date|description|amount|accountId)`.
- No em dashes anywhere: copy, comments, code.
- GPT never does arithmetic. All numbers in AI prompts come from our computed signals.
- Three tabs only: Today / Budget / Yield. Debts tab removed; debt cards + splits move into Budget. No more tabs without explicit approval.
